# 2026-08-08 Liuary-002 公共日志

## npm 自动发布排查归档（404 secret 名字 + 403 2FA 冲突）

- **时间**：2026-08-08
- **操作人**：Archiver（归档官）
- **类型**：排查经验归档

### 排查背景

GitHub Actions workflow 自动发布 npm 包 `openfeel@1.0.1` 失败，经历两个阶段的错误诊断，最终定位根因并确定解决方案。

### 诊断链

| 阶段 | 错误现象 | 根因 | 修复 |
|------|---------|------|------|
| 1 | 404 Not Found | `.github/workflows/ci.yml` 引用 `secrets.NPM_TOKEN`，仓库实际 Secret 名为 `OPENFEEL_AUTO_NPM`，名字不匹配致 token 取空 | ci.yml 改引用 `secrets.OPENFEEL_AUTO_NPM`（提交 e5485ec） |
| 2 | 403 Forbidden + 2FA required | 使用 automation token（legacy），与包级强制 2FA 冲突 | 改用 Granular token（Read and write + Bypass 2FA 开启 + 指定包） |

### 关键认知

1. **npm publish 404 的真实含义**：npm registry 对空/无效/权限不足的 token 故意返回 404（非 401/403），防止攻击者通过返回码推断包是否存在。`publish 时的 404 ≠ 包不存在`，通常指认证失败。
2. **npm 已弃用 legacy token**：自 2025 年 11 月起只支持 Granular access token，automation token 已被移除且与包级 2FA 冲突。
3. **Bypass 2FA 覆盖包级 2FA**：Granular token 的 Bypass 2FA 选项设为 true 时，覆盖账号级和包级 2FA 要求，CI 发布无需 OTP，包级 2FA 安全不降级。

### 解决方案

- 创建 Granular token（权限 Read and write + Bypass 2FA 开启 + 指定包 openfeel）
- 更新 GitHub Secret `OPENFEEL_AUTO_NPM`
- 包级设置保持 "Require two-factor authentication or a granular access token with bypass 2fa enabled"

### 知识沉淀

| 分类 | 条目 | 内容 |
|------|------|------|
| troubleshooting.md | npm publish 404/403 诊断链：secret 名字不匹配 + 2FA 冲突 | 两阶段诊断链 + 关键认知（404 真实含义、legacy token 弃用、Bypass 2FA）+ 官方文档 |
| setup.md | CI/CD npm 自动发布配置：Granular token + Bypass 2FA | token 配置 + workflow 写法 + 排查清单 |

### 官方文档依据

- https://docs.npmjs.com/about-access-tokens
- https://docs.npmjs.com/configuring-two-factor-authentication

### 涉及文件

- `.github/workflows/ci.yml`（已修改：secret 引用名 NPM_TOKEN → OPENFEEL_AUTO_NPM，提交 e5485ec）
- `.openfeel/kb/troubleshooting.md`（+1 条目）
- `.openfeel/kb/setup.md`（+1 条目）
- `.openfeel/kb/index.md`（更新分类概览、各分类摘要、最近更新）
