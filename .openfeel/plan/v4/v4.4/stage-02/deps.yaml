# v4.4-stage-02 操作方案依赖声明
# Auto-generated by Schemer, 2026-07-14
# 依赖类型: hard = 必须完成, soft = 弱依赖, mutual_exclusion = 串行互斥

stages:
  v4.4-stage-02:
    ops:
      op-001:
        file: "op-001.md"
        title: "修复 flow.json 日志上下文缺失 + 公域日志降噪"
        deps: []
        type: hard

      op-002:
        file: "op-002.md"
        title: "实现 REV 闭环：blocking REV 阻塞 done 推进"
        deps:
          - "op-001"  # hard: 共享 flow-manager.ts（advanceStagePhase 签名变更必须先于 REV 检查）
        type: hard

      op-003:
        file: "op-003.md"
        title: "实现 git commit 自动提交机制"
        deps:
          - "op-001"  # soft: 共享 flow.ts（不同区域：op-001 不修改 flow.ts，仅依赖项目构建可用）
          - "op-002"  # soft: 共享 flow.ts（不同区域：git 脏区检查插入点在 REV 检查之后）
        type: soft

      op-004:
        file: "op-004.md"
        title: "实现日志强制落档骨架"
        deps:
          - "op-001"  # hard: 共享 flow-manager.ts（advanceStagePhase 签名变更必须先于骨架触发逻辑）
        type: hard

      op-005:
        file: "op-005.md"
        title: "实现自动推进询问（Feel prompt 强化）"
        deps:
          - "op-004"  # hard: 共享 feel.md 模板文件（三份同文件，需串行修改避免合并冲突）
        type: hard

      op-006:
        file: "op-006.md"
        title: "自测与回归验证"
        deps:
          - "op-001"  # hard: 需 op-001 产出完成后才可验证
          - "op-002"  # hard: 需 op-002 产出完成后才可验证
          - "op-003"  # hard: 需 op-003 产出完成后才可验证
          - "op-004"  # hard: 需 op-004 产出完成后才可验证
          - "op-005"  # hard: 需 op-005 产出完成后才可验证
        type: hard

# 依赖关系图 (→ 表示 hard, -→ 表示 soft)
#
# op-001 ──────→ op-002 ──→ op-003 (soft)
#    │              │
#    │              └──────────→ op-006
#    │
#    └──────→ op-004 ──→ op-005 ──→ op-006
#
# 推荐执行顺序: op-001 → op-002 → op-004 → op-005 → op-003 → op-006
# （op-003 可提前执行，与 op-002/op-004/op-005 无 hard 冲突）

# 文件冲突矩阵
# flow-manager.ts: op-001 + op-002 + op-004（全部串行）
# flow.ts:         op-002 + op-003（串行）
# public-logger.ts: op-001（独占）
# executor.md×3:   op-003（独占）
# feel.md×3:       op-004 + op-005（串行）
