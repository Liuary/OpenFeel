# op-001: lint i18n + kb 健康 + CLI-Agent 对齐

## 目标

为 v5.4-stage-01 实现三项能力：

1. **`openfeel lint i18n`**：校验 `src/core/i18n-data/zh-CN.ts` 与 `en.ts` 键一致性（空值、中文版独有键、英文版独有键）。
2. **`openfeel lint kb`**：扫描 `.openfeel/kb/*.md` 中的文件引用（markdown 链接 + 代码引用），检测过期（文件已不存在）引用。
3. **CLI-Agent 能力对齐**：为 CLI 已有的 roadmap / health / recover / wizard 四个命令创建 `/opfx:*` skill（最小 viable 版本），并同步 feel.md 技能映射表（中英文模板 + 项目部署副本）。

## 实施步骤

- [x] 步骤1：新建 `src/commands/lint.ts`，实现 `lint` 命令组（`lint i18n` / `lint kb` 两个子命令），参考现有命令模式（roadmap.ts 命令组 + i18n.ts 的 `t()`/`getCliLang()`）
- [x] 步骤2：在 `src/cli/index.ts` 静态导入并注册 `registerLintCommand`（文件头注释同步更新）
- [x] 步骤3：在 `src/core/i18n-data/zh-CN.ts` 与 `en.ts` 新增 `lint` 域（lint 命令输出字符串）与 `help.lint*` 帮助键（双语成对）；在 `src/core/i18n.ts` 导入新域并加入 `zhDomains` / `enDomains`
- [x] 步骤4：新建 4 个最小 skill 文件：`.opencode/skills/roadmap/SKILL.md`、`.opencode/skills/health/SKILL.md`、`.opencode/skills/recover/SKILL.md`、`.opencode/skills/wizard/SKILL.md`（参考 check-kb/SKILL.md 的 frontmatter 结构，每个 10-15 行）
- [x] 步骤5：更新 feel.md 技能映射表：`src/core/templates-data/agents/zh-CN/feel.md`、`src/core/templates-data/agents/en/feel.md`、`.opencode/agents/feel.md`（三份各追加 4 行 `/opfx:*` 映射）

## 产出文件

- `src/commands/lint.ts`（新建）
- `src/cli/index.ts`
- `src/core/i18n-data/zh-CN.ts`
- `src/core/i18n-data/en.ts`
- `src/core/i18n.ts`
- `.opencode/skills/roadmap/SKILL.md`（新建）
- `.opencode/skills/health/SKILL.md`（新建）
- `.opencode/skills/recover/SKILL.md`（新建）
- `.opencode/skills/wizard/SKILL.md`（新建）
- `src/core/templates-data/agents/zh-CN/feel.md`
- `src/core/templates-data/agents/en/feel.md`
- `.opencode/agents/feel.md`

## 自测清单

- [x] `npm run build` 通过，无 TypeScript 编译错误
- [x] `npm test` 全部通过（298/298），无回归
- [x] `openfeel lint i18n` 输出校验结果（✅ 422 键一致）
- [x] `openfeel lint kb` 输出过期引用检测结果（扫描 5 个 kb 文件，报告 1 条真实过期引用）
- [x] `openfeel --help` 出现 lint 命令且 help 翻译正常（无 Missing key 警告）
- [x] 4 个新 skill 文件存在且格式符合（frontmatter name/description + 步骤，13-14 行）

- **阶段**：exec_running
- **最多重试**：3

## 修正记录

| 时间 | 说明 | 操作者 |
|------|------|--------|
| 2026-08-07 | lint kb 过滤规则细化：首版将代码片段/主目录/占位符误报为过期引用（12 条），增加 `~`、括号、逗号、`..`、反斜杠过滤与已知项目目录前缀白名单后收敛为 1 条真实报告 | Executor |
| 2026-08-07 | 超范围更新 `test/core/update.test.ts`：新增 4 个 skill 后 build.js 注入的 Skill 定义 8→12，该测试硬编码 skipped 数量 20→24（属任务 3 连带更新） | Executor |
| 2026-08-07 | 产出物含构建自动注入文件 `src/core/template-loader.ts`（feel.md 模板更新）与 `src/core/update.ts`（Skill 定义 12 个），由 build.js 生成非手改 | Executor |
| 2026-08-07 | roadmap skill 路径采用实际代码结构 `.openfeel/roadmap/`（任务示例中的 `.openfeel/plan/v5/roadmap-v5.md` 与实际 `src/core/plan/roadmap.ts` 不符，以现有实现为准） | Executor |
