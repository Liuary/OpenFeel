# op-005：CI/CD 集成

- **阶段**：v1.0.0-stage-02（发布工程）
- **最多重试**：3

## 目标

创建 `.github/workflows/ci.yml` GitHub Actions 工作流：push/PR 触发，Node.js 20.x/22.x 双版本矩阵，`npm ci` → `npm run build` → `npm test`。

## 实施步骤

- [x] 1. 确认 `.github/workflows/` 目录（已存在，ci.yml 为 v0.1.0 初期创建的初始文件）
- [x] 2. 创建/更新 `.github/workflows/ci.yml`：`on: [push, pull_request]`（任务模板格式）+ 20.x/22.x 矩阵 + checkout@v4 + setup-node@v4 + npm ci + build + test
- [x] 3. 验证 `.gitignore` 未忽略 `.github/`（`git check-ignore` 返回未被忽略 ✓）
- [x] 4. 本地构建与测试通过（op-003 已验证：`npm run build` ✅ + 395 测试全通过，本次仅改 YAML 触发条件不影响构建）

## 产出文件

- `.github/workflows/ci.yml`（更新）

## 自测清单

- [x] ci.yml 存在且结构正确（name、on、jobs、strategy.matrix、steps 齐全）
- [x] Node 矩阵含 20.x 与 22.x
- [x] 步骤顺序：checkout → setup-node → npm ci → build → test
- [x] `.gitignore` 未忽略 `.github/`
- [x] `npm run build` + `npm test` 本地通过

## 偏差记录

- ci.yml 并非新建：文件已存在于 v0.1.0 初期（提交 3498e05），本次按任务模板将 `on` 触发条件从 `push/pull_request branches: [main]` 更新为 `on: [push, pull_request]`（全部分支触发），其余步骤与任务模板一致。
