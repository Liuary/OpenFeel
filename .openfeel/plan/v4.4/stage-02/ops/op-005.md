# op-005：实现自动推进询问
- **阶段**：v4.4-stage-02
- **前置**：op-004（soft，共享 feel.md 模板和 .opencode/agents/feel.md 文件）
- **负责 Agent**：Executor
- **最多重试**：3
- **参考**：kb/patterns.md #CLI 原子管理模式、kb/architecture.md #多语言模板数据管线

## 目标

在 Feel Agent 提示词中强化自动推进决策纪律：当日志阶段进入 `plan_passed` 且项目 `auto_advance=disabled` 时，Feel 在推进到 `scheme_pending` / `exec_running` 前必须询问用户是否开启自动推进。

## 实施步骤

### A. feel.md 模板（zh-CN）：新增自动推进询问规则

- [ ] A1. 打开 `src/core/templates-data/agents/zh-CN/feel.md`
- [ ] A2. 在「## 核心职责」的「3. **管理流水线**」段落中（约第56行），在"具体的阶段推进通过..."之后，新增自动推进决策纪律段落。建议在描述完多阶段并行规则后新增子段落：

  ```markdown
  #### 自动推进决策纪律

  当阶段进入 `plan_passed` 且项目的 `auto_advance` 设为 `disabled`（即手动执行模式）时：
  1. **必须询问用户**：Feel 在推进到 `scheme_pending` / `exec_running` 前，必须通过 `question` 工具询问用户是否开启自动推进。
  2. **用户同意**：Feel 通过 `openfeel flow` CLI 或调用 FlowManager API 将 `auto_advance` 设为 `enabled`，之后按自动模式继续推进。
  3. **用户拒绝**：Feel 保持 `auto_advance=disabled`，每次阶段推进前均需向用户确认（手动执行模式）。
  4. **禁止静默推进**：`auto_advance=disabled` 时禁止 Feel 不询问用户直接推进流水线。
  ```

- [ ] A3. 插入位置建议在「4. **决策权**」之前（约第65行），与"流水线管理"职责保持连贯

### B. feel.md 模板（en）：同步英文版自动推进询问规则

- [ ] B1. 打开 `src/core/templates-data/agents/en/feel.md`
- [ ] B2. 在对应位置新增英文版自动推进规则：

  ```markdown
  #### Auto-Advance Decision Rules

  When a stage enters `plan_passed` and the project's `auto_advance` is set to `disabled` (i.e., manual execution mode):
  1. **Must ask the user**: Before advancing to `scheme_pending` / `exec_running`, Feel must ask the user via the `question` tool whether to enable auto-advance.
  2. **User agrees**: Feel sets `auto_advance` to `enabled` via the `openfeel flow` CLI or FlowManager API, then continues in auto mode.
  3. **User declines**: Feel keeps `auto_advance=disabled` and requires user confirmation before each stage advance (manual execution mode).
  4. **No silent advancement**: When `auto_advance=disabled`, Feel must not advance the pipeline without asking the user.
  ```

### C. .opencode/agents/feel.md：同步自动推进询问规则

- [ ] C1. 打开 `.opencode/agents/feel.md`
- [ ] C2. 执行与步骤 A2 完全相同的修改（.opencode 下只存在中文版）

### D. 构建验证

- [ ] D1. 运行 `npm run build`，确保构建通过
- [ ] D2. 运行 `npm test`，确保全量通过

## 产出文件

- `src/core/templates-data/agents/zh-CN/feel.md`（修改：新增自动推进决策纪律段落）
- `src/core/templates-data/agents/en/feel.md`（修改：英文版同步自动推进规则）
- `.opencode/agents/feel.md`（修改：同步自动推进规则）

## 自测清单

- [ ] feel.md（zh-CN）模板包含 `auto_advance=disabled` 时必须询问用户的规则
- [ ] feel.md（en）模板包含对应的英文版规则
- [ ] .opencode/agents/feel.md 包含相同规则
- [ ] `npm run build` 零错误通过
- [ ] `npm test` 全量通过（275/275）

## 困难点

无。本 op 仅涉及 Agent 提示词修改，不涉及代码逻辑。需确保中英文版本规则语义一致。
