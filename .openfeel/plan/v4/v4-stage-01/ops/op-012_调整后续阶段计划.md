# op-012：调整后续阶段计划（stage-02/03/04）

- **阶段**：v4-stage-01
- **状态**：done
- **前置**：op-001, op-002, op-003, op-004, op-005, op-009, op-010, op-011（必须完成所有 Agent 变更和引用清理后再调整后续阶段）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标
基于 stage-01 的 Agent 体系变更（15→7），重新评估并调整 stage-02/03/04 计划中的任务描述和依赖关系，确保后续阶段与新的 7 Agent 体系一致。

## 实施步骤
- [x] 1. 使用 `task(explore)` 探索 `.openfeel/plan/v4/` 下的 stage-02、stage-03、stage-04 目录
- [x] 2. 读取各阶段 status.md，记录当前任务描述
- [x] 3. 逐项检查并更新以下内容：
  - **Agent 名称引用**：将所有引用已删除 Agent 的名称替换为新的 7 Agent 体系
    - `architect` / `Architect Agent` → `reviewer` / `Reviewer`
    - `code` / `Code Agent` / `code-worker` → `executor` / `Executor`（根据上下文：Bug 修复由 Executor 负责）
    - `tester` / `Tester Agent` → `feel-tester` / `Feel Tester`
    - `auto-runner` / `AutoRunner` → `Feel`（调度职责）
    - `review-worker` / `test-writer` / `debug` / `ask` → 删除相关引用（职责已合并）
  - **阶段依赖**：检查 deps.yaml 中是否有引用已删除 Agent 的依赖声明
  - **任务范围**：若后续阶段有任务描述特定于某个已删除 Agent，调整任务范围
- [x] 4. 保守原则：
  - 仅修改明确的 Agent 名称引用，不改变任务目标和范围
  - 若某阶段任务的核心逻辑依赖已删除的 Agent 能力（如 worktree 机制），标注 `FIXME: v4 Agent 体系变更后需要重新评估此任务`
  - 不新增或删除任务项
- [x] 5. 更新每个阶段的 status.md「状态记录」表，添加 stage-01 变更记录

## 产出文件
- `.openfeel/plan/v4/v4-stage-02/status.md`（若存在）
- `.openfeel/plan/v4/v4-stage-03/status.md`（若存在）
- `.openfeel/plan/v4/v4-stage-04/status.md`（若存在）
- `.openfeel/plan/v4/deps.yaml`（若存在且需更新）

## 自测清单
- [x] stage-02/03/04 status.md 中无 `architect` / `code-worker` / `review-worker` / `auto-runner` / `test-writer` / `debug` / `ask` / `tester` 残留
- [x] 无 `AutoRunner` 残留
- [x] 无 `worktree` 残留（status.md 模板的 Worktree / Session 元数据字段为阶段级标记，非机制引用）
- [x] 各阶段的「当前责任 Agent」和「上一责任 Agent」字段值与新 7 Agent 体系一致
- [x] 阶段依赖关系（deps.yaml）中的 Agent 引用已更新（无需修改，已使用新名称）
- [x] 不存在依赖 worktree 等已删除机制的任务，无需标注 FIXME

## 修正记录
| 次数 | 时间 | 问题 | 修正内容 |
|------|------|------|----------|
