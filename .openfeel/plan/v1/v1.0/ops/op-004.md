# op-004：npm 发布准备

- **阶段**：v1.0.0-stage-02（发布工程）
- **最多重试**：3

## 目标

修复 package.json 中占位符 URL（repository.url / bugs.url），运行 `npm pack --dry-run` 验证产物完整性，确认 `files` 字段与实际产物一致，验证 `.gitignore` 未忽略 `.github/` 目录。

## 实施步骤

- [ ] 1. 读取 package.json 确认当前 `repository.url` 与 `bugs.url` 占位符
- [ ] 2. 修复 `repository.url` → `https://github.com/Liuary/OpenFeel.git`
- [ ] 3. 修复 `bugs.url` → `https://github.com/Liuary/OpenFeel/issues`
- [ ] 4. 运行 `npm pack --dry-run` 验证产物完整性
- [ ] 5. 核对 `files` 字段（`["dist", "bin", "schemas"]`）与实际产物一致
- [ ] 6. 验证 `.gitignore` 未忽略 `.github/` 目录
- [ ] 7. 运行 `npm run build` 确认构建无回归

## 产出文件

- `package.json`

## 自测清单

- [ ] package.json 中无占位符 URL
- [ ] `npm pack --dry-run` 产物包含 dist/、bin/、schemas/
- [ ] `files` 字段与实际产物一致
- [ ] `.gitignore` 未忽略 `.github/`
- [ ] `npm run build` 通过

## 偏差记录

- （待回写）
