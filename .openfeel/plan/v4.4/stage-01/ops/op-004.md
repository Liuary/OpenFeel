# op-004：首次使用语言选择提示（init.ts 入口）

- **阶段**：v4.4-stage-01
- **前置**：op-003（全局配置读写函数必须先实现）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

在 `src/core/init.ts` 的 `initProject()` 函数入口，`ensureInfoJson()` 之前，调用 `isFirstUse()` 检测。若为首次使用 OpenFeel，弹出中英双语提示让用户选择全局默认语言，结果写入 `~/.openfeel/config.json`。非交互环境（CI/CD）输出提示信息并默认 `zh-CN`，不阻塞流程。

## 实施步骤

### 1. 导入全局配置函数

- [ ] 在 `src/core/init.ts` 的 import 块中新增：
```typescript
import { ensureInfoJson, isFirstUse, getGlobalConfig, setGlobalConfig, DEFAULT_GLOBAL_CONFIG } from './workspace/identity.js';
```
- [ ] 注意：已有 `ensureInfoJson` 的导入，仅追加 `isFirstUse` / `setGlobalConfig` / `DEFAULT_GLOBAL_CONFIG`

### 2. 实现首次使用语言选择函数

- [ ] 在 `init.ts` 中新增 `ensureGlobalConfig()` 函数（放在 `promptLanguage()` 函数附近）：
```typescript
/**
 * 确保全局配置文件存在。
 * 首次使用时提示用户选择全局默认语言，非交互环境默认 zh-CN。
 * 仅当全局配置文件不存在时触发。
 */
async function ensureGlobalConfig(): Promise<'zh-CN' | 'en'> {
  if (!isFirstUse()) {
    // 已配置，返回现有全局语言
    return getGlobalConfig().lang;
  }

  // 非交互环境（CI/CD / 无 TTY）
  if (!process.stdout.isTTY) {
    console.log('首次使用 OpenFeel：检测到非交互环境，全局默认语言设置为 zh-CN。');
    console.log('使用 openfeel config set lang <zh-CN|en> 可修改。');
    setGlobalConfig({ ...DEFAULT_GLOBAL_CONFIG, lang: 'zh-CN' });
    return 'zh-CN';
  }

  // 交互环境：中英双语提示
  return new Promise((resolve) => {
    const rl = readline.createInterface({
      input: process.stdin,
      output: process.stdout,
    });

    console.log('\n🌐 欢迎使用 OpenFeel！请选择全局默认语言');
    console.log('   Welcome to OpenFeel! Please select your global default language:');
    console.log('');
    console.log('   1. English (en)');
    console.log('   2. 中文 (zh-CN)');
    console.log('');

    rl.question('请输入选项 (1/2) / Enter choice (1/2) [2]: ', (answer) => {
      rl.close();
      const trimmed = answer.trim().toLowerCase();
      const lang: 'zh-CN' | 'en' =
        (trimmed === '1' || trimmed === 'en' || trimmed === 'english') ? 'en' : 'zh-CN';

      setGlobalConfig({ ...DEFAULT_GLOBAL_CONFIG, lang });
      console.log(lang === 'en'
        ? `\n✓ Global language set to English. You can change it later with: openfeel config set lang`
        : `\n✓ 全局语言已设置为中文。后续可通过以下命令修改：openfeel config set lang`);
      console.log('');
      resolve(lang);
    });
  });
}
```
- [ ] **关键设计**（参见 kb/patterns.md #双语 CLI 交互模式）：
  - 选择使用 readline（已导入）而非 `@inquirer/prompts`，因为此时项目尚未初始化，无需额外依赖
  - 中英双语提示明确显示两个选项
  - 选择后**立即**写入全局配置，确保后续流程可读

### 3. 集成到 `initProject()` 流程

- [ ] 在 `initProject()` 函数中，**`ensureInfoJson()` 之前**插入 `ensureGlobalConfig()` 调用：
```typescript
export async function initProject(projectPath: string, cliLang?: string): Promise<InitResult> {
  const created: string[] = [];
  const updated: string[] = [];

  // 0. 确保全局配置存在（首次使用时交互选择语言）
  await ensureGlobalConfig();

  // 1. 创建 .openfeel/ 目录结构
  const dirs = createWorkspace(projectPath);
  // ...
```
- [ ] **位置理由**：在项目初始化之前确定全局语言偏好，使后续项目语言选择可以基于全局默认值。但 `ensureGlobalConfig()` **不阻塞** `initProject()` 的正常流程（非交互环境静默通过）。

### 4. 调整项目语言选择逻辑

- [ ] 在 `initProject()` 的第 4a 步（语言选择），**将全局默认语言作为项目语言的默认值**：
  - 当前逻辑：`cliLang` 参数 > 交互式选择 > 默认 `zh-CN`
  - 调整后：`cliLang` > 交互式选择的**默认值改为全局默认语言** > 全局默认语言
```typescript
  // 4a. 语言选择：CLI --lang 参数 > 交互式选择 > 全局默认语言
  const globalLang = getGlobalConfig().lang;
  let selectedLang: 'zh-CN' | 'en';
  if (cliLang === 'en' || cliLang === 'zh-CN') {
    selectedLang = cliLang;
    console.log(`Agent 提示词语言: ${selectedLang === 'en' ? 'English' : '中文'}`);
  } else if (cliLang) {
    console.warn(`无效的 --lang 值 "${cliLang}"，回退到交互式选择`);
    selectedLang = await promptLanguage();
  } else {
    selectedLang = await promptLanguage();
  }
```
- [ ] **不改变** `promptLanguage()` 本身的默认值逻辑（它已有自己的交互默认 `zh-CN`）。op-004 仅关心全局层的语言选择，项目层保持向后兼容。

### 5. 向后兼容性保证

- [ ] 已有项目（`~/.openfeel/config.json` 已存在）→ `ensureGlobalConfig()` 直接返回，不弹出任何提示
- [ ] 已有项目 `.info.json` 无 `lang` 字段 → `getLang()` 回退 `zh-CN`（已有逻辑不变）
- [ ] 已有项目 `.info.json` 有 `lang: 'en'` → 全局配置不影响项目级语言选择

### 6. 类型检查

- [ ] `npx tsc --noEmit` 无类型错误

## 产出文件

- `src/core/init.ts` — 新增 `ensureGlobalConfig()` 函数，调整 `initProject()` 入口流程（~50 行增量）

## 自测要点

- [ ] 首次运行（无 `~/.openfeel/config.json`）→ 终端显示中英双语提示
- [ ] 选择 `1`（English）→ `~/.openfeel/config.json` 写入 `{ "lang": "en", "projects": {} }`
- [ ] 选择 `2`（zh-CN）→ 写入 `{ "lang": "zh-CN", "projects": {} }`
- [ ] 非 TTY 环境（如 CI/CD）→ 不弹出提示，直接写入 `zh-CN` 并输出提示信息
- [ ] 第二次运行（已有 config.json）→ `ensureGlobalConfig()` 直接返回，无任何提示
- [ ] 已有项目执行 `openfeel init` → 全局语言选择不影响项目已有 `.info.json` 的 `lang` 字段
- [ ] `npx tsc --noEmit` 无类型错误

## KB 参考

- 参见 kb/patterns.md #双语 CLI 交互模式 — init 选择→持久化→update 读取 三段式架构
- 参见 kb/patterns.md #向后兼容的可选配置字段模式 — 只读访问器 + 不强制覆盖
