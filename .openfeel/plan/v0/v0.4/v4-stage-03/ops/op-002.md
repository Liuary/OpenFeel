# op-002：Executor 方案一致性回写

- **阶段**：v4-stage-03
- **前置**：无（独立 op，与 op-001 并行；修改 executor.md，与 op-001 的 reviewer.md 无文件冲突）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标
在 Executor Agent（`executor.md`）中增加「方案一致性回写」步骤：编码完成后自动比对操作方案声明的产出文件与实际产出，将偏差标记回方案文件的修正记录表中，落实 Roadmap stage-03 验证项："编码完成后方案文件有偏差标记"。

## 实施步骤
- [ ] 1. **读取本方案文件**：执行前先完整阅读 `op-002.md`，确认所有步骤和自测清单理解无误
- [ ] 2. 读取当前 `.opencode/agents/executor.md`（123 行），确认现有结构（核心职责 → 工作规则 → 前置校验 → 工作流程 → 模型选择 → 注意事项）
- [ ] 3. 在「## 工作流程」中，将第 4 步"自测验证"扩展为含方案回写的版本：

  将原有第 4 步：
  ```
  4. **自测验证**：按方案自测清单逐项验证，运行项目构建命令确认无编译错误。测试不通过则记录原因并进入重试机制。
  ```

  替换为：
  ```
  4. **自测验证**：按方案自测清单逐项验证，运行项目构建命令确认无编译错误。测试不通过则记录原因并进入重试机制。
  5. **方案一致性回写**：编码和自测完成后，执行以下回写步骤（详见「方案一致性回写」章节）。
  ```

  并将原第 5 步"输出报告"序号改为 6。

- [ ] 4. 在「## 工作流程」之后、「## 模型选择与约束」之前，插入新章节「## 方案一致性回写」，内容如下：

  ```markdown
  ## 方案一致性回写

  编码和自测通过后，必须执行方案一致性回写，确保方案声明与实际产出对齐。

  ### 回写时机
  在「工作流程」第 4 步（自测验证）通过后立即执行。

  ### 回写步骤

  1. **收集声明产出**：从操作方案文件的「## 产出文件」章节提取所有声明的文件路径列表
  2. **收集实际产出**：通过 `glob` 工具扫描方案中声明的文件模式，结合本次操作实际修改/新增的文件，生成实际产出列表
  3. **比对差异**：
     - 方案声明了但未产出 → 标记为"遗漏"
     - 实际产出了但方案未声明 → 标记为"超范围"
     - 方案声明与实际一致 → 标记为"一致"
  4. **回写偏差**：若存在"遗漏"或"超范围"，在操作方案文件的「## 修正记录」表中追加一条记录：

     | 次数 | 时间 | 问题 | 修正内容 |
     |------|------|------|----------|
     | 自检 | {当前时间} | 方案一致性偏差 | 遗漏：{未产出文件列表}；超范围：{多产出文件列表} |

     若完全一致，也在修正记录表中追加一条：
     | 次数 | 时间 | 问题 | 修正内容 |
     |------|------|------|----------|
     | 自检 | {当前时间} | 方案一致性检查 | 声明与产出一致，无偏差 |

  5. **告知 Feel**：在自测报告的「方案一致性回写结果」字段中注明比对结果（一致 / 存在偏差及详情）

  ### 偏差不阻塞
  方案一致性回写仅记录偏差，不阻塞流水线推进。若偏差为"超范围"（多产出文件），需在自测报告中说明多产出文件的必要性。
  ```

- [ ] 5. 校对全文：确认新增章节的 markdown 层级（`##` / `###`）正确，无覆盖原有章节内容

## 产出文件
- `.opencode/agents/executor.md`（修改）

## 自测清单
- [ ] `executor.md` 中存在「## 方案一致性回写」章节
- [ ] 该章节位于「## 工作流程」之后、「## 模型选择与约束」之前
- [ ] 「工作流程」中第 4 步（自测验证）之后存在第 5 步「方案一致性回写」
- [ ] 原第 5 步「输出报告」序号已变更为第 6 步
- [ ] 「方案一致性回写」包含 5 个步骤（收集声明/收集实际/比对差异/回写偏差/告知 Feel）
- [ ] 回写步骤中明确指出偏差回写到方案文件的「## 修正记录」表
- [ ] 包含"偏差不阻塞"说明
- [ ] 包含"完全一致也记录"的兜底逻辑
- [ ] 所有原有章节（核心职责/工作规则/前置校验/模型选择/注意事项）完整保留
- [ ] 文件编码为 UTF-8

## 修正记录
| 次数 | 时间 | 问题 | 修正内容 |
|------|------|------|----------|
| 自检 | 2026-07-02 | 方案一致性检查 | 声明与产出一致，无偏差 |
