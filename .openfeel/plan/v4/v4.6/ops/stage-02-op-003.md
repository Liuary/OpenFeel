# op-003：增强 AGENTS.md "禁止过度设计"规则
- **阶段**：v4.6-stage-02
- **前置**：无
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

修改 `AGENTS.md` 第 2 条（行 18~23），在现有四条判定项之后追加代码层面与架构层面的明确区分说明，保留所有现有内容不删减。

## 实施步骤

- [ ] Step 1：定位 `AGENTS.md` 第 23 行（`用户明确要求简洁实现时，以上阈值自动降低。` 之后）

- [ ] Step 2：在现有第 23 行后插入以下 3 行（新第 24-26 行）：

```markdown
    本规则同时约束代码实现与架构设计：
    - 代码层面：避免无意义的抽象层、过度包装、不必要的设计模式
    - 架构层面：无复用需求时不引入基类、中间件或设计模式包装
```

> **精确替换策略**：
> - oldString 取原第 23 行 `用户明确要求简洁实现时，以上阈值自动降低。`
> - newString 取 `用户明确要求简洁实现时，以上阈值自动降低。\n    本规则同时约束代码实现与架构设计：\n    - 代码层面：避免无意义的抽象层、过度包装、不必要的设计模式\n    - 架构层面：无复用需求时不引入基类、中间件或设计模式包装`
> - 使用 `edit` 工具精确替换，避免整段重写。

- [ ] Step 3：目视确认修改后第 2 条从行 18 到行 26，结构如下：

```
2. 设计应保持简洁，避免过度设计。以下任一情况视为可能过度设计，须与用户确认：
   - 新增或修改文件超过 3 个
   - 引入新抽象层（基类、中间件、设计模式包装）但无明显复用需求
   - 为单一功能引入第三方库或框架
   - 计划中包含过多未来扩展点
   用户明确要求简洁实现时，以上阈值自动降低。
   本规则同时约束代码实现与架构设计：
   - 代码层面：避免无意义的抽象层、过度包装、不必要的设计模式
   - 架构层面：无复用需求时不引入基类、中间件或设计模式包装
```

- [ ] Step 4：运行 `npx tsc --noEmit` 确认无类型错误（md 文件不参与 TypeScript 编译，此项为流程完整性检查）

## 产出文件

- `AGENTS.md`（修改，+3 行）

## 自测清单

- [ ] 原第 18~23 行内容完整保留，无删减
- [ ] 第 24-26 行为新增的三行代码层/架构层分离说明
- [ ] 新增行的缩进与原列表项一致（使用 4 空格缩进 `    `）
- [ ] Markdown 渲染预览：列表项和新增段落层次清晰
- [ ] `git diff` 确认变更仅 +3 行，无意外范围扩散
