# op-001: 工具链内化 + 一致性治理

- **阶段**：v5.1-stage-01
- **最多重试**：3

## 目标

1. CLI 内化归档 git commit（done 阶段自动 git add + commit）
2. Agent 提示词格式统一（`#` 层级编号 + `1. **标题**：说明`）
3. AGENTS.md 模板同步（补全缺失的 4 节）

## 实施步骤

- [ ] 任务1：在 flow-manager.ts 的 advanceStagePhase 中，当目标 phase 为 done 时自动执行 git add + commit（execSync + try-catch 静默跳过），新增 i18n 键
- [ ] 任务2：扫描 .opencode/agents/*.md 与 templates-data/agents/zh-CN/*.md，统一职责列表格式为 `1. **标题**：说明`，中英双语同步
- [ ] 任务3：对比根 AGENTS.md 与 agents-md/zh-CN.md，将缺失的 4 节追加到 zh-CN.md，en.md 同步翻译，保留 /opfx: 技能映射表

## 产出文件

- `src/core/flow-manager.ts`
- `src/core/i18n/`（zh-CN.ts / en.ts）
- `.opencode/agents/*.md`（9 个）
- `src/core/templates-data/agents/{zh-CN,en}/*.md`
- `src/core/templates-data/agents-md/{zh-CN,en}.md`

## 自测清单

- [ ] `npm run build` 构建通过
- [ ] `npm test` 无回归
- [ ] flow advance 到 done 时触发 git commit（在有变更时）
- [ ] 非 git 仓库/无变更时静默跳过不阻塞
