# op-005：其他 Agent 适配 — Planner/Archiver 通过 Feel 间接写 flow.json
- **阶段**：v4.1-stage-03
- **前置**：op-004（feel.md 路由更新后，下游 Agent 适配以 Feel 为中心）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

更新 Planner 和 Archiver 的 Agent prompt，明确它们对 flow.json 的操作必须通过 Feel 间接完成（使用 `openfeel flow` CLI 命令委托），禁止直接修改 flow.json。同时更新 AGENTS.md 的 Agent 职责边界表同步变更。

## KB 参考

- 参见 kb/patterns.md #CLI 原子管理模式 — Agent 通过 CLI 命令操作数据文件，禁止直接 edit
- 参见 kb/architecture.md #15→7 Agent 精简体系 — 各 Agent 职责边界清晰，写操作归口 Feel

## 实施步骤

- [ ] 1. `read` 本方案文件完整内容（必做）
- [ ] 2. **planner.md 适配**（约 5 行追加）：
  - 在「核心职责」末尾追加一条：
    ```markdown
    5. **禁止直写 flow.json**：计划制定/变更完成后，通过 Feel 调用
       `openfeel flow advance --stage <id> --to <phase>` 推进流水线状态。
       不得直接 `edit` 或 `write` flow.json 文件。计划产出写入
       `.openfeel/plan/{stage}/plan.md`，由 Feel 读取后统一推进。
    ```
  - 在「拒绝条件」节或新增节注明：当计划被接受后，状态推进由 Feel 执行
- [ ] 3. **archiver.md 适配**（约 5 行追加）：
  - 在「核心职责」节（或「归档流程」节步骤后）追加：
    ```markdown
    ### 步骤 4（NEW）：推进流水线状态
    归档完成后，通过 Feel 调用 `openfeel flow advance --stage <id> --to done`
    将对应阶段标记为完成。Archiver **不直接修改** flow.json，所有流水线状态
    变更通过 Feel + CLI 命令原子操作完成。
    ```
  - 若已有类似内容，更新引用从"全局 phase"改为"阶段 phase"
- [ ] 4. **AGENTS.md 适配**（约 5 行变更）：
  - 在「跨 Agent 工具使用约束」第 3 条「职责边界」表中：
    - Planner 行追加"不直写 flow.json（通过 Feel 写入）"
    - Archiver 行追加"不直写 flow.json（通过 Feel 写入）"
  - 在「8 Agent 体系总览」表（若存在）中同步更新

## 产出文件

- `.opencode/agents/planner.md`
- `.opencode/agents/archiver.md`
- `AGENTS.md`（项目根目录）

## 自测清单

- [ ] planner.md 含"禁止直写 flow.json"或等效表述
- [ ] archiver.md 含"通过 Feel 调用 openfeel flow advance"或等效表述
- [ ] AGENTS.md 职责边界表中 Planner/Archiver 含有 flow.json 写入约束
- [ ] planner.md 和 archiver.md 文件行数在 80-120 目标范围内
- [ ] `grep -r "edit.*flow.json" .opencode/agents/` 无修改建议类结果（仅提示/禁止描述可保留）
