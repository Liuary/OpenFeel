# op-003：MERGED INTO op-002

- **阶段**：v4.3-stage-03
- **状态**：❌ 已合并

> **合并原因（REV-001）**：原 op-002（AGENTS.md 翻译 + build.js 修改）与 op-003（core-instructions 翻译 + build.js 修改）共享 `build.js` 文件。
> 在 Batch 2 中并行执行会导致后提交者覆盖先提交者的 build.js 修改。
> 已将所有内容合并入 [op-002](./op-002.md)，本文件保留作为历史记录。

## 原内容

原 op-003 包含：
1. 翻译 `templates-data/core-instructions/zh-CN.md` → `en.md`
2. 修改 `build.js` 的 `generateTemplateFromCoreMd()` 支持多语言 B64 注入
3. 修改 `build.js` 的 `validateCoreInstruction()` 适配多语言

以上全部并入 op-002 的 Part B（翻译）+ Part D（build.js core-instructions 注入）。

## 影响

- deps.yaml 中 op-003 条目已移除，依赖关系已合并到 op-002
- op-006 的 hard 依赖中 op-003 已替换为 op-002（op-002 已覆盖原 op-003 的产出）
