# op-003：REV blocking 标记 — Agent 语义 + 数据结构

- **阶段**：v4-stage-03
- **前置**：op-001（hard — op-001 修改 reviewer.md 审查维度表，op-003 在同一文件的「注意事项」中追加 blocking 相关条款，需确保 op-001 完成后 reviewer.md 结构稳定）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标
在 Reviewer Agent（`reviewer.md`）中引入 REV blocking 概念（非阻塞 REV 不中断流水线），并同步在 `flow-manager.ts` 的 `ReviewItem` 数据结构中增加 `blocking` 字段。落实 Roadmap stage-03 验证项："非阻塞 REV 不中断流水线"。

## 实施步骤
- [ ] 1. **读取本方案文件**：执行前先完整阅读 `op-003.md`，确认所有步骤和自测清单理解无误

### Part A：数据结构变更（flow-manager.ts）

- [ ] 2. 读取 `src/core/flow-manager.ts`，定位 `ReviewItem` 接口定义（约第 48-60 行）
- [ ] 3. 在 `ReviewItem` 接口中，于 `autoFixDetail` 字段之后新增 `blocking` 字段：

  ```typescript
  /** 是否阻塞流水线。
   * - true：REV 未关闭时阻止流水线进入下一阶段（阻塞性 REV）
   * - false（默认）：REV 不中断流水线，仅作为记录跟踪（非阻塞 REV）
   *
   * 方案级审查 REV 默认非阻塞（blocking=false），代码级审查 REV 默认阻塞（blocking=true）。
   * 审查者可根据严重程度覆盖默认值。
   */
  blocking?: boolean;
  ```

- [ ] 4. 检查 `flow.ts` 中 `flow review add` 命令的 `reviewItem` 构造逻辑（约第 292-302 行），在构造对象中增加 `blocking` 字段的默认值与 CLI 参数支持：

  修改构造对象，增加：
  ```typescript
  blocking: options.blocking !== undefined ? options.blocking : true,
  ```

  并在命令选项定义中增加：
  ```typescript
  .option('--blocking [boolean]', '是否阻塞流水线（默认 true）', 'true')
  ```

- [ ] 5. 在 `flow.ts` 的 `flow review add` 命令的输出中，增加 blocking 状态显示：

  在输出行（约第 337 行）中追加 blocking 标记：
  ```typescript
  const blockingLabel = reviewItem.blocking !== false ? '[阻塞]' : '[非阻塞]';
  console.log(`✓ ${blockingLabel} 审查条目已添加: ${revId}...`);
  ```

### Part B：Agent 语义变更（reviewer.md）

- [ ] 6. 读取 `.opencode/agents/reviewer.md`（已由 op-001 修改），定位「## 注意事项」章节
- [ ] 7. 在「## 注意事项」中追加 blocking 相关条款：

  ```markdown
  - REV 条目默认 `blocking=true`（阻塞流水线）。以下情况可设为 `blocking=false`：
    - 仅影响代码风格/命名建议，不影响功能正确性
    - 属于优化建议而非必要修复项
    - 方案级审查中发现的可后续修正的次要问题
  - 非阻塞 REV 的流水线行为：审查提交后 pipeline 直接推进到下一阶段（跳过 review_failed），REV 条目保持 open 状态跟踪直至 closed。
  - 每个操作（op）至少需要 1 条阻塞性 REV 被 closed 才能标记阶段为 review_passed。
  ```

- [ ] 8. 校对全文，确认 reviewer.md 的 blocking 条款与 op-001 新增的模式一致性条款不冲突

## 产出文件
- `src/core/flow-manager.ts`（修改 — ReviewItem 接口）
- `src/commands/flow.ts`（修改 — review add 命令支持 blocking）
- `.opencode/agents/reviewer.md`（修改 — 追加 blocking 条款）

## 自测清单
- [ ] `ReviewItem` 接口包含 `blocking?: boolean` 字段，含 JSDoc 注释
- [ ] `flow review add` 命令支持 `--blocking` 选项（默认 true）
- [ ] `flow review add` 命令输出中包含 `[阻塞]` 或 `[非阻塞]` 标记
- [ ] `reviewer.md` 的「注意事项」中包含 blocking 相关条款（3 条）
- [ ] reviewer.md 条款中明确列出可设为 non-blocking 的 3 种情况
- [ ] reviewer.md 条款中包含"非阻塞 REV 不触发 review_failed"的说明
- [ ] TypeScript 编译通过：`npx tsc --noEmit`
- [ ] 文件编码均为 UTF-8

## 修正记录
| 次数 | 时间 | 问题 | 修正内容 |
|------|------|------|----------|
