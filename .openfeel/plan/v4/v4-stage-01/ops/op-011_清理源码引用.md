# op-011：清理源码中对已移除 Agent 的引用

- **阶段**：v4-stage-01
- **状态**：pending
- **前置**：op-001, op-002, op-003, op-004, op-005, op-009（必须完成 Agent 文件变更后再清理源码引用）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标
清理项目源码（`.openfeel/` 域文件 + 其他配置文件）中对已删除 9 个 Agent 的引用，确保无残留、无断裂。

## 实施步骤
- [ ] 1. **全局 grep 扫描**（使用 task(explore) 并行扫描以下目录）：
  - `.openfeel/` — 配置文件、流水线定义、状态文件
  - `.opencode/` — instructions、skills、config
  - 项目根目录 `AGENTS.md`
  - `src/` — 若存在 TypeScript 源码
  搜索关键词：`architect`（非 `architecture` 上下文）、`code-worker`、`review-worker`、`auto-runner`、`test-writer`、`debug`（非 `debugger` 上下文）、`ask`（非 `task` 上下文）、`tester`（非 `feel-tester` 上下文）、`AutoRunner`、`worktree`
- [ ] 2. **分类处理搜索结果**：
  - **agent_id 引用**（如 `agent_type: "architect"` / `"architect"` / `"auto-runner"`）：删除或替换为 Feel 调度
  - **职责描述引用**（如「由 Architect 审查」）：替换为 `Reviewer`
  - **自动闭环引用**（如「AutoRunner 接管」）：替换为 Feel 调度描述
  - **注释/文档引用**：更新为新 Agent 体系
- [ ] 3. **重点清理以下文件**（基于已知引用点）：
  - `pipeline.yaml` — 移除对 auto-runner / code-worker / review-worker 的阶段定义
  - `flow.json` — 检查 agent 字段是否有废弃引用
  - `.openfeel/dev/dev_core.md` — 更新废弃 Agent 引用
  - `.openfeel/stages/` — 检查阶段文件中的 Agent 引用
  - `skills/` 目录 — 更新 skill 描述中的 Agent 名称引用
  - `AGENTS.md` — 已在 op-010 处理，此处做二次验证
- [ ] 4. **保守原则**：仅修改明确引用已删除 Agent 的行，不扩大修改范围。不确定时标注 FIXME 注释待人工确认

## 产出文件
- 根据 grep 结果确定，预期涉及文件 ≤ 5 个

## 自测清单
- [ ] 全局 grep 无 `architect`（指 Agent ID，非 architecture）残留
- [ ] 全局 grep 无 `code-worker` / `review-worker` / `auto-runner` / `test-writer` / `test-writer` 残留
- [ ] 全局 grep 无 `AutoRunner` 残留（指调度器名称）
- [ ] `tester`（指旧 Agent ID，非 feel-tester）的引用已清理或更新
- [ ] `pipeline.yaml` / `flow.json` 结构完整，JSON/YAML 格式校验通过
- [ ] 项目构建命令（`npm run build`）无报错
- [ ] 修改的文件列表记录在自测报告中

## 修正记录
| 次数 | 时间 | 问题 | 修正内容 |
|------|------|------|----------|
