# op-004：实现日志强制落档骨架
- **阶段**：v4.4-stage-02
- **前置**：op-001（hard，public-logger.ts 的 logMilestone 方法为骨架日志的底层，且共享 flow-manager.ts 文件）
- **负责 Agent**：Executor
- **最多重试**：3
- **参考**：kb/patterns.md #CLI 原子管理模式、kb/architecture.md #多语言模板数据管线（模板同步策略）

## 目标

在流水线核心 (`flow-manager.ts`) 的关键 phase 推进节点自动创建私域日志骨架文件，配合 feel.md Agent 提示词强化（告知"骨架已创建，填充即可"），确保关键节点日志不遗漏。

## 实施步骤

### A. flow-manager.ts：新增骨架创建函数

- [ ] A1. 打开 `src/core/flow-manager.ts`
- [ ] A2. 在文件顶部 import 区域（约第12行），新增导入：
  ```typescript
  import { writeFileSync } from 'node:fs';  // 已存在，检查是否已导入
  ```
  （当前第12行已导入 `writeFileSync`，无需重复）

- [ ] A3. 在类中新增私有方法 `createLogSkeleton`。建议放在 `startStage`/`endStage` 之后（约第596行附近）：

  ```typescript
  /**
   * 在私域日志目录创建带日期前缀的骨架文件
   * Agent 只需填充内容，无需手动创建文件
   * @param stageId 阶段 ID（如 v4.4-stage-02）
   * @param label 骨架标签（如 exec_running / review_pending / test_pending）
   */
  private createLogSkeleton(stageId: string, label: string): void {
    const username = this.getUsername();
    const now = new Date();
    const dateStr = this.formatDateStr(now);
    const logDir = resolve(this.projectPath, '.openfeel', 'users', username, 'log');

    // 确保私域日志目录存在
    if (!existsSync(logDir)) {
      mkdirSync(logDir, { recursive: true });
    }

    // 计算 NNN 序号（基于当日已有文件数 + 1）
    const nnn = this.computeSkeletonNnn(logDir, dateStr);
    const fileName = `${dateStr}-${String(nnn).padStart(3, '0')}.md`;
    const filePath = resolve(logDir, fileName);

    // 仅当文件不存在时创建（幂等性）
    if (!existsSync(filePath)) {
      const content = `# ${fileName.replace('.md', '')}
- **时间**：${now.toISOString()}
- **阶段**：${stageId}
- **节点**：${label}
- **状态**：待填充

> 此文件由流水线自动创建。请在此记录操作详情。
`;
      writeFileSync(filePath, content, 'utf-8');
    }
  }

  /** 获取当前用户名 */
  private getUsername(): string {
    try {
      const infoPath = resolve(this.projectPath, '.openfeel', '.info.json');
      if (existsSync(infoPath)) {
        const raw = readFileSync(infoPath, 'utf-8');
        const info = JSON.parse(raw) as Record<string, unknown>;
        if (info.user && typeof info.user === 'string') {
          return info.user;
        }
      }
    } catch {
      // 加载失败使用默认值
    }
    return 'unknown';
  }

  /** 格式化日期为 yyyy-mm-dd */
  private formatDateStr(date: Date): string {
    const yyyy = date.getFullYear().toString();
    const MM = (date.getMonth() + 1).toString().padStart(2, '0');
    const dd = date.getDate().toString().padStart(2, '0');
    return `${yyyy}-${MM}-${dd}`;
  }

  /** 计算当日已有文件数，返回下一个序号 */
  private computeSkeletonNnn(logDir: string, datePrefix: string): number {
    try {
      const entries = readdirSync(logDir);
      let maxNnn = 0;
      for (const entry of entries) {
        if (entry.startsWith(datePrefix) && entry.endsWith('.md')) {
          const nnnStr = entry.slice(datePrefix.length + 1, -3); // +1 跳过连字符
          const nnn = parseInt(nnnStr, 10);
          if (!isNaN(nnn) && nnn > maxNnn) {
            maxNnn = nnn;
          }
        }
      }
      return maxNnn + 1;
    } catch {
      return 1;
    }
  }
  ```

- [ ] A4. 需要在文件顶部确认 `readdirSync` 已导入（第12行已导入）

### B. flow-manager.ts：在 advanceStagePhase 中触发骨架创建

- [ ] B1. 在 `advanceStagePhase` 方法中（约第730行），更新 stage phase 之后、写入日志之前，新增骨架触发逻辑：

  ```typescript
  // 日志强制落档骨架：关键 phase 节点自动创建骨架文件
  const SKELETON_PHASES: PipelinePhase[] = [
    'exec_running',
    'review_pending',
    'test_pending',
    'archiving',
  ];
  if (SKELETON_PHASES.includes(targetPhase)) {
    this.createLogSkeleton(stageName, targetPhase);
  }
  ```

- [ ] B2. 插入位置建议在 `this.data.stages[stageName].phase = targetPhase;` 之后、`const pendingOpEntry = ...` 之前

### C. feel.md 模板（zh-CN）：新增日志骨架填充说明

- [ ] C1. 打开 `src/core/templates-data/agents/zh-CN/feel.md`
- [ ] C2. 在「## 日志记录纪律」节（约第99行）的「### 必须记录的事件」段落中，新增一条说明：
  ```markdown
  ### 骨架文件提示

  关键操作（推进到 exec_running / review_pending / test_pending / archiving）时，流水线会自动在私域日志目录创建带日期前缀的骨架文件。Feel 无需手动创建日志文件，看到骨架文件时填充内容即可。
  ```
- [ ] C3. 插入位置建议在「### 必须记录的事件」列表之后、「### 日志条目格式」之前

### D. feel.md 模板（en）：同步英文版骨架填充说明

- [ ] D1. 打开 `src/core/templates-data/agents/en/feel.md`
- [ ] D2. 在对应位置（约第103行）新增：
  ```markdown
  ### Skeleton File Note

  During critical operations (advancing to exec_running / review_pending / test_pending / archiving), the pipeline automatically creates skeleton files with date prefixes in the private log directory. Feel does not need to manually create log files; simply fill in the content when you see a skeleton file.
  ```

### E. .opencode/agents/feel.md：同步骨架填充说明

- [ ] E1. 打开 `.opencode/agents/feel.md`
- [ ] E2. 执行与步骤 C2 完全相同的修改（.opencode 下只存在中文版）

### F. 构建验证

- [ ] F1. 运行 `npm run build`，确保构建通过
- [ ] F2. 运行 `npm test`，确保全量通过

## 产出文件

- `src/core/flow-manager.ts`（修改：新增 createLogSkeleton* 系列方法 + advanceStagePhase 触发逻辑）
- `src/core/templates-data/agents/zh-CN/feel.md`（修改：新增骨架文件提示段落）
- `src/core/templates-data/agents/en/feel.md`（修改：英文版同步骨架文件提示）
- `.opencode/agents/feel.md`（修改：同步骨架文件提示）

## 自测清单

- [ ] 调用 `advanceStagePhase('stage-01', 'exec_running')` → `.openfeel/users/{username}/log/` 下出现当日日期的骨架文件
- [ ] 骨架文件内容包含阶段、节点、时间信息，且标记"待填充"
- [ ] 重复调用同一阶段同一 phase：不重复创建骨架文件（幂等性）
- [ ] 推进到非关键 phase（如 `plan_passed`）：不创建骨架文件
- [ ] feel.md 模板包含"骨架文件由流水线自动创建，填充即可"的说明
- [ ] `npm run build` 零错误通过
- [ ] `npm test` 全量通过（275/275）

## 困难点

1. **日期前缀格式与已有日志一致性**：骨架文件名格式需与 `AGENTS.md` 中定义的私域日志格式一致（`yyyy-mm-dd-NNN.md`）。当前私域日志规范（`AGENTS.md` 第81行）为 `yyyy-mm-dd-NNN.md`，`public-logger.ts` 中为 `{date}-{username}-{NNN}.md`。骨架文件采用私域日志格式（不含 username，因已在用户目录下），与 `AGENTS.md` 规范一致。

2. **骨架文件 vs 公域日志区分**：骨架文件写入私域日志目录（`.openfeel/users/{username}/log/`），公域日志通过 PublicLogger 写入（`.openfeel/log/`）。两者不冲突。
