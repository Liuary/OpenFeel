# op-007：archiver.md 扩充 — 知识去重触发条件 + 降级策略

- **阶段**：v4.1-stage-02
- **前置**：无（与 op-001~006 并行安全——修改不同文件）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

将 `archiver.md` 从 107 行扩充到 90–110 行（当前 107 行，需微调），明确知识去重的触发条件和降级策略。

> 参见 kb/architecture.md #知识库自动化体系 — 检索→去重→沉淀三环闭环已建立，archiver.md 需明确何时触发去重、去重失败时如何处理。

## 实施步骤

- [x] 1. 读取当前 `.opencode/agents/archiver.md` 完整内容（107 行）
- [x] 2. 在 `### 步骤 2：检索现有条目` 之前新增 `## 知识去重触发条件` 节：
  - **必须触发去重**（每次提取新知识条目前）：
    - 从操作记录中提取了新的架构决策、代码模式、排查经验
    - 知识条目标题或内容涉及已有分类中的已知领域
  - **可跳过去重**（以下场景无需调用 `findSimilarEntries`）：
    - 纯 Bug 记录归档（BUG → `.openfeel/bugs/`，不涉及 kb/）
    - 日志汇总类操作（log 归档，不涉及知识提取）
    - 完全新领域（标题关键词在 kb/index.md 中无任何匹配 → 跳过检索直接新增）
  - 判断流程：提取条目 → 查阅 kb/index.md 分类摘要 → 有关键词匹配 → 触发去重 → 相似度判断 → 更新或新增
- [x] 3. 在 `### 步骤 4b：新增条目` 之后新增 `## 去重失败降级策略` 节：
  - 当 `kb-dedup` 模块不可用时（`import` 失败、Node 环境不兼容）：
    1. 手动读取对应分类文件（如 `architecture.md`）的完整内容
    2. 提取所有 `## [+]` 条目标题，与新条目标题做关键词匹配
    3. 关键词匹配规则：提取标题中的核心名词（去除日期、编号），与现有条目标题比较
    4. 若有 ≥ 60% 的关键词重叠 → 标记为"疑似重复"，**不新增**，改为记录到 dev_last.md 待人工复核
    5. 若无匹配 → 标注 `"未去重，待人工复核"` 后新增条目
  - 重试机制：降级新增后，在下次会话启动时通过 `dev_last.md` 中的经验暂存提醒用户确认
- [x] 4. 适当精简 `### 步骤 2` 中 `findSimilarEntries` 的使用说明（当前 5 行 TypeScript 代码示例可压缩为 2 行描述）
- [x] 5. 最终检查：`wc -l` 确认行数在 90–110 范围内

## 产出文件

- `.opencode/agents/archiver.md`

## 自测清单

- [x] archiver.md 行数在 90–110 范围（`wc -l`）
- [x] `## 知识去重触发条件` 存在，明确"必须触发"和"可跳过"的场景分类
- [x] 触发条件中含判断流程图（查阅 kb/index.md → 关键词匹配 → 去重）
- [x] `## 去重失败降级策略` 存在，含手动关键词匹配步骤和 "疑似重复" 处理规则
- [x] 降级策略中含 `dev_last.md` 经验暂存回写路径
- [x] 原有的归档流程 4 步骤（提取→检索→判断→更新/新增）保持完整
- [x] 原有的归档内容表、流水线阶段枚举表保持完整
- [x] git diff 检查新增内容合理，未破坏原有结构
- [x] 文件编码 UTF-8

## 修正记录

| 次数 | 时间 | 问题 | 修正内容 |
|------|------|------|----------|
| 自检 | 2026-07-07 | 方案一致性检查 | 声明与产出一致（仅 `.opencode/agents/archiver.md`），无偏差 |
