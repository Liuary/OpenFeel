# op-008：构建验证

- **阶段**：v4.6-stage-01
- **前置**：op-001, op-002, op-003, op-004, op-005, op-006, op-007（全部变更完成后验证）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

运行构建和测试，验证 Vision Agent 的模板管线、Agent 定义和关联文档变更全部正确。

## 实施步骤

### 步骤 1：运行构建

执行 `npm run build`，确认构建成功无报错。

预期结果：`build.js` 自动遍历 `templates-data/agents/zh-CN/` 和 `en/` 下的 `.md` 文件（含新增的 `vision.md`），内联写入 `src/core/template-loader.ts`，构建成功退出。

### 步骤 2：验证 template-loader.ts 包含 vision 模板

读取 `src/core/template-loader.ts`，在 `AUTO-GENERATED-BEGIN` / `AUTO-GENERATED-END` 块中确认存在：
- `zh-CN.vision` 键对应的模板字符串（非空）
- `en.vision` 键对应的模板字符串（非空）

若构建后这两个条目缺失，说明 build.js 未正确遍历新模板，需检查 build.js 的遍历逻辑是否需要注册新 Agent（根据架构设计应不需要，但须验证）。

### 步骤 3：运行模板一致性校验

模板一致性校验由 `build.js` 的 `validateTemplates()` 函数执行（已集成在 `npm run build` 流程中），确认：
- 9 个 Agent（feel / planner / schemer / executor / reviewer / feel-tester / archiver / 事务官 / vision）的模板内容一致
- `.opencode/agents/vision.md` 的内容与 `src/core/templates-data/agents/zh-CN/vision.md` 一致

若一致性校验报错，需根据错误信息定位不一致的 Agent，修复后重新构建。

### 步骤 4：运行测试

执行 `npm test`，确认：
- 项目现有 ~298 个测试全部通过
- 无回归错误

### 步骤 5：Git 提交

所有验证通过后，执行：
```bash
git add -A
git commit -m "op-001~008: v4.6-stage-01 Vision Agent 全链路落地"
```

## 产出文件

- 无新增文件（纯验证，但构建可能更新 `src/core/template-loader.ts`）

## 自测清单

- [ ] `npm run build` 退出码为 0，无构建错误
- [ ] `src/core/template-loader.ts` 的 `AGENT_TEMPLATES` 对象中包含 `zh-CN.vision` 键（模板字符串非空）
- [ ] `src/core/template-loader.ts` 的 `AGENT_TEMPLATES` 对象中包含 `en.vision` 键（模板字符串非空）
- [ ] 模板一致性校验通过（期望输出 `✅ All 9 agent templates pass consistency check` 或类似信息）
- [ ] `npm test` 全部通过（298/298），无回归错误
- [ ] `.opencode/agents/` 目录下有 9 个 Agent 定义文件（含 vision）
- [ ] `src/core/templates-data/agents/zh-CN/` 和 `en/` 各含 9 个 `.md` 模板文件
- [ ] 所有变更已通过 `git commit` 纳入版本管理
