# op-006：更新 model-check 技能 — 角色映射回退表

- **阶段**：v4.6-stage-01
- **前置**：op-003（确定 vision 的 model 字段值 `alibaba/qwen-vl-plus`）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

更新 model-check 技能的角色映射回退表：
1. **补充既有遗漏**：新增 `utility.md` 条目（当前 8 个 Agent 只列了 7 个）
2. **新增 v4.6**：新增 `vision.md` 条目

最终回退表从 7 个条目扩展为 9 个条目。

## 实施步骤

> **重要**：修改前须先 `read .opencode/skills/model-check/SKILL.md` 确认当前文件内容与预期一致。使用 `edit` 工具精确替换。

### 步骤 1：替换角色映射回退表（行 44-52）

将当前的 7 行回退表替换为 9 行（含新增的 `utility.md` 和 `vision.md`）。

**旧文本**（精确匹配）：
```
| `feel.md` | `primary_reasoning`（主力推理） |
| `planner.md` | `reasoning`（推理） |
| `executor.md` | `fast`（快速） |
| `reviewer.md` | `cross_model`（异种推理） |
| `archiver.md` | `reasoning`（推理） |
| `schemer.md` | `reasoning`（推理） |
| `feel-tester.md` | `reasoning`（推理） |
```

**新文本**：
```
| `feel.md` | `primary_reasoning`（主力推理） |
| `planner.md` | `reasoning`（推理） |
| `executor.md` | `fast`（快速） |
| `reviewer.md` | `cross_model`（异种推理） |
| `archiver.md` | `reasoning`（推理） |
| `schemer.md` | `reasoning`（推理） |
| `feel-tester.md` | `reasoning`（推理） |
| `utility.md` | `fast`（快速） |
| `vision.md` | `multimodal`（多模态） |
```

### 步骤 2：最终验证

重新 `read .opencode/skills/model-check/SKILL.md`，确认：
- 回退表共 9 个条目
- `utility.md` → `fast`（快速），插入在 `feel-tester.md` 之后
- `vision.md` → `multimodal`（多模态），插入在 `utility.md` 之后
- 表头行未被修改
- 表前后上下文（步骤 3/4 描述文字和后续章节）未被影响

## 产出文件

- `.opencode/skills/model-check/SKILL.md`（修改）

## 自测清单

- [ ] 回退表从 7 行扩展为 9 行（8→9 Agent 覆盖：新增 utility + vision）
- [ ] `utility.md` 条目存在，模型角色为 `fast`（快速）
- [ ] `vision.md` 条目存在，模型角色为 `multimodal`（多模态）
- [ ] 表条目顺序：feel → planner → executor → reviewer → archiver → schemer → feel-tester → utility → vision
- [ ] 表头 `| Agent 文件 | 默认模型角色 |` 未被修改
- [ ] 表之前的段落（优先级 4 说明）未被修改
- [ ] 表之后的段落（步骤 3 检查 config.yaml）未被修改
- [ ] 文件编码和行尾未被改变
