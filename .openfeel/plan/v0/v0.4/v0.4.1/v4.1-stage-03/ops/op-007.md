# op-007：测试覆盖 — 多阶段并行推进场景测试 + 迁移脚本测试
- **阶段**：v4.1-stage-03
- **前置**：op-002（FlowManager）+ op-003（CLI）+ op-006（migrate）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

为 v4.1-stage-03 的多阶段独立状态机变更编写/扩展测试用例，覆盖：多阶段并行推进、迁移脚本、新 Schema 校验、CLI 命令参数变化。

## KB 参考

- 参见 kb/troubleshooting.md #fuzzyCorrectPhase 正则尾部下划线 — 边角 case 测试覆盖
- 参见 kb/troubleshooting.md #repair dry-run 误报 — dry-run 测试需验证 `fixed` 返回值语义

## 实施步骤

- [ ] 1. `read` 本方案文件完整内容（必做）
- [ ] 2. **新增测试文件 `test/commands/flow-migrate.test.ts`**（约 150 行）：
  - `describe('flow migrate')` 测试套件
  - **测试用例**：
    - [ ] `migrate 旧格式→新格式：旧 phase 下沉到 active stage`
      - 创建旧格式 flow.json（`pipeline.phase = "exec_running"`，`current = {stage:"stage-01",op:"op-001"}`）
      - 执行 migrate → 验证 `stages["stage-01"].phase = "exec_running"`，`pipeline.phase = "active"`
    - [ ] `migrate 旧格式：所有已完成 stage 的 phase 设为 "done"`
      - stages 中有两个 stage，仅 current.stage 对应的那个得到旧 phase，另一个得 `"done"`
    - [ ] `migrate 已是新格式 → 不修改`
      - pipeline.phase 已是 `"active"` → migrate 返回 no-op
    - [ ] `migrate 旧格式 done → pipeline.phase 变为 "done"`
      - 旧 phase 为 `"done"` → 全局 phase 变为 `"done"`
    - [ ] `migrate --dry-run → 输出预览，文件不变`
      - 对比 dry-run 前后的 flow.json 内容
    - [ ] `migrate --no-backup → 不生成 .bak`
      - 检查 `flow.json.v4.0.bak` 不存在
    - [ ] `migrate 旧格式中 current.stage="-" → 旧 phase 下沉到第一个 stage`
      - 边界情况测试
- [ ] 3. **扩展 `test/core/flow-manager.test.ts`**（约 200 行新增）：
  - **新增测试套件**：
    - [ ] `describe('advanceStagePhase')` — 测试新增的 `advanceStagePhase` 方法：
      - 正常推进 stage phase
      - 非法 stageName → 抛出错误
      - 非法 phase 值 → 模糊修正 / 拒绝
      - 同步更新 pipeline.current
      - 同步更新 stage.status
      - 追加日志条目
    - [ ] `describe('per-stage phase validation')` — 测试 `validate()` 对 stage phase 的校验：
      - stage phase 合法 → valid=true
      - stage phase 非法 → errors 含对应错误
      - pipeline.phase 合法 MetaPhase → valid=true
      - pipeline.phase 非法 MetaPhase → errors 含对应错误
    - [ ] `describe('multi-stage parallel scenarios')` — 多阶段并行场景：
      - stage-01.phase = "exec_running"，stage-02.phase = "plan_pending" → summary 正确展示
      - canAdvance 基于各自 stage phase 独立校验
      - recoverContext 返回当前活跃 stage 的 phase
    - [ ] `describe('deprecated advancePhase backward compat')` — 旧 API 兼容：
      - 调用旧 `advancePhase` → 输出 warn + 内部委托到 `advanceStagePhase`
- [ ] 4. **扩展 `test/commands/flow.ts`**（若存在 CLI 集成测试文件，约 100 行）：
  - `flow advance --stage` 必选校验
  - `flow status` 按阶段展示
  - `flow current` 含 stage phase
  - 若不存在该测试文件，则在 `flow-manager.test.ts` 中覆盖逻辑层等价测试
- [ ] 5. **更新现有测试中的 phase 断言**：
  - 搜索 `test/` 目录下所有引用 `pipeline.phase` 旧值的测试（如 `expect(...).toBe('plan_pending')`）
  - 将 pipeline.phase 断言改为 MetaPhase 值（`'active'`/`'paused'`/`'done'`）
  - 将引用 stage phase 的断言改为通过 `stages[stageId].phase` 访问
- [ ] 6. 运行 `npx vitest run` 确认全部测试通过（目标 225+/227，新增测试不引入回归）

## 产出文件

- `test/commands/flow-migrate.test.ts`（新增）
- `test/core/flow-manager.test.ts`（扩展）
- `test/commands/flow.ts`（若存在，扩展）

## 自测清单

- [ ] 迁移测试 7 个用例全部通过
- [ ] `advanceStagePhase` 测试覆盖正常/异常路径
- [ ] 多阶段并行场景测试通过（两个 stage 不同 phase 共存）
- [ ] 旧 `advancePhase` 兼容测试通过（输出 warn）
- [ ] 旧测试中的 phase 断言已更新为 MetaPhase/stage phase（无 `plan_pending` 字面错误断言）
- [ ] `npx vitest run` 全部通过，无新增失败
- [ ] 测试文件命名符合项目规范（`*.test.ts`）
- [ ] 每个测试用例的 `describe` 块含中文注释说明测试意图
