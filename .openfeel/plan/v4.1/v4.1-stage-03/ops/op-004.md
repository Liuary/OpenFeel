# op-004：feel.md 路由更新 — 遍历 stages 找活跃阶段替代读全局 phase
- **阶段**：v4.1-stage-03
- **前置**：op-003（CLI 适配完成，flow status 按阶段展示就绪）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

更新 Feel Agent 的 prompt（`.opencode/agents/feel.md`），使其感知 flow.json 多阶段架构：调度前遍历 `stages` 找活跃阶段（非读全局 `pipeline.phase`），调度决策基于阶段级 phase。

## KB 参考

- 参见 kb/architecture.md #Feel 调度 + CLI 推进模型 — Feel 通过 CLI 读取 flow.json 推进流水线，本次变更为其中"读取 flow.json"环节的路径变更
- 参见 kb/patterns.md #CLI 原子管理模式 — Feel 只读 flow.json 状态，不直接 edit

## 实施步骤

- [ ] 1. `read` 本方案文件完整内容（必做）
- [ ] 2. 打开 `.opencode/agents/feel.md`，定位到「核心职责」第 3 条"管理流水线"
- [ ] 3. 在「管理流水线」条下追加一段 **多阶段感知** 说明（约 8-12 行）：
  ```markdown
  3. **管理流水线**：通过 `openfeel flow` 命令查询和推进 flow.json 中的流水线状态。
     - flow.json 已改为**多阶段独立状态机**：全局 `pipeline.phase` 仅表示宏观状态
       （`active`/`paused`/`done`），每个阶段 `stages.{stageId}.phase` 记录自身的
       流水线阶段（如 `exec_running`/`review_pending`）。
     - **调度前必须遍历 `stages`**：读取 `flow status` 输出中的各阶段 phase，
       找到 `phase != 'done'` 的活跃阶段作为当前调度目标。
     - 多阶段并行（如 stage-03 编码时 stage-04 在计划）时，Feel 需按优先级
       或依赖关系选择当前推进的阶段，暂停其他阶段。
     - 具体的阶段推进通过 `openfeel flow advance --stage <id> --to <phase>` 命令执行。
  ```
- [ ] 4. 在「委托边界」节更新 Feel 决策上下文：将原"查看 flow.json phase 决定下一步"改为"查看活跃 stage phase 决定下一步"
- [ ] 5. 在「可调用的技能」表 `flow` 行中，更新用途描述为"查询/推进流水线状态（多阶段感知）"
- [ ] 6. （可选）在「注意事项」中追加一条："流水线全局 phase（active/paused/done）仅作为元信息，调度决策必须基于阶段 phase"
- [ ] 7. 确保 feel.md 总行数保持在 90-120 行目标范围内（当前 115 行，追加约 10-15 行 → 需压缩 3-5 行冗余内容）

## 产出文件

- `.opencode/agents/feel.md`

## 自测清单

- [ ] feel.md 中包含"多阶段独立状态机"相关说明
- [ ] feel.md 中包含"遍历 stages"或等效路由逻辑描述
- [ ] feel.md 中包含 `flow advance --stage` 的命令示例
- [ ] feel.md 行数在 90-130 行之间（追加后不应严重超标）
- [ ] feel.md frontmatter 未变更（description/mode/color/permission 字段保持）
