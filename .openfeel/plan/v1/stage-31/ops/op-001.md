# op-001：flow advance --stage 缺失提示增强（i18n 文案修改）
- **阶段**：v1.0.0-stage-31
- **前置**：无（独立操作）
- **负责 Agent**：Executor
- **最多重试**：3
- **问题编号**：#4（--stage 缺失提示不友好）
- **参考**：plan.md #问题 #4 详细技术方案
- **关联 REV**：REV-001（测试策略补充）

## 目标

修改 i18n 键 `flow.advance.errorNoStage`，在 `--stage` 缺失时引导用户先创建阶段。

## 知识库参考

- kb/patterns.md #CLI 国际化封装模式：所有 CLI 输出通过 `t()` + 键命名体系，中英双语同步

## 产出文件

- `src/core/i18n-data/zh-CN.ts`
- `src/core/i18n-data/en.ts`

## 实施步骤

### 步骤 1：修改 zh-CN.ts 中的 `advance.errorNoStage`

**当前**（`src/core/i18n-data/zh-CN.ts` L93）：
```typescript
'advance.errorNoStage':       { key: 'flow.advance.errorNoStage',       zh: '错误：--stage 参数必须指定阶段 ID（如 stage-03）', en: '' },
```

**修改为**：
```typescript
'advance.errorNoStage':       { key: 'flow.advance.errorNoStage',       zh: '错误：--stage 参数必须指定阶段 ID（如 stage-03）。如果是新项目，请先运行 openfeel stage create <id> 创建阶段。', en: '' },
```

- [ ] 使用 `edit` 工具精确替换 zh-CN.ts L93 的 `zh` 字段值

### 步骤 2：修改 en.ts 中的 `advance.errorNoStage`

**当前**（`src/core/i18n-data/en.ts` L87）：
```typescript
'advance.errorNoStage':       { key: 'flow.advance.errorNoStage',       zh: '', en: 'Error: --stage parameter must specify a stage ID (e.g. stage-03)' },
```

**修改为**：
```typescript
'advance.errorNoStage':       { key: 'flow.advance.errorNoStage',       zh: '', en: 'Error: --stage parameter must specify a stage ID (e.g. stage-03). If this is a new project, run openfeel stage create <id> first.' },
```

- [ ] 使用 `edit` 工具精确替换 en.ts L87 的 `en` 字段值

### 执行顺序

步骤 1 和步骤 2 无相互依赖，可任意顺序执行。

## 自测清单

- [ ] **TypeScript 编译**：`npm run build` 零错误
- [ ] **i18n 对称性**：`openfeel lint i18n` 通过
- [ ] **验证 — 中文提示（REV-001 测试策略补充）**：
  1. 在**无 flow.json 项目**中执行 `openfeel flow advance --to plan_pending`（不带 `--stage`）
  2. 确认 stderr 输出包含 "如果是新项目，请先运行 openfeel stage create <id> 创建阶段。"
  3. 确认退出码为 1
- [ ] **验证 — 英文提示**：
  1. 设置语言为 en（`openfeel config set lang en`）后执行相同命令
  2. 确认 stderr 输出包含 "If this is a new project, run openfeel stage create <id> first."
- [ ] **现有测试无回归**：`npm test` 全部通过（当前 399/399）

## REV-001 回应：测试策略补充

| 检测项 | 方法 | 覆盖 |
|--------|------|------|
| 中文引导文案完整性 | CLI 手动验证（stderr 字符串匹配） | 中文路径 |
| 英文引导文案完整性 | CLI 手动验证（stderr 字符串匹配） | 英文路径 |
| 退出码正确性 | CLI 手动验证（`echo $LASTEXITCODE` = 1） | 行为一致性 |
| i18n 键对称性 | `openfeel lint i18n` 自动化 | 422+ 键全量 |

## 风险说明

| 风险 | 概率 | 缓解 |
|------|------|------|
| 无 | — | 仅修改 i18n 文本，不涉及代码逻辑变更，零风险 |
