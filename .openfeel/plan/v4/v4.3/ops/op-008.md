# op-008：测试覆盖 — template-loader 单元测试 + init/update 集成测试 + 回退逻辑测试

- **阶段**：v4.3-stage-03
- **前置**：op-001~op-007（soft — 功能实现完成后测试，但测试文件可提前编写）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

新增以下测试覆盖，确保双语功能正确性并防止回归：

1. **template-loader 单元测试**：验证中英文模板加载函数返回正确内容
2. **init 集成测试**：验证交互选择后 `.info.json` 正确写入 lang 值
3. **update --lang 集成测试**：验证参数正确传递到模板加载器
4. **回退逻辑测试**：验证 `.info.json` 无 lang 字段时默认 zh-CN

## 测试框架

项目使用 **vitest**（`npm test` 执行）。测试文件放在 `test/core/` 和 `test/commands/` 中。

## 实施步骤

### Part A：template-loader 单元测试

- [ ] A1. 创建 `test/core/template-loader.test.ts`
- [ ] A2. 编写测试用例：

```typescript
import { describe, it, expect, beforeAll } from 'vitest';
// 注：import 路径需根据实际模块解析方式调整
```

| # | 测试用例 | 验证点 |
|:--:|---------|--------|
| A1 | `loadAgentTemplate('zh-CN', 'feel')` | 返回非空字符串，含中文内容 |
| A2 | `loadAgentTemplate('en', 'feel')` | 返回非空字符串，含英文内容，含 "Orchestrator" |
| A3 | `loadAgentTemplate('zh-CN', 'archiver')` | 返回中文 Archiver 模板 |
| A4 | `loadAgentTemplate('en', 'archiver')` | 返回英文 Archiver 模板 |
| A5 | `loadAgentTemplate('fr', 'feel')` | 回退到 zh-CN，返回中文内容 |
| A6 | `loadAgentTemplate('en', 'nonexistent')` | 抛出 Error，信息含 `actual lang=en` |
| A7 | `listAgentIds('zh-CN')` | 返回 8 个 Agent ID 数组 |
| A8 | `listAgentIds('en')` | 返回 8 个 Agent ID 数组（与 zh-CN 相同） |
| A9 | `loadTemplate('zh-CN', 'agents-md')` | 返回 UTF-8 明文中文 AGENTS.md 模板 |
| A10 | `loadTemplate('en', 'agents-md')` | 返回 UTF-8 明文英文 AGENTS.md 模板 |
| A11 | `loadTemplate('zh-CN', 'core-instructions')` | 返回 UTF-8 明文（非 Base64），含中文 |
| A12 | `loadTemplate('en', 'core-instructions')` | 返回 UTF-8 明文英文 core instructions |
| A13 | `loadTemplate('fr', 'core-instructions')` | 回退到 zh-CN |

### Part B：init 集成测试

- [ ] B1. 扩展现有 `test/core/init.test.ts` 或在 `test/commands/init.test.ts` 中添加

| # | 测试用例 | 验证点 |
|:--:|---------|--------|
| B1 | `initProject` 在 .info.json 不存在时写入 `lang: 'zh-CN'` | `ensureInfoJson` 产物含 `lang` 字段 |
| B2 | `initProject` 在 .info.json 已存在但无 lang 时补充写入 `zh-CN` | 回退逻辑触发 |
| B3 | `initProject` 在 .info.json 已有 `lang: 'en'` 时保留不变 | 向后兼容 |

- [ ] B2. 注意：交互式选择（readline）的测试较复杂，需模拟 `process.stdout.isTTY`。简化方案：仅测试非交互默认路径和 `writeLang` 内部函数。

### Part C：update --lang 集成测试

- [ ] C1. 扩展 `test/core/update.test.ts` 或在 `test/commands/` 下新增

| # | 测试用例 | 验证点 |
|:--:|---------|--------|
| C1 | `updateProject` 传入 `lang='en'` → 部署英文 Agent 文件 | 产出文件内容为英文 |
| C2 | `updateProject` 传入 `lang='zh-CN'` → 部署中文 Agent 文件 | 产出文件内容为中文 |
| C3 | `updateProject` 传入非法 lang → 回退到 zh-CN 或报错 | 健壮性 |

### Part D：回退逻辑测试

- [ ] D1. 编写 `test/core/workspace/identity.test.ts`（如已存在则扩展）

| # | 测试用例 | 验证点 |
|:--:|---------|--------|
| D1 | `getLang` 对无 `lang` 字段的 .info.json 返回 `'zh-CN'` | 默认值 |
| D2 | `getLang` 对 `lang='en'` 返回 `'en'` | 正常读取 |
| D3 | `getLang` 对 `lang='fr'`（非法值）返回 `'zh-CN'` | 非法值回退 |
| D4 | `ensureInfoJson` 不覆盖已有 `user` 字段 | 数据完整性 |

## 产出文件

| 类型 | 文件 |
|------|------|
| 新增 | `test/core/template-loader.test.ts` |
| 修改/扩展 | `test/core/init.test.ts`（或 `test/commands/init.test.ts`） |
| 修改/扩展 | `test/core/update.test.ts` |
| 新增/扩展 | `test/core/workspace/identity.test.ts` |

## 自测清单

- [ ] `npm test` 全量测试通过，含新增测试用例
- [ ] 新增 template-loader 测试 13 条全部通过
- [ ] 新增 init 测试 3 条全部通过
- [ ] 新增 update 测试 3 条全部通过
- [ ] 新增回退逻辑测试 4 条全部通过
- [ ] 无既有测试回归（对比 op 执行前后 `npm test` 结果）
- [ ] 测试覆盖计划 `roadmap/v4.3.md` 的质量指标（阶段汇总时验证）
