# op-004：更新 AGENTS.md — Agent 总览表（8→9）

- **阶段**：v4.6-stage-01
- **前置**：op-001, op-002, op-003（确定 vision 的最终名称和角色定义后执行）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

将 AGENTS.md 中的 Agent 体系总览从 8 个更新为 9 个，新增 Vision 视觉官条目。

## 实施步骤

> **重要**：修改前须先 `read AGENTS.md` 确认当前文件内容与预期一致。使用 `edit` 工具精确替换。

### 步骤 1：更新 Feel 调度约束中的 Agent 列表（行 83）

在 Feel 调度约束的 Agent 列表中插入 Vision，位于事务官之后、Archiver 之前。

**旧文本**（精确匹配）：
```
Feel 总统领统一调度下游 Agent（Planner / Schemer / Executor / Reviewer / Feel Tester / 事务官 / Archiver），通过 `task` 工具按流水线阶段（计划→方案→执行→审查→测试→归档）串行推进。各 Agent 仅在自己的职责边界内操作，不得越界启动其他 Agent 或自行修改 flow.json 状态。
```

**新文本**：
```
Feel 总统领统一调度下游 Agent（Planner / Schemer / Executor / Reviewer / Feel Tester / 事务官 / Vision / Archiver），通过 `task` 工具按流水线阶段（计划→方案→执行→审查→测试→归档）串行推进。各 Agent 仅在自己的职责边界内操作，不得越界启动其他 Agent 或自行修改 flow.json 状态。
```

### 步骤 2：更新标题（行 87）

**旧文本**：
```
### 8 Agent 体系总览
```

**新文本**：
```
### 9 Agent 体系总览
```

### 步骤 3：更新表格（行 89-98）— 插入 Vision 行

在「事务官」行之后、「Archiver」行之前插入 Vision 行。

**旧文本**（精确匹配）：
```
| 事务官 | 事务官 | 快速模型 (Flash) | subagent |
| Archiver | 归档官 | 推理模型 | subagent |
```

**新文本**：
```
| 事务官 | 事务官 | 快速模型 (Flash) | subagent |
| Vision | 视觉官 | 多模态模型 (qwen-vl-plus) | subagent |
| Archiver | 归档官 | 推理模型 | subagent |
```

### 步骤 4：最终验证

重新 `read AGENTS.md`，确认：
- 标题为 `### 9 Agent 体系总览`
- 表格共 10 行（1 表头 + 9 数据行），Vision 行位于事务官之后、Archiver 之前
- Feel 调度约束列表包含 Vision

## 产出文件

- `AGENTS.md`（修改）

## 自测清单

- [ ] 标题从 `### 8 Agent 体系总览` 改为 `### 9 Agent 体系总览`
- [ ] Feel 调度约束行（行 84）包含 `Vision`（位于 `事务官 / Vision / Archiver` 序列中）
- [ ] 表格中新增 Vision 行：`| Vision | 视觉官 | 多模态模型 (qwen-vl-plus) | subagent |`
- [ ] Vision 行在事务官之后、Archiver 之前
- [ ] 表格共 9 个 Agent 数据行（header + 9 rows = 10 行）
- [ ] 表格中 Feel 行仍为 `primary` 调起方式
- [ ] 文件编码和行尾未被改变
