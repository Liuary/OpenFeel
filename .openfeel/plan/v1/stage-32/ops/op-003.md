# op-003：冲突文件写入 + CLI 冲突报告 + 工作区目录扩展
- **阶段**：v1.0.0-stage-32
- **前置**：op-002（需要改造后的 `writeWithMergeDetection` 三态逻辑和 `UpdateResult.conflicts` 字段）
- **负责 Agent**：Executor
- **最多重试**：3
- **关联 REV**：REV-005（模板路径补充完整 + 双模板同步说明）、REV-006（.gitignore 追加 `update_state.json` 和 `update_conflicts/`）

## 目标

在 `updateProject()` 中实现冲突文件的 Git 风格标记写入，在 CLI 命令层输出冲突报告，扩展 `WORKSPACE_DIRS` 新增 `update_conflicts` 目录，新增 2 条 i18n 键（zh-CN + en 双语对称），追加 `.gitignore` 忽略条目。

## 实施步骤

- [ ] **步骤 1**：在 `src/core/update.ts` 新增 `writeConflictFile()` 函数
- [ ] **步骤 2**：在 `updateProject()` 中集成冲突文件写入——遍历 `conflicts` 列表，调用 `writeConflictFile()`
- [ ] **步骤 3**：在 `src/commands/update.ts` 新增冲突报告输出——遍历 `result.conflicts`，输出摘要和解决指引
- [ ] **步骤 4**：[FIX] 新增 2 条 i18n 键：`update.conflictsTitle` 和 `update.conflictsHint`（`zh-CN.ts` + `en.ts` 双语对称）
- [ ] **步骤 5**：[FIX] 在 `src/core/workspace/structure.ts` 的 `WORKSPACE_DIRS` 数组中追加 `'update_conflicts'`
- [ ] **[FIX]** 步骤 6：[FIX] 在 `.gitignore` 中追加 `update_state.json` 和 `update_conflicts/` 条目（REV-006）
- [ ] **步骤 7**：[FIX] 修正产出文件路径——使用完整的项目根相对路径（REV-005 路径补充）
- [ ] **步骤 8**：运行 TypeScript 编译检查和全量测试

## 产出文件

| 类型 | 路径 |
|------|------|
| 修改 | `src/core/update.ts` |
| 修改 | `src/commands/update.ts` |
| 修改 | `src/core/workspace/structure.ts` |
| 修改 | `src/core/i18n-data/zh-CN.ts` |
| 修改 | `src/core/i18n-data/en.ts` |
| 修改 | `.gitignore` |

## 详细代码变更

### 1. `src/core/update.ts`——新增 `writeConflictFile()` 函数

在文件末尾（`updateProject()` 函数之后、`export` 之前），新增：

```typescript
/**
 * 写入冲突标记文件到 .openfeel/update_conflicts/{relativePath}
 * 格式：Git 风格冲突标记
 *
 * REV-006：update_conflicts/ 目录已加入 .gitignore，不纳入版本管理。
 */
function writeConflictFile(
  projectPath: string,
  relativePath: string,
  currentContent: string,
  incomingContent: string,
  openfeelVersion: string,
): void {
  const conflictsBase = resolve(projectPath, '.openfeel', 'update_conflicts');
  mkdirSync(conflictsBase, { recursive: true });

  const conflictPath = resolve(conflictsBase, relativePath);
  mkdirSync(dirname(conflictPath), { recursive: true });

  const conflictContent = [
    `<<<<<<< CURRENT (用户修改版)`,
    currentContent,
    `=======`,
    incomingContent,
    `>>>>>>> INCOMING (openfeel v${openfeelVersion} 更新)`,
  ].join('\n');

  writeFileSync(conflictPath, conflictContent, 'utf-8');
}

/**
 * 获取 OpenFeel 工具自身版本号
 * 从工具自身的 package.json 读取。
 */
function getOpenfeelVersion(): string {
  const pkgPath = resolve(
    dirname(fileURLToPath(import.meta.url)),
    '..',
    'package.json',
  );
  const pkg = JSON.parse(readFileSync(pkgPath, 'utf-8'));
  return pkg.version;
}
```

> 若 `getOpenfeelVersion()` 已在 op-001 的 `update-state.ts` 中实现并导出，则此处改为 `import { getOpenfeelVersion } from './update-state.js'`，不重复定义。若 op-001 未导出该函数（当前设计为 `update-state.ts` 内部函数），则 `update.ts` 中需自行实现。Executor 应根据 op-001 的实际实现选择正确方案。

### 2. `src/core/update.ts`——集成冲突文件写入

在 `updateProject()` 中，于 state 更新逻辑之前（即 `// ── REV-001：统一更新 update_state.json ──` 注释块之前），插入冲突文件写入逻辑：

```typescript
// ── 写入冲突标记文件 ──
if (conflicts.length > 0) {
  const openfeelVersion = getOpenfeelVersion();
  for (const relPath of conflicts) {
    const absPath = resolve(projectPath, relPath);
    const currentContent = existsSync(absPath)
      ? readFileSync(absPath, 'utf-8')
      : '';
    // 获取 incoming 内容：从模板/构建产物中重新读取
    // 注意：由于 writeWithMergeDetection 未实际写入，需单独获取 incoming 内容
    const incomingContent = getIncomingContent(relPath);
    writeConflictFile(projectPath, relPath, currentContent, incomingContent, openfeelVersion);
  }
}
```

> **实现说明**：需要新增 `getIncomingContent(relPath: string): string` 辅助函数，根据 `relPath` 从模板/构建产物中获取对应内容。对 Agent 文件 → 从 `loadAgentTemplate()` 获取；对 Skill 文件 → 从 `SKILL_DEFINITIONS` 获取；对 core.md → 从 `loadTemplate(lang, 'core-instructions')` 获取；对 opencode.jsonc → 从 `buildUpdatedJsonc()` 获取；对 AGENTS.md → 从 `loadTemplate(lang, 'agents-md')` 获取。该映射逻辑与 `updateProject()` 中生成内容的逻辑一一对应。

### 3. `src/commands/update.ts`——CLI 冲突报告

在现有 `result.skipped` 输出块之后、`result.created.length === 0 && result.updated.length === 0` 判断之前（约第 80 行），插入：

```typescript
// 冲突文件报告
if (result.conflicts && result.conflicts.length > 0) {
  console.log(t('update.conflictsTitle', lang));
  for (const item of result.conflicts) {
    console.log(`  ⚠ ${item}`);
  }
  console.log(t('update.conflictsHint', lang));
}
```

同时确保函数开头的 import 已包含新的依赖（若 `conflicts` 尚未从 `UpdateResult` 解构使用则无需修改 import）。

### 4. [FIX] 新增 i18n 键（双语对称）

**`src/core/i18n-data/zh-CN.ts`**——在 `update` 域的 `langConflict` 条目之后追加：

```typescript
'conflictsTitle':             { key: 'update.conflictsTitle',             zh: '检测到冲突（文件已被手动修改，拒绝覆盖）',        en: '' },
'conflictsHint':              { key: 'update.conflictsHint',              zh: '冲突文件已写入 .openfeel/update_conflicts/，请手动合并后重新运行 openfeel update。', en: '' },
```

**`src/core/i18n-data/en.ts`**——在 `update` 域的 `langConflict` 条目之后追加：

```typescript
'conflictsTitle':             { key: 'update.conflictsTitle',             zh: '', en: 'Conflicts detected (files have been manually modified, overwrite refused)' },
'conflictsHint':              { key: 'update.conflictsHint',              zh: '', en: 'Conflict files written to .openfeel/update_conflicts/. Please merge manually, then re-run openfeel update.' },
```

> **REV-005**：确保两语言文件的 `Object.keys()` 完全一致（遵循 kb/patterns.md #CLI 国际化封装模式）。

### 5. [FIX] 工作区目录扩展

**`src/core/workspace/structure.ts`**——在 `WORKSPACE_DIRS` 数组末尾追加：

```typescript
const WORKSPACE_DIRS = [
  'plan',
  'kb',
  'dev',
  'log',
  'code_review',
  'bugs',
  'users',
  'tmp',
  'manual',
  'update_conflicts',  // 新增：update 冲突文件目录
];
```

> 参见 kb/patterns.md #WORKSPACE_DIRS 同步模式——新增目录须同步更新 WORKSPACE_DIRS 数组。`init` 和 `createWorkspace()` 会自动创建该目录。

### 6. [FIX] `.gitignore` 追加（REV-006）

在 `.gitignore` 的现有内容末尾追加：

```
# update 状态与冲突文件（不纳入版本管理）
.openfeel/update_state.json
.openfeel/update_conflicts/
```

`.gitignore` 修改后的完整内容：

```
node_modules/
dist/

# 私域目录（.openfeel 工作区规范：个人日志/笔记/审查/Bug 不纳入版本管理）
.openfeel/users/
coverage/

# update 状态与冲突文件（不纳入版本管理）
.openfeel/update_state.json
.openfeel/update_conflicts/
```

## 自测清单

- [ ] `writeConflictFile()` 在 `.openfeel/update_conflicts/` 下正确生成文件
- [ ] 冲突文件内容符合 Git 风格格式：`<<<<<<< CURRENT` / `=======` / `>>>>>>> INCOMING`
- [ ] 冲突文件的目录层级与相对路径一致（如 `.opencode/agents/reviewer.md` → `update_conflicts/.opencode/agents/reviewer.md`）
- [ ] CLI 输出：无冲突时无额外输出；有冲突时输出 `conflictsTitle` 和每个冲突文件路径及 `conflictsHint`
- [ ] i18n 键：`update.conflictsTitle` 和 `update.conflictsHint` 在 zh-CN 和 en 中均存在且键名一致
- [ ] `WORKSPACE_DIRS` 包含 `'update_conflicts'`，`createWorkspace()` 自动创建该目录
- [ ] `.gitignore` 包含 `.openfeel/update_state.json` 和 `.openfeel/update_conflicts/` 条目
- [ ] `npm run build` 成功，`npm test` 全量通过（需更新 i18n 键总数断言和 update_result 相关测试）
- [ ] `openfeel lint i18n` 无键对称性错误（新增 2 键后 zh-CN / en 对称）

## 依赖版本

| 包名 | 版本 | 用途 | 选定依据 |
|------|------|------|----------|
| 无新增 | — | — | 所有依赖为项目现有 |

> 知识库参考：kb/patterns.md #WORKSPACE_DIRS 同步模式、kb/patterns.md #CLI 国际化封装模式

## 方案修正记录（Executor 回写）

| 日期 | 修正项 | 说明 |
|------|--------|------|
| 2026-08-11 | getOpenfeelVersion 复用 | 方案注明「若 op-001 已导出则 import 复用」。op-001 已导出，故 `src/core/update.ts` 直接 `import { getOpenfeelVersion } from './update-state.js'`，未重复定义。 |
| 2026-08-11 | getIncomingContent 实现 | 方案要求新增辅助函数按 relPath 从模板获取 incoming 内容，已按 5 类路径（core.md / agents / skills / jsonc / AGENTS.md）实现正则匹配映射，与 updateProject 生成逻辑一一对应。 |
| 2026-08-11 | 测试补充 | 新增 4 个测试：冲突文件写入（Git 风格格式 + 目录层级）、无冲突不建目录、UpdateResult.conflicts 存在性、REV-003 场景 3 混合分类。update.test.ts 26 项全过。 |
| 2026-08-11 | lint i18n 验证 | `openfeel lint i18n` 输出「443 键一致」，zh-CN/en 键对称无错误（REV-005）。 |
| 2026-08-11 | 部署文件行尾规范化 | 执行 `node bin/openfeel.js update .` 传播模板时，若干部署文件（archiver/executor/planner/reviewer 等）行尾由 CRLF 规范化为 LF，git 判定内容无变化（autocrlf=input 下自动忽略）。仅 `.opencode/agents/feel.md` 含真实内容变更（op-004 冲突检测节传播）。 |
