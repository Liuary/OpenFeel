# op-004：Feel Agent 启动时冲突检测集成
- **阶段**：v1.0.0-stage-32
- **前置**：op-001（soft 依赖——仅需 `update_state.json` 存在并被正确维护；不依赖 op-002/op-003 的改造产物）
- **负责 Agent**：Executor
- **最多重试**：3
- **关联 REV**：REV-004（非 TTY 行为定义——静默跳过冲突检测）、REV-005（模板路径补充完整 + 双模板同步说明）

## 目标

在 Feel Agent 的 prompt 模板（zh-CN + en 双语）中，于「记忆加载」节和「决策追加」节之间，新增「冲突检测」节。Feel 启动时读取 `.openfeel/update_state.json`，检测到 `status=conflict` 的条目后，在 TTY 环境输出提示引导用户处理，非 TTY 环境静默跳过。

## 实施步骤

- [ ] **步骤 1**：[FIX] 修改 `src/core/templates-data/agents/zh-CN/feel.md`——在「记忆加载」和「决策追加」之间插入「冲突检测」节
- [ ] **步骤 2**：[FIX] 修改 `src/core/templates-data/agents/en/feel.md`——在「Memory Loading」和「Decision Appending」之间插入「Conflict Detection」节（双语对称同步）
- [ ] **步骤 3**：运行 `npm run build`，验证模板变更自动注入 `template-loader.ts` 并传播到 `.opencode/agents/feel.md`
- [ ] **步骤 4**：[FIX] 验证双模板同步——zh-CN 和 en 的结构完全对称（节标题、步骤编号、输出格式均一致，仅语言不同）（REV-005）
- [ ] **步骤 5**：运行 `npm test` 确认无回归

## 产出文件

| 类型 | 完整路径（REV-005 修正） | 说明 |
|------|--------------------------|------|
| 修改 | `src/core/templates-data/agents/zh-CN/feel.md` | 中文源模板 |
| 修改 | `src/core/templates-data/agents/en/feel.md` | 英文源模板 |
| 自动传播 | `.opencode/agents/feel.md` | 部署文件（由 `npm run build` → `openfeel update` 自动同步） |
| 自动生成 | `src/core/template-loader.ts` | 构建产物（由 `npm run build` 自动内联更新） |

> **REV-005 双模板同步说明**：zh-CN 和 en 内容语言各自独立，但结构（节标题、步骤编号、输出格式）必须完全对称。本节标题：zh-CN 为 `## 冲突检测`，en 为 `## Conflict Detection`。

## 详细代码变更

### 1. zh-CN 模板插入（`src/core/templates-data/agents/zh-CN/feel.md`）

**插入位置**：在 `## 记忆加载` 节结束后（约第 310 行 `4.` 条目结束）、`## 决策追加` 节开始前（约第 312 行）之间，插入新节。

**插入内容**：

```markdown
## 冲突检测

Feel 启动时检测 `.openfeel/update_state.json`（若文件存在）：

1. 读取 `update_state.json`，遍历 `files` 字段，查找 `status=conflict` 的条目
2. 若存在冲突：
   - 若终端为 TTY 交互环境，输出冲突列表和解决指引：
     ```
     ⚠️ 检测到 openfeel update 冲突：
       {file1}
       {file2}
     （共 {N} 个冲突文件）
     冲突文件已保存在 .openfeel/update_conflicts/ 目录。
     请手动合并冲突后运行 openfeel update 更新状态。
     ```
   - 若终端非 TTY 环境（如 CI/CD），**静默跳过**，不输出任何冲突提示
     （非交互环境下无法处理冲突，输出提示只会污染日志）
3. 不阻塞 Feel 主体流程——冲突提示后照常进入主流程
4. 若 `update_state.json` 不存在：**静默跳过**（项目尚未执行过 `openfeel update`）
```

> **REV-004**：非 TTY 环境（`process.stdout.isTTY === false`）下静默跳过冲突检测，不输出任何信息——避免 CI/CD 日志污染。Feel 的 prompt 是声明性指令，实际 `isTTY` 判断在 Feel Agent 运行时由平台执行。

### 2. en 模板插入（`src/core/templates-data/agents/en/feel.md`）

**插入位置**：在 `## Memory Loading` 节结束后（约第 309 行 `4.` 条目结束）、`## Decision Appending` 节开始前（约第 311 行）之间。

**插入内容**：

```markdown
## Conflict Detection

At startup, Feel checks `.openfeel/update_state.json` (if the file exists):

1. Read `update_state.json`, iterate over the `files` field, and find entries with `status=conflict`
2. If conflicts exist:
   - If the terminal is an interactive TTY environment, output the conflict list and resolution guidance:
     ```
     ⚠️ openfeel update conflicts detected:
       {file1}
       {file2}
     ({N} conflict file(s) total)
     Conflict files saved in .openfeel/update_conflicts/ directory.
     Please merge manually, then run openfeel update to update state.
     ```
   - If the terminal is NOT a TTY environment (e.g., CI/CD), **silently skip** without any output
     (conflicts cannot be resolved in non-interactive environments; output would only pollute logs)
3. Do not block Feel's main flow — silently proceed after the conflict prompt
4. If `update_state.json` does not exist: **silently skip** (the project has not run `openfeel update`)
```

### 3. 构建与传播

执行：

```bash
npm run build
```

验证：
- `template-loader.ts` 中的 `feel` 模板内容已包含新增的「冲突检测」/「Conflict Detection」节
- `.opencode/agents/feel.md` 的 Base64 校验通过（与 zh-CN 模板内容一致）

### 4. REV-005 路径确认

| 错误路径（原 plan 中省略前缀） | 正确路径（REV-005 修正后） |
|------|------|
| `templates-data/agents/zh-CN/feel.md` | `src/core/templates-data/agents/zh-CN/feel.md` |
| `templates-data/agents/en/feel.md` | `src/core/templates-data/agents/en/feel.md` |
| `.opencode/agents/feel.md` | `.opencode/agents/feel.md`（项目根相对，路径已正确） |

## 自测清单

- [ ] zh-CN feel.md 在「记忆加载」和「决策追加」之间包含「冲突检测」节
- [ ] en feel.md 在「Memory Loading」和「Decision Appending」之间包含「Conflict Detection」节
- [ ] 两语言版本的结构完全对称：节标题、步骤编号（1-4）、输出格式一致
- [ ] 内容中包含 `update_state.json`、`update_conflicts/`、`status=conflict` 关键词
- [ ] 内容中明确声明非 TTY 环境静默跳过（REV-004）
- [ ] `npm run build` 成功，`template-loader.ts` 已更新
- [ ] `.opencode/agents/feel.md` 的 zh-CN 源内容与新模板一致
- [ ] `npm test` 全量通过
- [ ] Feel Agent 实际启动时（需在真实 TTY 环境验证）：
  - 无 `update_state.json` → 静默跳过
  - 有 `update_state.json` 但无 conflict → 静默跳过
  - 有 conflict 且 TTY → 输出冲突提示
  - 有 conflict 且非 TTY → 静默跳过（REV-004）
- [ ] 冲突提示后 Feel 正常进入主流程（不阻塞）

> **注**：「自测清单」最后一条为集成测试场景，Executor 可结合实际环境判断是否可执行。若无法在真实 Feel Agent 环境中测试，至少确认模板内容正确且 `npm test` 通过。

## 依赖版本

| 包名 | 版本 | 用途 | 选定依据 |
|------|------|------|----------|
| 无新增 | — | — | 纯模板变更，无代码依赖 |

> 知识库参考：kb/patterns.md #init/update 重启提醒对称输出模式（isTTY 检查模式）、kb/patterns.md #新增 Agent 全链路更新清单模式（构建验证流程）、kb/architecture.md #多语言模板数据管线

## 方案修正记录（Executor 回写）

| 日期 | 修正项 | 说明 |
|------|--------|------|
| 2026-08-11 | 传播方式修正 | 全局 npm 安装的 `openfeel` 命令为旧版本（不含本 stage 逻辑）。改用项目入口 `node bin/openfeel.js update .` 完成模板传播，`.opencode/agents/feel.md` 成功写入「冲突检测」节（行 312-330），`template-loader.ts` 已注入两语言节（行 767/2055）。 |
| 2026-08-11 | 传播副作用清理 | 全局旧版本首次 update 曾将 `AGENTS.md` / `opencode.jsonc` / `.opencode/instructions/core.md` 覆盖为旧模板内容（非本 stage 产出），已 `git checkout` 还原。更新后生成 `.openfeel/update_state.json`（gitignore 已忽略，不纳入版本管理）。 |
| 2026-08-11 | 双模板对称验证 | zh-CN（行 312-330）与 en（行 311-329）结构完全对称：节标题、步骤编号 1-4、TTY/非 TTY 分支、输出格式、占位符 {file1}/{file2}/{N} 均一致，仅语言不同（REV-005）。 |
| 2026-08-11 | 集成测试说明 | 自测清单末条（Feel Agent 实际启动 TTY 行为）依赖真实终端环境，本环境为非 TTY 无法完整执行；已通过模板内容正确性 + `npm test` 全量通过替代验证（方案已注明此降级路径）。 |
