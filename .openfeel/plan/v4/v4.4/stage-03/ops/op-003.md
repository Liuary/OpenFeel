# op-003：强化 Agent 的 package.json 模板要求

- **阶段**：v4.4-stage-03
- **前置**：无（阶段内独立，与 stage-02 共享 executor.md 模板文件——soft 依赖，需确保 stage-02 的 git commit 要求已合并后再修改同一文件）
- **负责 Agent**：Executor
- **最多重试**：3
- **参考**：kb/patterns.md #构建脚本多语言循环生成模式（中英文模板对称原则）、kb/architecture.md #多语言模板数据管线（templates-data 源文件 → build.js 构建时内联 → template-loader 运行时按语言加载）

## 目标

在 Executor Agent 提示词模板（中英文版本）中明确 `package.json` 最小模板要求，确保 Agent 在新建项目时生成的 `package.json` 包含完整的必填字段：`name`、`version: "1.0.0"`、`type: "module"`、`scripts.test`。

## 技术决策

> **模板修改时机**：`templates-data/agents/{lang}/executor.md` 是源模板文件，修改后需执行 `npm run build` 重新内联到 `template-loader.ts`。部署到目标项目时由 `openfeel update` 调用 `loadAgentTemplate()` 加载。
>
> **中英文对称**：遵循 kb/patterns.md #构建脚本多语言循环生成模式，两语言文件的 package.json 要求必须一致（仅语言不同），确保中英 Agent 产出同质化。

## 实施步骤

### A. 中文 executor.md 新增 package.json 模板要求

- [ ] A1. 打开 `src/core/templates-data/agents/zh-CN/executor.md`
- [ ] A2. 定位段落：在「## 注意事项」节之前（约第158行），新增一个独立章节 `## package.json 模板要求`，内容如下：

  ```markdown
  ## package.json 模板要求

  当任务涉及创建新项目或初始化 `package.json` 时，必须确保生成的文件包含以下最小模板字段：

  ```json
  {
    "name": "项目名称",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
      "test": "vitest run"
    }
  }
  ```

  **必填字段说明**：

  | 字段 | 要求 | 说明 |
  |------|------|------|
  | `name` | 必填 | 项目名称，使用小写字母和连字符 |
  | `version` | 必填 | 固定初始值 `"1.0.0"` |
  | `type` | 必填 | 固定为 `"module"`，使用 ES Module 导入模式 |
  | `scripts.test` | 必填 | 至少包含一个测试命令（如 `"vitest run"`） |

  > 这些字段是 OpenFeel 标准化项目的最小要求。Agent 可根据项目实际需求在此基础上扩展字段，但不得遗漏任何必填项。
  ```

- [ ] A3. 确认新增章节不破坏后续「## 注意事项」的 Markdown 层级结构（`##` 级标题正确闭合）。

### B. 英文 executor.md 同步新增 package.json 模板要求

- [ ] B1. 打开 `src/core/templates-data/agents/en/executor.md`
- [ ] B2. 在与中文版相同位置（「## Notes」节之前，约第158行），新增 `## package.json Template Requirements` 章节，内容如下：

  ```markdown
  ## package.json Template Requirements

  When the task involves creating a new project or initializing `package.json`, ensure the generated file includes the following minimum template fields:

  ```json
  {
    "name": "project-name",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
      "test": "vitest run"
    }
  }
  ```

  **Required fields**:

  | Field | Requirement | Description |
  |-------|-------------|-------------|
  | `name` | Required | Project name, using lowercase letters and hyphens |
  | `version` | Required | Fixed initial value `"1.0.0"` |
  | `type` | Required | Fixed as `"module"`, using ES Module import mode |
  | `scripts.test` | Required | At least one test command (e.g., `"vitest run"`) |

  > These fields are the minimum requirements for an OpenFeel standardized project. Agents may extend with additional fields as needed, but must not omit any required fields.
  ```

- [ ] B3. 确认新增章节不破坏后续「## Notes」的 Markdown 层级结构。

### C. 构建验证

- [ ] C1. 执行 `npm run build`，确认模板内容正确内联到 `template-loader.ts`
- [ ] C2. 验证：在构建产物中搜索 `package.json Template Requirements`（英文）+ `package.json 模板要求`（中文），确认两语言均包含新章节

## 产出文件

| 类型 | 文件 | 说明 |
|------|------|------|
| **修改** | `src/core/templates-data/agents/zh-CN/executor.md` | 新增 package.json 模板要求章节（中文） |
| **修改** | `src/core/templates-data/agents/en/executor.md` | 新增 package.json Template Requirements 章节（英文） |

## 自测清单

- [ ] 中文 executor.md 的「## package.json 模板要求」章节包含 name / version / type / scripts.test 四个必填字段
- [ ] 英文 executor.md 的「## package.json Template Requirements」章节与中文版字段一一对应
- [ ] `npm run build` 编译成功，无错误
- [ ] 构建产物中搜索 `package.json 模板要求` 能找到中文版完整章节内容
- [ ] 构建产物中搜索 `package.json Template Requirements` 能找到英文版完整章节内容
- [ ] `npm test` 全量通过（模板变更不破坏现有测试）
- [ ] （可选）使用 `openfeel update` 部署到测试项目后，executor.md 包含完整 package.json 模板章节
