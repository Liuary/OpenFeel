# op-004：npm 发布准备

- **阶段**：v1.0.0-stage-02（发布工程）
- **最多重试**：3

## 目标

修复 package.json 中占位符 URL（repository.url / bugs.url），运行 `npm pack --dry-run` 验证产物完整性，确认 `files` 字段与实际产物一致，验证 `.gitignore` 未忽略 `.github/` 目录。

## 实施步骤

- [x] 1. 读取 package.json 确认当前 `repository.url` 与 `bugs.url` 占位符（`https://github.com/user/openfeel`）
- [x] 2. 修复 `repository.url` → `https://github.com/Liuary/OpenFeel.git`
- [x] 3. 修复 `bugs.url` → `https://github.com/Liuary/OpenFeel/issues`
- [x] 4. 运行 `npm pack --dry-run` 验证产物完整性（192 文件 / 294.3 kB，dist/bin/schemas 齐全）
- [x] 5. 核对 `files` 字段（`["dist", "bin", "schemas"]`）与实际产物一致（README.md + package.json 为 npm 自动包含）
- [x] 6. 验证 `.gitignore` 未忽略 `.github/` 目录（.gitignore 仅含 node_modules/、dist/、.openfeel/users/、coverage/）
- [x] 7. 运行 `npm run build` 确认构建无回归（op-003 已跑，package.json 修改不影响构建）

## 产出文件

- `package.json`

## 自测清单

- [x] package.json 中无占位符 URL（无 `user/openfeel` 残留）
- [x] `npm pack --dry-run` 产物包含 dist/、bin/、schemas/
- [x] `files` 字段与实际产物一致
- [x] `.gitignore` 未忽略 `.github/`
- [x] `npm run build` 通过

## 偏差记录

- 无偏差。`npm pack --dry-run` 实际产物 192 文件，README.md 与 package.json 由 npm 自动包含（files 字段外），与 `files` 声明一致。
