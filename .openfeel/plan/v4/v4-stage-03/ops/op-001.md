# op-001：内部模式一致性审查子维度 + 更新审查模板

- **阶段**：v4-stage-03
- **前置**：无（独立 op）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标
在 Reviewer Agent（`reviewer.md`）中细化「一致性」维度为「外部一致性 + 内部模式一致性」双子维度，并同步更新审查流程和模板说明，落实 Roadmap stage-03 验证项："审查报告包含「内部模式一致性」子维度条目"。

## 实施步骤
- [ ] 1. **读取本方案文件**：执行前先完整阅读 `op-001.md`，确认所有步骤和自测清单理解无误
- [ ] 2. 读取当前 `.opencode/agents/reviewer.md`（45 行），确认现有结构（审查维度表为 5 行单层表格）
- [ ] 3. 将「## 审查维度」章节的表格从 5 行单层扩展为含子维度的结构：

  将原有表格：
  ```
  | 维度 | 检查内容 |
  |------|----------|
  | 正确性 | 实现是否符合方案目标 |
  | 规范性 | 是否符合项目编码规范 |
  | 安全性 | 是否存在安全隐患 |
  | 完整性 | 是否覆盖所有方案步骤 |
  | 一致性 | 是否与既有架构一致 |
  ```

  替换为含子维度的表格：
  ```
  | 维度 | 子维度 | 检查内容 |
  |------|--------|----------|
  | 正确性 | — | 实现是否符合方案目标，功能逻辑是否正确 |
  | 规范性 | — | 是否符合项目编码规范（AGENTS.md） |
  | 安全性 | — | 是否存在安全隐患（注入、越权、泄露等） |
  | 完整性 | — | 是否覆盖所有方案步骤，产出文件是否齐全 |
  | 一致性 | 外部一致性 | 是否与既有整体架构和技术选型兼容 |
  | | 内部模式一致性 | 同类模块/函数是否使用一致校验风格、命名规范、错误处理模式 |
  ```

- [ ] 4. 在「## 审查维度」表格之后、「## 审查流程」之前，插入新章节「### 内部模式一致性检查要点」，内容如下：

  ```markdown
  ### 内部模式一致性检查要点

  审查同类代码时，重点检查以下模式一致性：

  1. **校验风格**：同类函数是否使用一致的参数校验方式（如都使用 Zod schema 或都使用手动 if 检查），不混用两种范式
  2. **命名规范**：相邻/同类函数的参数和返回值命名是否遵循相同约定（如 `opId` vs `operationId` 不混用）
  3. **错误处理**：同类操作的错误处理路径是否一致（如都抛出特定 Error 类型 vs 都返回 null，不混用）
  4. **返回模式**：同类查询函数是否使用一致的返回签名（如都返回 `{ data, error }` 或都直接返回值）
  5. **日志约定**：同类模块是否使用一致的日志格式和级别（如都使用 `appendLog` 方法）

  > 内部模式一致性审查的触发条件：当审查范围内存在 **≥2 个同类实体**（如同组函数、同模块方法、同命名前缀的类）时，必须逐条检查上述 5 项。
  ```

- [ ] 5. 更新「## 审查流程」章节，在流程图中增加模式一致性检查步骤：

  将原有流程：
  ```
  读取操作方案 → 审查代码 diff → 提交 REV 条目 → Schemer 修正 → 再审 → 通过
  ```

  替换为（单行 ASCII 流程图）：
  ```
  读取操作方案 → 审查代码 diff → 逐维度检查（含内部模式一致性） → 提交 REV 条目 → Schemer 修正 → 再审 → 通过
  ```

- [ ] 6. 在「## 注意事项」中追加一条关于模式一致性的条款：

  ```markdown
  - 模式一致性审查仅在有 ≥2 个同类实体时触发；单一孤立函数不强制要求此项。
  ```

- [ ] 7. 写完后校对全文，确认：章节层级正确（`##` / `###`）、表格对齐、无 markdown 语法错误

## 产出文件
- `.opencode/agents/reviewer.md`（修改）

## 自测清单
- [ ] `reviewer.md` 的「审查维度」表格包含 7 行（正确性/规范性/安全性/完整性/外部一致性/内部模式一致性），含「子维度」列
- [ ] 「内部模式一致性」行的检查内容包含"校验风格、命名规范、错误处理"
- [ ] 存在「### 内部模式一致性检查要点」章节，位于「审查维度」与「审查流程」之间
- [ ] 该章节包含 5 项检查要点（校验风格/命名规范/错误处理/返回模式/日志约定）
- [ ] 该章节包含触发条件说明（≥2 个同类实体）
- [ ] 「审查流程」文本中包含"逐维度检查（含内部模式一致性）"
- [ ] 「注意事项」中包含模式一致性触发条件条款
- [ ] 所有原有章节（核心职责/模型选择/其他注意事项）完整保留
- [ ] 文件编码为 UTF-8

## 修正记录
| 次数 | 时间 | 问题 | 修正内容 |
|------|------|------|----------|
