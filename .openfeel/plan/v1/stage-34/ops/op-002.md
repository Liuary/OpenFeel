# op-002：flow-manager 读取路径统一（findStatusPath 三级回退 + 健康检查同步 + 死代码清理）

- **阶段**：v1.0.0-stage-34
- **前置**：op-001（hard，复用 path.ts 的 findStageStatusPath）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

将 flow-manager.ts 的 status.md 读取路径从「stages 优先、plan 平铺回退」统一为 path.ts 的三级回退；健康检查 `checkCrossFileConsistency` 同步走 `plan/{series}/` 多级；删除 `checkZombieStates` 死代码。

## 实施步骤

### 步骤 1：顶部新增 import

`src/core/flow-manager.ts` 第 30 行（`export { type PipelinePhase ... }` 之后）新增：

```ts
import { findStageStatusPath } from './plan/path.js';
```

### 步骤 2：重构 findStatusPath（1380-1391 行）

将私有方法整体替换为委托实现：

```ts
  /** 查找 status.md 的路径（三级回退：plan/{series}/ 精确 → plan 递归 → stages 兜底） */
  private findStatusPath(stageId: string): string | null {
    return findStageStatusPath(this.projectPath, stageId);
  }
```

- 删除原 1381-1391 行的 `stagesDir` / `planDir` 双路径判断。
- `stageId` 参数可能为完整（`v1.0.0-stage-34`）、历史（`v4-stage-04`）或短名（`stage-01`），`findStageStatusPath` 已统一处理。

### 步骤 3：重构 checkCrossFileConsistency（2425-2475 行）

将方法体替换为（删除 `planDir`/`stagesDir` 两个局部变量，改用 `findStageStatusPath`）：

```ts
  /** 2. 检查 flow.json 与 plan/{series}/stage-NN/status.md 跨文件一致性 */
  private checkCrossFileConsistency(items: HealthCheckItem[]): void {
    if (!this.data) {
      return;
    }

    let total = 0;
    let consistent = 0;

    for (const [stageId, stage] of Object.entries(this.data.stages)) {
      // 三级回退查找 status.md（plan/{series}/ 精确 → plan 递归 → stages 兜底）
      const statusPath = findStageStatusPath(this.projectPath, stageId);
      if (!statusPath) {
        continue;
      }

      total++;
      try {
        const content = readFileSync(statusPath, 'utf-8');
        // 查找状态行：**状态**：planned
        const statusMatch = content.match(/\*\*状态\*\*[：:]\s*(\w+)/);
        if (statusMatch) {
          const fileStatus = statusMatch[1];
          if (fileStatus === stage.status) {
            consistent++;
          } else {
            items.push({ section: '跨文件一致性', status: 'warn', message: `${stageId}: flow.json 状态="${stage.status}"，status.md 状态="${fileStatus}"` });
          }
        }
      } catch {
        items.push({ section: '跨文件一致性', status: 'warn', message: `${stageId}: 无法读取 status.md` });
      }
    }

    if (total > 0) {
      if (consistent === total) {
        items.push({ section: '跨文件一致性', status: 'pass', message: `一致 (${consistent}/${total} stages)` });
      } else {
        items.push({ section: '跨文件一致性', status: 'warn', message: `${consistent}/${total} stages 一致` });
      }
    }
  }
```

- 删除原 2431-2434 行 `planDir` / `stagesDir` 局部变量声明。
- 删除原 2440-2448 行「stages 优先 → plan 平铺回退」的手写判断，整体收敛到 `findStageStatusPath`。

### 步骤 4：删除 checkZombieStates 死代码（2483-2485 行）

删除以下两行（`planDir` / `stagesDir` 定义后循环 2487-2504 未引用，属死代码）：

```ts
    // 僵尸阶段检测：各 stage 在 status.md 中声明的状态与 stage.status 不一致
    const planDir = resolve(this.projectPath, '.openfeel', 'plan');
    const stagesDir = resolve(this.projectPath, '.openfeel', 'stages');
```

保留注释第一行，仅删除 `planDir` / `stagesDir` 两行。该方法不涉及 status.md 路径读取，无需接入 path.ts。

### 步骤 5：更新 flow-manager.test.ts 断言

`test/core/flow-manager.test.ts`：

1. **1675 行注释更新**（从「stages 优先」改为「三级回退」）：

```ts
      // 构造 status.md（findStatusPath 三级回退：plan/{series}/ 精确 → plan 递归 → stages 兜底）
```

2. **1676 行路径改为多级**（验证第 1 级精确路径）：

```ts
      const planDir = join(tmpDir, '.openfeel', 'plan', 'v1', 'stage-01');
```

   （原为 `join(tmpDir, '.openfeel', 'plan', 'stage-01')` 平铺。该测试 `current.stage = 'stage-01'` 短名，`findStageStatusPath` 解析为 series `v1`、stageDir `stage-01`，第 1 级精确命中 `plan/v1/stage-01/status.md`。）

3. **2099 / 2109 行 deps.yaml 测试确认不改**：`writeFileSync(join(planDir, 'deps.yaml'), ...)` 中 `planDir = .openfeel/plan` 顶层，deps.yaml 位置正确（`.openfeel/plan/deps.yaml`），路径与系列子目录无关，**保持原样**。

## 产出文件

- `src/core/flow-manager.ts`
- `test/core/flow-manager.test.ts`

## 自测清单

- [ ] findStatusPath 已委托 `findStageStatusPath`，无「stages 优先」残留
- [ ] checkCrossFileConsistency 遍历 stages 键走三级回退，无 `planDir`/`stagesDir` 局部变量残留
- [ ] checkZombieStates 2484-2485 两行死代码已删除，循环逻辑不变
- [ ] checkDepsYaml（2568 行）`.openfeel/plan/deps.yaml` 路径未被改动
- [ ] `npm test` 中 flow-manager.test.ts（recoverContext + 健康检查相关）通过

## 验证

- op-002 完成后执行 `npm test`，确认 flow-manager.test.ts 通过（尤其 recoverContext 的 1675 测试与健康检查测试）。

## 困难点说明

- **findStatusPath 语义变化**：原实现「stages 优先」→ 新实现「plan/{series}/ 优先」。存量项目 status.md 仍在 `stages/` 时由第 3 级兜底读取，不破坏（决策 3）。op-004 的 commands/stage.ts 将复用同一语义，故 op-002 先定版（soft 依赖）。
- **recoverContext 测试兼容**：原测试 status.md 放平铺 `plan/stage-01/`，改多级 `plan/v1/stage-01/` 后由第 1 级命中，语义不变、断言不变（`stageStatus` 内容相同）。
