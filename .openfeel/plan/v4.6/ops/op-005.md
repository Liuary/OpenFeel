# op-005：更新 core.md — 职责边界和调度列表

- **阶段**：v4.6-stage-01
- **前置**：op-001, op-002, op-003（确定 vision 的最终名称）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

更新 `.opencode/instructions/core.md` 中两处 Agent 列表，添加 Vision。

## 实施步骤

> **重要**：修改前须先 `read .opencode/instructions/core.md` 确认当前文件内容与预期一致。使用 `edit` 工具精确替换。

### 步骤 1：更新路径自校验规则适用范围（行 63）

在「此规则适用于所有 Agent」列表中插入 Vision，位于 Feel Tester 之后、Archiver 之前。

**旧文本**（精确匹配）：
```
此规则适用于所有 Agent（Feel / Planner / Schemer / Executor / Reviewer / Feel Tester / Archiver）。
```

**新文本**：
```
此规则适用于所有 Agent（Feel / Planner / Schemer / Executor / Reviewer / Feel Tester / Vision / Archiver）。
```

### 步骤 2：更新 Feel 调度下游 Agent 列表（行 148）

在 Feel 调度列表中插入 Vision，位于 Feel Tester 之后、Archiver 之前。

**旧文本**（精确匹配）：
```
人工流程为默认模式。Feel 根据 flow.json 状态调度下游 Agent（Planner / Schemer / Executor / Reviewer / Feel Tester / Archiver），不依赖旧式自动化调度。
```

**新文本**：
```
人工流程为默认模式。Feel 根据 flow.json 状态调度下游 Agent（Planner / Schemer / Executor / Reviewer / Feel Tester / Vision / Archiver），不依赖旧式自动化调度。
```

### 步骤 3：最终验证

重新 `read .opencode/instructions/core.md`，确认：
- 行 63 的自校验范围列表包含 Vision
- 行 148 的调度列表包含 Vision
- 两处 Vision 的位置均为 Feel Tester 之后、Archiver 之前

## 产出文件

- `.opencode/instructions/core.md`（修改）

## 自测清单

- [ ] 行 63（路径自校验范围）包含 `Vision`，序列为 `... / Feel Tester / Vision / Archiver`
- [ ] 行 148（Feel 调度列表）包含 `Vision`，序列为 `... / Feel Tester / Vision / Archiver`
- [ ] 两处 Agent 列表均保持原有顺序（Planner → Schemer → Executor → Reviewer → Feel Tester → **Vision** → Archiver）
- [ ] 文件编码和行尾未被改变
- [ ] 文件其他内容无意外修改（通过与原始文件 diff 确认仅两处变更）
