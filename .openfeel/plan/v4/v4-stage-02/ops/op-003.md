# op-003：扩展 check-kb skill — 自动触发语义检索回退

- **阶段**：v4-stage-02
- **状态**：done
- **前置**：无
- **负责 Agent**：Executor
- **最多重试**：3

## 目标
修改 `check-kb` 技能的步骤 5（语义检索回退），使其在精确匹配无结果时**真正自动触发**语义检索，消除当前"需手动调用 search-kb skill"的中间步骤，实现一步到位的回退。

## 背景
当前步骤 5 的标题写"自动触发"，但实质操作是：提示 Agent "加载 `skills/search-kb/SKILL.md` 技能"——这要求 Agent 额外调用 `skill("search-kb")`，并未实现真正的自动语义检索。本 op 将步骤 5 改写为自包含的语义检索流程。

## 实施步骤
- [x] 1. **读取本方案文件**：执行前先完整阅读 `op-003.md`，确认所有步骤和自测清单理解无误
- [x] 2. 读取当前 `C:\Code\AI\OpenFeel\.opencode\skills\check-kb\SKILL.md` 完整内容（88 行）
- [x] 3. 定位「### 5. 语义检索回退」章节（约第 50~78 行），将其**整块替换**为以下内容：

  ```markdown
  ### 5. 语义检索回退（自动）

  当精确匹配未找到任何相关条目（无 `[+]` 条目或所有条目与当前任务无关）时，**本技能自行执行语义检索**，无需调用方另行加载 `search-kb`：

  1. **索引就绪检查**：检查 `.openfeel/tmp/vectors/index.json` 是否存在
  2. **索引缺失时**：输出以下提示后结束，不再继续回退：
     ```
     💡 精确匹配未找到相关条目，语义检索的向量索引尚未构建。

        运行以下命令构建索引后再重试：
        pip install sentence-transformers
        python scripts/build_kb_index.py
     ```
  3. **索引就绪时**：从当前任务上下文中提取关键词和需求描述，构造查询文本，执行：
     ```
     python scripts/search_kb.py "<查询文本>" --top-k 10 --verbose
     ```
  4. **解析并输出**：将检索结果格式化输出：

     ```
     💡 精确匹配未找到相关条目，已自动回退到语义检索。

     🔍 语义检索结果（共 {N} 条）

       #{1} [{分类}] {条目标题}
         文件: {分类}.md | 得分: {score}
         内容: {摘要}

       #{2} ...
     ```

  5. **低分提示**：若所有结果得分均低于 0.3，追加提示：
     ```
     ⚠️ 所有语义检索结果得分均低于 0.3，建议优化查询词或确认知识库覆盖范围。
     ```

  6. **索引过期提示**：检索完成后追加：
     ```
     💡 如需确保索引为最新，可运行: python scripts/build_kb_index.py --dry-run
     ```

  > 注：语义检索是 `check-kb` 的内置回退能力。调用方只需 `skill("check-kb")` 一次，无需再手动调用 `search-kb`。
  ```

- [x] 4. 同时修改 skill 顶部的 `description` 字段（frontmatter 第 3 行），从：
  ```
  description: 渐进式查阅 .openfeel/kb/ 项目知识库，按当前任务需求返回最相关的参考信息，避免一次加载全量内容。
  ```
  改为：
  ```
  description: 渐进式查阅 .openfeel/kb/ 项目知识库，按当前任务需求返回最相关的参考信息。精确匹配无结果时自动触发语义检索回退，避免一次加载全量内容。
  ```

- [x] 5. 确认「### 6. 强制检索标记」章节（约第 80~88 行）**保持不变**，不做任何修改
- [x] 6. 写完后校对行数，预计从 88 行增至约 105 行（步骤 5 内容更详细）

## 产出文件
- `.opencode/skills/check-kb/SKILL.md`（修改）

## 自测清单
- [x] `SKILL.md` frontmatter 的 `description` 包含「自动触发语义检索回退」
- [x] 「### 5. 语义检索回退」标题变为「### 5. 语义检索回退（自动）」
- [x] 步骤 5 中不再出现「加载 `skills/search-kb/SKILL.md` 技能」字样
- [x] 步骤 5 包含「本技能自行执行语义检索」的明确声明
- [x] 步骤 5 包含索引就绪检查（检查 `index.json` 是否存在）
- [x] 步骤 5 包含索引缺失时的提示（含 `pip install` 和 `build_kb_index.py` 命令）
- [x] 步骤 5 包含语义检索命令 `python scripts/search_kb.py "<查询文本>" --top-k 10 --verbose`
- [x] 步骤 5 包含结果格式化输出模板（含得分和摘要）
- [x] 步骤 5 包含低分提示（得分 < 0.3 时）
- [x] 步骤 5 末尾有注解说「调用方只需 `skill("check-kb")` 一次」
- [x] 「### 6. 强制检索标记」章节内容完全不变
- [x] 文件编码为 UTF-8

## 修正记录
| 次数 | 时间 | 问题 | 修正内容 |
|------|------|------|----------|
