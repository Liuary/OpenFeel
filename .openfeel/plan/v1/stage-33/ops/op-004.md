# op-004：decisions.md 纳入框架标准（新建实例 + templates.ts + init.ts + core.md 双层模板源 + 测试断言）
- **阶段**：v1.0.0-stage-33
- **前置**：无（完全独立，批次 1 可并行）。**REV-007 已处理**：deps.yaml produces 已补 `test/core/init.test.ts`（8→9 项），plan.md 概览文件数已改 9
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

长期技术/架构决策获得独立持久存储 `.openfeel/dev/decisions.md`（ADR 轻量格式：决策+理由+日期+状态），纳入框架标准——新建项目实例 + init 自动生成 + core.md 会话自检纳入 + 写入规则定义 + init.test.ts 必选断言。

## ⚠️ 前置（core.md 双层模板源已发散）

计划约束#4/REV 假设 `core-instructions/{lang}.md` 与 `opencode/instructions/{lang}.md` 逐行一致。**实测 zh 层已发散**：

- `core-instructions/zh-CN.md`（update 路径）的「此规则适用于所有 Agent（… / Archiver）」与「调度下游 Agent（… / Archiver）」两处**缺 Vision**；
- `opencode/instructions/zh-CN.md`（init 路径）与 `.opencode/instructions/core.md`（部署实例）两处**含 Vision**；
- en 层两文件一致（都缺 Vision，未发散）。

本 op 只改「公共域文件」列表（第 22-25 行），该列表在 4 个文件（2 层 × 2 语言）中**位置与内容完全一致**，改动安全。**禁止整文件复制**（会错位 Vision 差异）。此发散属 Vision 扩展（8→9 Agent）遗留，超出本 stage 范围，仅在方案如实标注，不修复。

## 实施步骤

### 改动 1：新建 `.openfeel/dev/decisions.md`（新增文件）

内容 = ADR 骨架 + 一条 seed 记录。**注意**：计划正文「如 C# 技术选型」为 Pantheogen 项目示例；OpenFeel 本仓库技术栈为 TypeScript，seed 条目改用本仓库自身已知长期决策（技术栈选型 TypeScript），语义等价（记录"本仓库的已知长期决策"）。

```markdown
# 决策记录（ADR）

> 长期技术/架构决策（技术选型、架构方向、跨会话有效的设计取舍）以 ADR 轻量格式记录于此。
> 会话临时决策（流程调整、单次取舍）记录在 .openfeel/users/{username}/dev_last.md 的「决策历史」节，不写入本文件。
> 写入时机：Feel 做出长期技术/架构决策时，同步追加一条 ADR 记录。

## ADR 模板

### ADR-{NNN}：{决策标题}
- **日期**：{yyyy-mm-dd}
- **状态**：proposed / accepted / superseded / deprecated
- **决策**：{一句话描述采纳的决策内容}
- **理由**：{为什么这样决策，含备选方案及取舍分析}

## ADR 记录

### ADR-001：技术栈选型 TypeScript (Node.js ≥20)
- **日期**：2026-08-15
- **状态**：accepted
- **决策**：OpenFeel 采用 TypeScript（Node.js ≥20）实现，核心依赖 Commander / Zod / YAML / fast-glob。
- **理由**：CLI 工具需类型安全与丰富生态；Zod 提供运行时校验，YAML 兼顾可读性与工具链成熟度。
```

### 改动 2：`src/core/templates.ts` 新增模板常量与获取函数

**位置 A**：在 `CURRENT_TEMPLATE_EN`（第 34 行）之后、`getDevCoreTemplate` 之前，新增两个模板常量。

> **模板字符串安全约束**：以下内容**不得包含反引号与 `${`**（避免模板字符串转义/插值错误）。路径中的 `{username}`、`{NNN}` 等花括号**无 `$` 前缀**，安全。

```typescript
/** 中文版 decisions.md 模板（ADR 轻量格式：决策+理由+日期+状态） */
const DECISIONS_TEMPLATE_ZH = `# 决策记录（ADR）

> 长期技术/架构决策（技术选型、架构方向、跨会话有效的设计取舍）以 ADR 轻量格式记录于此。
> 会话临时决策（流程调整、单次取舍）记录在 .openfeel/users/{username}/dev_last.md 的「决策历史」节，不写入本文件。
> 写入时机：Feel 做出长期技术/架构决策时，同步追加一条 ADR 记录。

## ADR 模板

### ADR-{NNN}：{决策标题}
- **日期**：{yyyy-mm-dd}
- **状态**：proposed / accepted / superseded / deprecated
- **决策**：{一句话描述采纳的决策内容}
- **理由**：{为什么这样决策，含备选方案及取舍分析}
`;

/** 英文版 decisions.md 模板（ADR 轻量格式） */
const DECISIONS_TEMPLATE_EN = `# Decision Log (ADR)

> Long-term technical/architecture decisions (technology selection, architecture direction, cross-session design trade-offs) are recorded here in lightweight ADR format.
> Session-scoped temporary decisions (process adjustments, one-off trade-offs) are recorded in the "Decision History" section of .openfeel/users/{username}/dev_last.md, not in this file.
> Write timing: When Feel makes a long-term technical/architecture decision, append an ADR entry synchronously.

## ADR Template

### ADR-{NNN}: {Decision Title}
- **Date**: {yyyy-mm-dd}
- **Status**: proposed / accepted / superseded / deprecated
- **Decision**: {one-sentence description of the adopted decision}
- **Rationale**: {why this decision, including alternatives and trade-off analysis}
`;
```

**位置 B**：在 `getCurrentTemplate`（第 48-50 行）之后，新增获取函数：

```typescript
/**
 * 根据语言返回 decisions.md 模板
 * @param lang 语言，'zh-CN' 或 'en'
 */
export function getDecisionsTemplate(lang: 'zh-CN' | 'en'): string {
  return lang === 'en' ? DECISIONS_TEMPLATE_EN : DECISIONS_TEMPLATE_ZH;
}
```

**位置 C**：在向后兼容导出段（第 53-54 行）追加：

```typescript
export const DECISIONS_TEMPLATE = DECISIONS_TEMPLATE_ZH;
```

### 改动 3：`src/core/init.ts` 新增 decisions.md 生成步

**位置 A**（import，第 19 行）：

- 旧：`import { getDevCoreTemplate, getCurrentTemplate } from './templates.js';`
- 新：`import { getDevCoreTemplate, getCurrentTemplate, getDecisionsTemplate } from './templates.js';`

**位置 B**（生成步）：在第 6 步 current.md（第 323-328 行）之后、第 7 步 kb/index.md（第 330 行）之前，插入：

```typescript
  // 6b. 生成 .openfeel/dev/decisions.md 模板（ADR 格式）
  const decisionsPath = resolve(projectPath, '.openfeel', 'dev', 'decisions.md');
  const decisionsResult = writeTemplateIfMissing(decisionsPath, getDecisionsTemplate(selectedLang));
  if (decisionsResult.created) {
    created.push('.openfeel/dev/decisions.md');
  }
```

**位置 C**（文档注释，第 253 行）：把「生成 dev_core.md → 生成 current.md → 生成 AGENTS.md」改为「生成 dev_core.md → 生成 current.md → 生成 decisions.md → 生成 AGENTS.md」。

### 改动 4-7：core.md 双层模板源「公共域文件」列表加 decisions.md

4 个文件（`core-instructions/zh-CN.md`、`core-instructions/en.md`、`opencode/instructions/zh-CN.md`、`opencode/instructions/en.md`）各在「公共域文件 / Public domain files」列表中、`.openfeel/dev/current.md` 之后插入一行：

zh-CN（在 `- `.openfeel/dev/current.md`` 之后新增）：

```markdown
- `.openfeel/dev/decisions.md`
```

en（在 `- `.openfeel/dev/current.md`` 之后新增）：

```markdown
- `.openfeel/dev/decisions.md`
```

> 插入后列表变为：dev_core.md → current.md → **decisions.md** → kb/index.md（顺序一致，decisions.md 位于 current.md 之后、kb/index.md 之前）。

### 改动 8：部署实例 `.opencode/instructions/core.md`

与改动 4 相同，在「公共域文件」列表 `.openfeel/dev/current.md` 之后插入 `- `.openfeel/dev/decisions.md``。

### 改动 9：`test/core/init.test.ts` 新增断言（必选，REV-005/REV-007）

在 `describe('initProject', ...)` 内、`it('应返回正确的 created 列表', ...)` 之后（第 110 行 `});` 之前），新增一个用例：

```typescript
  it('应生成 .openfeel/dev/decisions.md（ADR 模板骨架）', async () => {
    const result = await initProject(tmpDir);
    const decisionsPath = join(tmpDir, '.openfeel', 'dev', 'decisions.md');
    expect(existsSync(decisionsPath)).toBe(true);
    expect(result.created.some((e) => e === '.openfeel/dev/decisions.md')).toBe(true);

    const content = readFileSync(decisionsPath, 'utf-8');
    // ADR 骨架四要素：决策 / 理由 / 日期 / 状态
    expect(content).toContain('决策');
    expect(content).toContain('理由');
    expect(content).toContain('日期');
    expect(content).toContain('状态');
  });
```

> **断言依据**：vitest 非交互模式 `initProject(tmpDir)` 默认语言 zh-CN，生成的 decisions.md 为 DECISIONS_TEMPLATE_ZH，含「决策/理由/日期/状态」四标签。现有 `已存在 .openfeel/ 时不覆盖已有文件` 用例（`result2.created.length === 0`）因 decisions.md 使用 `writeTemplateIfMissing` 不受影响。

## 产出文件

- `.openfeel/dev/decisions.md`（新增）
- `src/core/templates.ts`
- `src/core/init.ts`
- `src/core/templates-data/core-instructions/zh-CN.md`
- `src/core/templates-data/core-instructions/en.md`
- `src/core/templates-data/opencode/instructions/zh-CN.md`
- `src/core/templates-data/opencode/instructions/en.md`
- `.opencode/instructions/core.md`
- `test/core/init.test.ts`

## 依赖版本锁定

本 op 无第三方依赖变更。

## 自测清单

- [ ] `.openfeel/dev/decisions.md` 已新建，含「ADR 模板」骨架（决策/理由/日期/状态 四字段）+ 一条 seed ADR 记录
- [ ] `templates.ts`：DECISIONS_TEMPLATE_ZH/EN 常量内容**不含反引号、不含 `${`**；`getDecisionsTemplate` 已导出；向后兼容导出 `DECISIONS_TEMPLATE` 已加
- [ ] `init.ts`：import 已加 getDecisionsTemplate；6b 步已插入且用 `writeTemplateIfMissing`；文档注释已更新
- [ ] core.md 4 文件 + 部署实例「公共域文件」列表均含 `.openfeel/dev/decisions.md`（位于 current.md 之后、kb/index.md 之前）
- [ ] 未误改 core.md 双层源既有的 Vision 差异（opencode/instructions/zh-CN.md 与 .opencode/instructions/core.md 的「Vision」两处仍在；core-instructions/zh-CN.md 两处仍无 Vision）
- [ ] `test/core/init.test.ts` 新增 decisions.md 断言用例，`expect(content).toContain('决策')` 等四断言可过
- [ ] `npm run build` 通过（步骤 1 generateTemplateFromCoreMd + 步骤 7 generateOpencodeConfigTemplates + validateCoreInstruction + validateOpencodeConfigTemplates 全绿；templates.ts 改动不触发 build 校验，但须 `npx tsc` 编译通过）
- [ ] `npm test` 通过（新增 decisions 断言 + 406 全量无回归；重点确认「已存在 .openfeel/ 时不覆盖」与「created 列表」两用例仍通过）
