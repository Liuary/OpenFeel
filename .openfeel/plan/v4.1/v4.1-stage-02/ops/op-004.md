# op-004：executor.md 重构 — 执行纪律硬化 + 前置校验精简 + 非编码小活承接

- **阶段**：v4.1-stage-02
- **前置**：无（与 op-001/002/003 并行安全——修改不同文件）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

将 `executor.md` 从 157 行**重构**到 100–120 行，压缩冗长的前置校验步骤描述，硬化执行纪律，新增非编码小活承接规则和违规后果。

> 参见 kb/patterns.md #Executor 强制第一步读方案、#Executor 前置校验三步模式 — 这些已沉淀的模式需在重构中保留核心逻辑但精简重复叙述。
> 参见 kb/troubleshooting.md #流水线文件引用断裂连锁修复 — 前置校验中的 CLI 命令引用应基于已验证的命令。

## 实施步骤

- [ ] 1. 读取当前 `.opencode/agents/executor.md` 完整内容（158 行），确认当前结构和各个校验步骤的文本量
- [ ] 2. **精简步骤 1（方案完整性校验）**：将 6 行的表格压缩为 3 行紧凑列表，删除冗余的 `缺失时反馈` 模板列（统一为引用式规范：`缺失任一必填项 → 拒绝执行，返回"方案 {op-id} 缺少 {字段名}"`）
- [ ] 3. **精简步骤 2（Phase 合法性校验）**：将 4 个子步骤合并为 2 条规则（校验 phase 枚举 + 确认 op-id 匹配），删除过度详细的反馈模板文字
- [ ] 4. **精简步骤 3（FlowManager 流转合法性校验）**：合并 3a/3b/3c 的文字，将"CLI 优先→手动兜底"的 40+ 行描述压缩为 15 行以内：
  - 保留：CLI 优先原则、`openfeel flow health --quick` 命令、手动 transitions 表兜底
  - 删除：重复的错误反馈模板（统一为 `"阶段流转不合法：{reason}"`）、过长的注释说明
  - 保留 3c 结果记录要求（精简为 2 行）
- [ ] 5. 在 `## 工作规则` 之后（前置校验之前）新增 `## 执行纪律` 节：
  - **第一步**：必须 `read` 方案文件完整内容，逐 checkbox 执行
  - **禁止**：看到"参考部署路径"就直接复制整个文件（跳步）
  - **标准流程**：读方案 → 前置校验 → 探索代码 → 编码 → 自测 → 回写
  - **违规后果**：跳步执行须记录到自测报告的「偏差记录」字段
  - 参见 kb/patterns.md #Executor 强制第一步读方案
- [ ] 6. 在 `## 工作规则` 之后新增 `## 非编码小活承接` 节：
  - 当 事务官 模型为 fast 无法胜任复杂判断时，Feel 可派非编码小活给 Executor
  - 适用任务类型：格式批量替换、配置项整理、文档结构调整
  - Feel 须在任务描述中显式声明 `type: utility`
  - Executor 收到 `type: utility` 任务时，仍须执行前置校验但可跳过完整代码探索步骤
- [ ] 7. 在 `## 方案一致性回写` 中补充一行：若存在「偏差记录」中含跳步违规，须额外标注到自测报告顶部
- [ ] 8. 压缩 `## 注意事项` 中重复的约束（如 CLI 命令使用与前置校验中已有说明的，合并引用）
- [ ] 9. 最终检查：`wc -l` 确认行数在 100–120 范围内；优先保留执行纪律和非编码小活，继续压缩不必要的前置校验模板文字

## 产出文件

- `.opencode/agents/executor.md`

## 自测清单

- [ ] executor.md 行数在 100–120 范围（`wc -l`）
- [ ] `## 执行纪律` 节存在，含"第一步必须 read 方案文件"和"禁止看路径直接复制"
- [ ] `## 非编码小活承接` 节存在，含 `type: utility` 声明要求
- [ ] 前置校验三步的原有核心逻辑保留：
  - 步骤 0（读取方案）完整保留
  - 步骤 1（方案完整性）压缩后仍有 6 项必填字段检查
  - 步骤 2（Phase 合法性）压缩后仍有 phase 枚举 + op-id 匹配
  - 步骤 3（FlowManager 校验）压缩后仍保留 CLI 优先 + 手动兜底双路
- [ ] 原有 frontmatter 保持不变（model: deepseek/deepseek-v4-flash 保留）
- [ ] `## 方案一致性回写` 节保留但已接入偏差记录
- [ ] git diff 检查压缩合理（删除的均为冗余模板文字，非逻辑变更）
- [ ] 文件编码 UTF-8

## 修正记录

| 次数 | 时间 | 问题 | 修正内容 |
|------|------|------|----------|
| 自检 | 2026-07-07 14:30 | 方案一致性检查 | 声明与产出一致，无偏差 |
