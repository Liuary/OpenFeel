# op-003：约束清理

- **阶段**：v4.1-stage-01
- **前置**：op-001（soft — op-001 落地构建自动同步后，手动同步约束才真正过时；但约束清理本身可先行）
- **负责 Agent**：Executor
- **最多重试**：2

## 目标

将 `.openfeel/dev/dev_core.md` 中「模板文件同步约束」条目标记为禁用，检查 `AGENTS.md` 中是否有手动同步提醒并处理，在公共日志记录变更原因。

## 知识库参考

- kb/patterns.md #CLI 原子管理模式 — 状态变更应通过 CLI 而非直接 edit（但 dev_core.md 和 AGENTS.md 是纯文档文件，无 CLI 管理，可直接 edit）

## 实施步骤

- [ ] **步骤 1**：read 本方案文件完整内容
- [ ] **步骤 2**：读取 `.openfeel/dev/dev_core.md`，定位 `## [+] 模板文件同步约束 (2026-07-02)` 条目（预计在第 112 行附近），将 `[+]` 标记改为 `[-]`
- [ ] **步骤 3**：在该条目正文末尾追加禁用原因说明（另起一行）：
  ```markdown
  > **已禁用 (v4.1-stage-01)**：构建时自动同步已落地。`npm run build` 自动完成三类模板的编码和注入，无需手动同步。参见 op-001。
  ```
- [ ] **步骤 4**：检查 `AGENTS.md` 全文（99 行），搜索是否有以下任一类描述：
  - "修改 Agent 后需手动更新 templates.ts"
  - "模板更新后需手动执行 XX"
  - "手动同步模板"
  - 其他与"手动同步模板"相关的提醒
  - **预判**：经审阅，AGENTS.md 中没有上述描述，无需修改。
- [ ] **步骤 5**：在公共日志 `.openfeel/log/` 中记录本次约束变更：
  - 创建日期目录（如 `2026/07/`），生成日志文件 `2026-07-06-{username}-001.md`
  - 更新 `index.md` 索引
  - 日志内容：简述禁用原因（"模板文件同步约束 (dev_core.md) 已禁用，原因：构建时自动同步已落地"）
  - 若 AGENTS.md 无需修改，也一并记录"AGENTS.md 经检查无需修改"
- [ ] **步骤 6**：运行 `npm test` 确保无回归

## 产出文件

- `.openfeel/dev/dev_core.md` — 1 条约束从 `[+]` 改为 `[-]`
- `.openfeel/log/2026/07/2026-07-06-{username}-001.md` — 新增变更日志
- `.openfeel/log/2026/07/day_index.md` — 可能新增
- `.openfeel/log/index.md` — 更新索引
- `AGENTS.md` — 确认无需修改（在自测报告中记录）

## 自测清单

- [ ] `dev_core.md` 中「模板文件同步约束」已标记为 `[-]`
- [ ] 禁用原因说明已追加，含 `v4.1-stage-01` 标识
- [ ] `AGENTS.md` 确认无需修改（附全文搜索结论）
- [ ] 公共日志已记录变更（含日期、原因、操作者）
- [ ] dev_core.md 其他 `[+]` 条目未被误改
- [ ] `npm test` 通过
