# op-003：plan 核心层写入迁移（stage.ts + scheme.ts 从 stages/ 改写 plan/{series}/）

- **阶段**：v1.0.0-stage-34
- **前置**：op-001（hard，复用 parseStageId / stageIdToPlanDir / normalizeStageId）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

将 `plan stage add` / `plan scheme create` 的写入目录从 `.openfeel/stages/` 迁移到 `.openfeel/plan/{series}/`，flow.json 键从短名统一为完整 stageId（决策 6），参数接受完整 stageId 或短名别名。

## 关键设计决策（困难点显式列出）

1. **flow.json 键规范化**（决策 6）：短名 `stage-01` 与完整 `v1.0.0-stage-01` 均注册为完整键 `v1.0.0-stage-01`。实现：`addStage`/`createScheme` 内部用 `parseStageId(name).fullStageId` 作为 `registerStage`/`syncToFlowJson` 的键，目录用 `parseStageId(name)` 的 `series` + `stageDir`。
2. **getScheme 点号解析**：完整 stageId `v1.0.0-stage-01` 含点号，原 `indexOf('.')` 分割会错误切分版本号。改用正则 `/^(.+)\.(op-\d+)$/`（尾部锚定 `op-NNN`），`.+` 贪婪匹配 + 回溯正确切出 stage 部分。
3. **非数字 NN 边界**：parseStageId 严格匹配 `stage-(\d+)`，现有 scheme.test.ts 的 `stage-auto` 非标准名解析失败，测试中改为合法短名 `stage-03`（决策 2 明确 stage 目录名为 `stage-NN` 数字）。
4. **listStages/listSchemes 两层遍历**：目录从一层（`stages/`）变两层（`plan/{series}/stage-NN/`），用 readdirSync 双重嵌套遍历；`Stage.name`/`Scheme.stage` 字段保持返回 stageDir（`stage-01`，展示用），`path`/`filePath` 字段返回多级相对路径。

## 实施步骤

### 步骤 1：改 `src/core/plan/stage.ts`

文件头注释（第 2-3 行）更新：`负责 .openfeel/stages/ 下的阶段目录创建与列取` → `负责 .openfeel/plan/{series}/ 下的阶段目录创建与列取`。

新增 import（第 7 行 `FlowManager` import 之后）：

```ts
import { parseStageId } from './path.js';
```

`Stage` 接口（第 13-14 行）注释更新：`相对路径 .openfeel/stages/stage-01/` → `相对路径 .openfeel/plan/v1/stage-01/`。

`addStage`（第 24-105 行）整体替换：

```ts
export function addStage(projectPath: string, name: string, deps?: string[]): void {
  // 解析 stageId（短名/完整），无法解析时抛错
  const parsed = parseStageId(name);
  if (!parsed) {
    throw new Error(`非法阶段名: ${name}（应为 stage-NN 或 vX.Y.Z.W-stage-NN）`);
  }

  const stageDir = resolve(projectPath, '.openfeel', 'plan', parsed.series, parsed.stageDir);

  // 确保阶段目录存在
  if (!existsSync(stageDir)) {
    mkdirSync(stageDir, { recursive: true });
  }

  // 创建 overview.md（若不存在）— 标题用完整 stageId
  const overviewPath = join(stageDir, 'overview.md');
  if (!existsSync(overviewPath)) {
    const depsText = deps && deps.length > 0 ? deps.map((d) => `- ${d}`).join('\n') : '无';
    const overviewContent = `# ${parsed.fullStageId}

## 目标

> 待补充

## 依赖

${depsText}

## 操作方案

> 待补充
`;
    writeFileSync(overviewPath, overviewContent, 'utf-8');
  }

  // 创建 status.md（若不存在）— 标题用完整 stageId
  const statusPath = join(stageDir, 'status.md');
  if (!existsSync(statusPath)) {
    const statusContent = `# ${parsed.fullStageId} 状态
...
`;
    writeFileSync(statusPath, statusContent, 'utf-8');
  }

  // 同步到 flow.json（若存在）— 键用完整 stageId
  const flowMgr = new FlowManager(projectPath);
  if (flowMgr.isLoaded()) {
    flowMgr.registerStage(parsed.fullStageId, deps ?? []);
    flowMgr.save();
  }
}
```

> status.md 骨架其余字段（执行模式/自动推进/状态记录等）保持原样，仅标题行 `${name}` → `${parsed.fullStageId}`。详见原文件第 65-95 行模板，逐字保留、仅改标题与 `${name}` 引用。

`listStages`（第 110-146 行）整体替换（两层遍历）：

```ts
export function listStages(projectPath: string): Stage[] {
  const planDir = resolve(projectPath, '.openfeel', 'plan');

  // 目录不存在时返回空列表
  if (!existsSync(planDir)) {
    return [];
  }

  const result: Stage[] = [];

  // 遍历 plan/{series}/ 下的 series 目录，再遍历其下 stage-NN 目录
  const seriesEntries = readdirSync(planDir, { withFileTypes: true });
  for (const seriesEntry of seriesEntries) {
    if (!seriesEntry.isDirectory()) {
      continue;
    }
    const seriesDir = join(planDir, seriesEntry.name);

    let stageEntries: import('node:fs').Dirent[];
    try {
      stageEntries = readdirSync(seriesDir, { withFileTypes: true });
    } catch {
      continue; // series 目录不可读时跳过
    }

    for (const stageEntry of stageEntries) {
      if (!stageEntry.isDirectory()) {
        continue;
      }

      const stageDirPath = join(seriesDir, stageEntry.name);
      const overviewPath = join(stageDirPath, 'overview.md');

      let overview = '';
      if (existsSync(overviewPath)) {
        overview = readFileSync(overviewPath, 'utf-8');
      }

      result.push({
        name: stageEntry.name,
        path: `.openfeel/plan/${seriesEntry.name}/${stageEntry.name}/`,
        overview,
      });
    }
  }

  // 按名称排序
  result.sort((a, b) => a.name.localeCompare(b.name));

  return result;
}
```

### 步骤 2：改 `src/core/plan/scheme.ts`

文件头注释（第 3-4 行）：`.openfeel/stages/{stage}/ops/` → `.openfeel/plan/{series}/{stage}/ops/`。

新增 import（第 8 行之后）：

```ts
import { parseStageId } from './path.js';
```

`Scheme` 接口（第 20-21 行）注释：`相对路径 .openfeel/stages/{stage}/ops/op-001_{title}.md` → `相对路径 .openfeel/plan/{series}/{stage}/ops/op-001_{title}.md`。

`syncToFlowJson`（第 101-152 行）：第 121-129 行 `flowData.stages[stageName]` 改用完整 stageId（调用方传入已规范化的完整 ID，见步骤 3），函数体 `stageName` 参数语义变为「完整 stageId」，注释同步。

`createScheme`（第 162-186 行）替换：

```ts
export function createScheme(projectPath: string, stageName: string, title: string): string {
  // 解析 stageId（短名/完整）得到 series + stageDir + 完整 ID
  const parsed = parseStageId(stageName);
  if (!parsed) {
    throw new Error(`非法阶段名: ${stageName}（应为 stage-NN 或 vX.Y.Z.W-stage-NN）`);
  }

  // 1. 确保 .openfeel/plan/{series}/{stageDir}/ops/ 目录存在
  const opsDir = resolve(projectPath, '.openfeel', 'plan', parsed.series, parsed.stageDir, 'ops');
  if (!existsSync(opsDir)) {
    mkdirSync(opsDir, { recursive: true });
  }

  // 2. 计算下一个 opId
  const opId = getNextOpId(opsDir);

  // 3. 生成模板内容并写入文件
  const safeTitle = title.replace(/\s+/g, '_');
  const fileName = `${opId}_${safeTitle}.md`;
  const filePath = join(opsDir, fileName);

  const content = generateSchemeTemplate(opId, parsed.fullStageId, title);
  writeFileSync(filePath, content, 'utf-8');

  // 4. 同步到 flow.json（键用完整 stageId）
  syncToFlowJson(projectPath, parsed.fullStageId, opId, title);

  // 5. 返回 opId
  return opId;
}
```

> 注意：`generateSchemeTemplate(opId, parsed.fullStageId, title)` 的第二个参数（模板中 `- **阶段**：` 行）也由短名改为完整 stageId，保持与 flow.json 键一致。

`getScheme`（第 193-253 行）替换（点号解析 + 复用 listSchemes）：

```ts
export function getScheme(projectPath: string, opId: string): Scheme | null {
  let targetStage: string | null = null;
  let targetOpId: string;

  // 解析 opId：'{stage}.{op-XXX}' 或纯 '{op-XXX}'
  // 用正则锚定尾部 .op-NNN，避免完整 stageId 版本号中的点号干扰
  const match = opId.match(/^(.+)\.(op-\d+)$/);
  if (match) {
    targetStage = match[1];
    targetOpId = match[2];
  } else {
    targetStage = null;
    targetOpId = opId;
  }

  // 若指定阶段，解析出 stageDir 后遍历查找
  let targetStageDir: string | null = null;
  if (targetStage) {
    const parsed = parseStageId(targetStage);
    if (!parsed) {
      return null; // 阶段名非法
    }
    targetStageDir = parsed.stageDir;
  }

  for (const scheme of listSchemes(projectPath, targetStageDir ?? undefined)) {
    if (scheme.opId === targetOpId) {
      return scheme;
    }
  }
  return null;
}
```

`listSchemes`（第 259-316 行）替换（两层遍历，`filePath` 改多级）：

```ts
export function listSchemes(projectPath: string, stageName?: string): Scheme[] {
  const planDir = resolve(projectPath, '.openfeel', 'plan');
  const result: Scheme[] = [];

  if (!existsSync(planDir)) {
    return result;
  }

  // 解析目标 stageDir（若指定阶段过滤）
  let targetStageDir: string | null = null;
  if (stageName) {
    const parsed = parseStageId(stageName);
    if (!parsed) {
      return result; // 阶段名非法时返回空
    }
    targetStageDir = parsed.stageDir;
  }

  // 遍历 plan/{series}/stage-NN/ 两层目录
  const seriesEntries = readdirSync(planDir, { withFileTypes: true });
  for (const seriesEntry of seriesEntries) {
    if (!seriesEntry.isDirectory()) {
      continue;
    }
    const seriesDir = join(planDir, seriesEntry.name);

    let stageEntries: import('node:fs').Dirent[];
    try {
      stageEntries = readdirSync(seriesDir, { withFileTypes: true });
    } catch {
      continue;
    }

    for (const stageEntry of stageEntries) {
      if (!stageEntry.isDirectory()) {
        continue;
      }
      if (targetStageDir && stageEntry.name !== targetStageDir) {
        continue;
      }

      const opsDir = join(seriesDir, stageEntry.name, 'ops');
      if (!existsSync(opsDir)) {
        continue;
      }

      const opFiles = readdirSync(opsDir).filter((f) => f.endsWith('.md'));
      for (const fileName of opFiles) {
        const opId = extractOpId(fileName);
        if (!opId) {
          continue;
        }

        const filePath = join(opsDir, fileName);
        const content = readFileSync(filePath, 'utf-8');

        result.push({
          opId,
          stage: stageEntry.name,
          title: extractTitle(fileName),
          content,
          filePath: `.openfeel/plan/${seriesEntry.name}/${stageEntry.name}/ops/${fileName}`,
        });
      }
    }
  }

  // 按阶段名 + opId 排序
  result.sort((a, b) => {
    const cmp = a.stage.localeCompare(b.stage);
    if (cmp !== 0) {
      return cmp;
    }
    return a.opId.localeCompare(b.opId);
  });

  return result;
}
```

> 注：`getScheme` 现有测试 `getScheme(tmpDir, 'stage-01.op-001')` 中 `scheme.stage` 断言 `'stage-01'` 不变（stage 字段返回 stageDir），opId 参数用正则解析后 targetStageDir=`stage-01`，匹配正常。

### 步骤 3：改 `src/commands/plan.ts`（参数兼容说明）

- 第 24 行 `.argument('<name>', '阶段名（如 stage-01）')` → `.argument('<name>', '阶段 ID（如 stage-01 或 v1.0.0-stage-01）')`
- 第 60 行 `.argument('<stage>', '阶段名（如 stage-01）')` → `.argument('<stage>', '阶段 ID（如 stage-01 或 v1.0.0-stage-01）')`

> 遗留风险 6 评估结论：plan 命令 description/argument 现状为硬编码中文（未走 i18n，与 stage create 的 `t('stage.create.desc')` 不同），本 stage 仅更新硬编码描述文本说明两种形式，不新增 i18n 键（避免过度设计；若后续统一 plan 命令 i18n 化另立 stage）。

### 步骤 4：更新三个测试文件

#### `test/core/plan/stage.test.ts`

| 行 | 原 | 改 |
|----|----|----|
| 26 | `join(tmpDir, '.openfeel', 'stages', 'stage-01')` | `join(tmpDir, '.openfeel', 'plan', 'v1', 'stage-01')` |
| 35/46/54/75 | `join(tmpDir, '.openfeel', 'stages', 'stage-01', 'overview.md')` | `join(tmpDir, '.openfeel', 'plan', 'v1', 'stage-01', 'overview.md')` |
| 37 | `expect(content).toContain('# stage-01')` | `expect(content).toContain('# v1.0.0-stage-01')` |
| 63 | `join(tmpDir, '.openfeel', 'stages', 'stage-01', 'status.md')` | `join(tmpDir, '.openfeel', 'plan', 'v1', 'stage-01', 'status.md')` |
| 104 | `expect(stages[0].overview).toContain('# stage-01')` | `expect(stages[0].overview).toContain('# v1.0.0-stage-01')` |

> listStages 的 `names).toContain('stage-01')`（第 100-101 行）保持：name 字段仍返回 stageDir `stage-01`。

#### `test/core/plan/scheme.test.ts`

| 行 | 原 | 改 |
|----|----|----|
| 33 | `join(tmpDir, '.openfeel', 'stages', 'stage-01', 'ops', 'op-001_实现登录功能.md')` | `join(tmpDir, '.openfeel', 'plan', 'v1', 'stage-01', 'ops', 'op-001_实现登录功能.md')` |
| 49 | 同上（`op-001_配置数据库.md`） | 改 `plan/v1` |
| 80 | `join(tmpDir, '.openfeel', 'stages', 'stage-auto', 'ops', 'op-001_自动创建.md')` | `join(tmpDir, '.openfeel', 'plan', 'v1', 'stage-03', 'ops', 'op-001_自动创建.md')`，且第 76 行 `createScheme(tmpDir, 'stage-auto', ...)` → `createScheme(tmpDir, 'stage-03', ...)` |
| 89 | `join(tmpDir, '.openfeel', 'stages', 'stage-01', 'ops')` | `join(tmpDir, '.openfeel', 'plan', 'v1', 'stage-01', 'ops')` |
| 107 | `flowData.stages['stage-01'] = {...}` | `flowData.stages['v1.0.0-stage-01'] = {...}` |
| 130/134 | `verifyData.stages['stage-01']` | `verifyData.stages['v1.0.0-stage-01']` |
| 224 | `it('stages 目录不存在时应返回空数组', ...)` | `it('plan 目录不存在时应返回空数组', ...)` |

#### `test/commands/plan.test.ts`

| 行 | 原 | 改 |
|----|----|----|
| 63 | `join(tmpDir, '.openfeel', 'stages', 'stage-01')` | `join(tmpDir, '.openfeel', 'plan', 'v1', 'stage-01')` |
| 105 | `join(tmpDir, '.openfeel', 'stages', 'stage-01', 'ops')` | `join(tmpDir, '.openfeel', 'plan', 'v1', 'stage-01', 'ops')` |
| 149 | `flowData.stages['stage-01'] = {...}` | `flowData.stages['v1.0.0-stage-01'] = {...}` |
| 164 | `flowContent.stages['stage-01']?.ops` | `flowContent.stages['v1.0.0-stage-01']?.ops` |

> plan.test.ts 第 60/99/116/133 行 `safeParse(['plan', 'stage', 'add', 'stage-01'])` 短名参数**保持不改**（短名别名合法，决策 6 保留）。

## 产出文件

- `src/core/plan/stage.ts`
- `src/core/plan/scheme.ts`
- `src/commands/plan.ts`
- `test/core/plan/stage.test.ts`
- `test/core/plan/scheme.test.ts`
- `test/commands/plan.test.ts`

## 自测清单

- [ ] `addStage('stage-01')` 写入 `plan/v1/stage-01/`，flow.json 键 `v1.0.0-stage-01`
- [ ] `addStage('v1.0.0-stage-01')` 与短名结果一致（同目录同键）
- [ ] `addStage('foo')` 抛错（非法阶段名）
- [ ] overview.md/status.md 标题为 `v1.0.0-stage-01`（完整 stageId）
- [ ] `createScheme('stage-01', title)` 写入 `plan/v1/stage-01/ops/op-001_*.md`，flow.json 键 `v1.0.0-stage-01`，模板 `- **阶段**：v1.0.0-stage-01`
- [ ] `getScheme('stage-01.op-001')` 与 `getScheme('v1.0.0-stage-01.op-001')` 均能命中（点号解析正确）
- [ ] `listStages`/`listSchemes` 两层遍历正确，`path`/`filePath` 返回多级路径
- [ ] `npm test` 三个测试文件全绿

## 验证

- op-003 完成后执行 `npm test`，确认 stage.test.ts / scheme.test.ts / plan.test.ts 通过。
