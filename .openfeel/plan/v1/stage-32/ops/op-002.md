# op-002：改造 update 核心 merge 逻辑
- **阶段**：v1.0.0-stage-32
- **前置**：op-001（需要 `src/core/update-state.ts` 的 hashContent / loadUpdateState / saveUpdateState / createUpdateState / updateFileHash / markFileConflict API）
- **负责 Agent**：Executor
- **最多重试**：3
- **关联 REV**：REV-001（冲突路径非冲突文件 hash 更新——**blocking, high**）、REV-003（测试场景补充说明）

## 目标

改造 `src/core/update.ts` 的 `updateProject()` 函数，将 `writeIfChanged()` 替换为基于 hash 比对的三态逻辑（不变 / 安全覆盖 / 冲突拒绝），集成 `update-state.ts` 的读/写 API，扩展 `UpdateResult` 接口增加 `conflicts` 字段。

## 实施步骤

- [ ] **步骤 1**：导入 `update-state.ts` 的 API（`hashContent`, `loadUpdateState`, `saveUpdateState`, `createUpdateState`, `updateFileHash`, `markFileConflict`）
- [ ] **步骤 2**：扩展 `UpdateResult` 接口，新增 `conflicts: string[]` 字段
- [ ] **步骤 3**：[FIX] 新增 `writeWithMergeDetection()` 函数，替换原 `writeIfChanged()`——实现三态逻辑（不变 / 安全覆盖 / 冲突拒绝），**遵循 REV-001 规则**
- [ ] **步骤 4**：改造 `updateProject()` 主循环——加载 state、调用 `writeWithMergeDetection()`、更新 state、处理冲突
- [ ] **步骤 5**：[FIX] 实现 REV-001 核心逻辑：无论有无冲突，所有 `created`/`updated` 文件的 hash 都必须更新到 state
- [ ] **步骤 6**：移除旧的 `writeIfChanged()` 函数（已不再被使用）
- [ ] **步骤 7**：运行 TypeScript 编译检查和测试

## 产出文件

- `src/core/update.ts`（修改）

## 详细代码变更

### 1. 文件顶部新增 import

在 `src/core/update.ts` 现有 import 块末尾追加：

```typescript
// 新增：update_state.json hash 比对与冲突检测
import {
  hashContent,
  loadUpdateState,
  saveUpdateState,
  createUpdateState,
  updateFileHash,
  markFileConflict,
  type UpdateState,
} from './update-state.js';
```

### 2. 扩展 UpdateResult 接口

找到 `export interface UpdateResult`（约第 15 行），新增 `conflicts` 字段：

```typescript
export interface UpdateResult {
  created: string[];
  updated: string[];
  skipped: string[];
  conflicts: string[];  // 新增：冲突文件相对路径列表
}
```

### 3. [FIX] 新增 writeWithMergeDetection() —— 替换原 writeIfChanged()

在原 `writeIfChanged()` 函数位置（约第 1213 行），整块替换为：

```typescript
/**
 * 带冲突检测的写入函数（替代原 writeIfChanged）
 *
 * 三态逻辑：
 *  - 文件不存在 → created
 *  - 文件已存在 + hash 匹配 update_state 中记录 → 安全覆盖 → updated
 *  - 文件已存在 + hash 不匹配 → 拒绝覆盖 → conflicts
 *  - 文件已存在 + 不在 update_state 管理中 → 安全覆盖 → updated（降级）
 *  - 文件已存在 + update_state 不存在或损坏 → 安全覆盖 → updated（降级为旧行为）
 *
 * REV-001：此函数仅负责分类（created/updated/skipped/conflicts），
 * hash 的实际更新在 updateProject() 末尾统一处理，
 * 确保"冲突路径非冲突文件 hash 同步更新"。
 */
function writeWithMergeDetection(
  filePath: string,
  content: string,
  relativePath: string,
  updateState: UpdateState | null,
  created: string[],
  updated: string[],
  skipped: string[],
  conflicts: string[],
): void {
  if (!existsSync(filePath)) {
    // 文件不存在 → 新建
    mkdirSync(dirname(filePath), { recursive: true });
    writeFileSync(filePath, content, 'utf-8');
    created.push(relativePath);
    return;
  }

  const existing = readFileSync(filePath, 'utf-8');

  // 内容相同 → skip（快速路径：全文比对，避免 hash 计算）
  if (existing === content) {
    skipped.push(relativePath);
    return;
  }

  // 检查 update_state 中是否有该文件的记录
  const fileState = updateState?.files[relativePath];
  if (fileState) {
    // 有记录 → 比对 hash
    const currentHash = hashContent(existing);
    if (currentHash === fileState.hash) {
      // hash 一致 → 用户未修改 → 安全覆盖
      writeFileSync(filePath, content, 'utf-8');
      updated.push(relativePath);
      return;
    }
    // hash 不一致 → 用户已修改 → 冲突！
    conflicts.push(relativePath);
    return;
  }

  // 无记录（降级路径：update_state 损坏或旧版本）→ 安全覆盖
  writeFileSync(filePath, content, 'utf-8');
  updated.push(relativePath);
}
```

### 4. 改造 updateProject() 函数

在 `updateProject()` 函数体开头（`const created/updated/skipped` 声明之后，AGENTS.md 逻辑之前），新增 state 加载逻辑：

```typescript
// ── 增量更新：加载 update_state.json ──
const state = loadUpdateState(projectPath);
const conflicts: string[] = [];

// 用于在流程结束时统一更新 hash 所需的映射
// key: relativePath, value: 写入后的文件内容（从磁盘读取）
const hashUpdateMap: Map<string, string> = new Map();
```

将函数内部所有 `writeIfChanged(filePath, content, relPath, created, updated, skipped)` 替换为：

```typescript
writeWithMergeDetection(filePath, content, relPath, state, created, updated, skipped, conflicts);
```

**具体替换位置**（共 4 处）：
- `writeIfChanged(coreInstructionsPath, coreContent, '.opencode/instructions/core.md', created, updated, skipped)` → 对应替换
- `writeIfChanged(filePath, content, relPath, created, updated, skipped)`（Agent 循环内）→ 对应替换
- `writeIfChanged(filePath, content, relPath, created, updated, skipped)`（Skill 循环内）→ 对应替换
- `writeIfChanged(jsoncPath, newContent, relJsoncPath, created, updated, skipped)` → 对应替换

同时，在 `writeWithMergeDetection` 调用后（对于 created/updated 的文件），记录到 hashUpdateMap：

```typescript
// 每次 writeWithMergeDetection 调用后追加：
if (existsSync(filePath)) {
  hashUpdateMap.set(relPath, readFileSync(filePath, 'utf-8'));
}
```

### 5. [FIX] REV-001 核心逻辑：函数末尾 hash 更新

在 `updateProject()` 的 `return { created, updated, skipped }` 之前（约第 1647 行位置），整块替换为该逻辑：

```typescript
// ── REV-001：统一更新 update_state.json ──
// 规则：
// 1. 无论有无冲突，所有 created/updated 文件的 hash 都必须更新到 state
// 2. 有冲突时，额外标记冲突文件
// 3. 首次 update（state === null）→ 创建新 state

const newState: UpdateState = state ?? createUpdateState(projectPath, {});

// 更新所有非冲突文件的 hash（created 和 updated）
for (const relPath of [...created, ...updated]) {
  const absPath = resolve(projectPath, relPath);
  if (existsSync(absPath)) {
    const content = readFileSync(absPath, 'utf-8');
    updateFileHash(newState, relPath, content);
  }
}

// 标记冲突文件
for (const relPath of conflicts) {
  markFileConflict(newState, relPath);
}

// 更新时间戳
newState.last_update = new Date().toISOString();

// 持久化
saveUpdateState(projectPath, newState);

return { created, updated, skipped, conflicts };
```

> **REV-001 说明**：原计划伪代码将 hash 更新放在 `if (conflicts.length === 0)` 分支内，导致有冲突时 `created`/`updated` 文件的 hash 未同步到 `update_state.json`，下次 update 这些文件将被误报为"全量变更"。修正后，无论有无冲突，所有非冲突文件的 hash 都更新。冲突文件仅标记 status 为 conflict。

### 6. 删除旧函数

将原 `writeIfChanged()` 函数（约第 1213-1233 行）删除，替换为步骤 3 中的 `writeWithMergeDetection()`。

## 自测清单

- [ ] `UpdateResult` 类型包含 `conflicts: string[]` 字段
- [ ] 首次 update（无 `update_state.json`）：所有文件 → created/updated，`update_state.json` 被创建，所有 hash 正确记录
- [ ] 二次 update（无手动修改）：hash 一致 → 安全覆盖，`update_state.json` 中 hash 更新
- [ ] 二次 update（手动修改某文件）：被修改文件触发 `conflicts`，未被修改文件正常覆盖，**且其 hash 同步更新**（REV-001）
- [ ] REV-001 验证：修改文件 A 后 update，文件 B/C 正常覆盖 → 文件 B/C 的 hash 在 `update_state.json` 中已更新，下次 update 不再全量误报（REV-001）
- [ ] `update_state.json` 损坏/不存在：降级为全量覆盖（旧行为），不阻塞
- [ ] REV-003 场景 1：部分冲突解决后重跑 update → 已解决的文件 hash 更新为 clean，未解决的仍为 conflict
- [ ] REV-003 场景 2：空 state 文件（`{"version":"1.0","files":{}}`）→ Schema 校验通过，行为同首次 update
- [ ] REV-003 场景 3：混合场景（1 个冲突 + 2 个 created + 3 个 updated + 4 个 skipped）→ 各数组正确分类
- [ ] TypeScript 编译通过 && `npm test` 无回归（需更新 `UpdateResult` 相关的测试断言）

> REV-003 场景 1~3 需 Executor 在自测中人工验证，或补充单元测试用例（若测试框架支持）。

## 依赖版本

| 包名 | 版本 | 用途 | 选定依据 |
|------|------|------|----------|
| 无新增 | — | — | 所有依赖来自 op-001 产出和 Node.js 内置模块 |

> 知识库参考：kb/patterns.md #部署传播内容哈希比对模式（本 op 在此基础上引入 hash 比对替代全文比对）

## 方案修正记录（Executor 回写）

| 日期 | 修正项 | 说明 |
|------|--------|------|
| 2026-08-11 | 测试断言更新（REV-003） | 原测试「修改已有 agent 内容后应正确更新」期望旧行为（手动修改后覆盖恢复）。按三态逻辑改为「手动修改 → 冲突拒绝且不覆盖」，并新增 REV-001（冲突时非冲突文件 hash 同步更新）、REV-003 场景 1/2/3 共 4 个测试用例。 |
| 2026-08-11 | hashUpdateMap 简化 | 方案步骤 4 描述引入 `hashUpdateMap` 记录写入后内容，但步骤 5 的 REV-001 核心逻辑直接以 `resolve(projectPath, relPath)` 从磁盘读取内容，功能等效且更简洁。实际实现省略 hashUpdateMap，避免未使用变量。 |
| 2026-08-11 | 编译产物路径修正（承 op-001） | 无代码影响，详见 op-001 回写记录。 |
