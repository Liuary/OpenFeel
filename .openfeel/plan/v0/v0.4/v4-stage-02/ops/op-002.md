# op-002：KB 检索增强 Planner Agent 决策

- **阶段**：v4-stage-02
- **状态**：done
- **前置**：无
- **负责 Agent**：Executor
- **最多重试**：3

## 目标
在 Planner Agent 的 system prompt（`planner.md`）中注入 KB 检索步骤，使 Planner 在制定分期大纲和工作阶段前自动加载相关知识库条目，实现从"只写"到"读写"的转变。

## 实施步骤
- [x] 1. **读取本方案文件**：执行前先完整阅读 `op-002.md`，确认所有步骤和自测清单理解无误
- [x] 2. 读取当前 `C:\Code\AI\OpenFeel\.opencode\agents\planner.md`，确认当前结构（35 行）
- [x] 3. 在 `planner.md` 的「## 核心职责」与「## 产出格式」之间，插入以下「## KB 检索增强」章节：

  ```markdown
  ## KB 检索增强

  在制定任何计划前，必须先加载 `check-kb` 技能查阅项目知识库：

  1. **加载技能**：调用 `skill("check-kb")` 加载渐进式知识库查阅能力
  2. **检索相关条目**：根据计划涉及的技术领域和目标，匹配知识库中的相关条目：
     - 计划涉及架构决策或技术选型 → 优先查阅 `architecture.md`
     - 计划涉及代码规范或开发约定 → 优先查阅 `patterns.md`
     - 计划涉及已知坑位或历史问题 → 优先查阅 `troubleshooting.md`
     - 计划涉及环境或依赖变更 → 优先查阅 `setup.md`
  3. **引用条目**：在计划文档中引用相关知识库条目（如"参见 kb/architecture.md #Worktree 并行批次策略"），确保计划与项目已有架构决策一致
  4. **无相关条目时**：照常制定计划，但需在计划中注明"知识库中暂无相关记录"

  此步骤确保 Planner 在制定计划前吸收项目已有知识，避免计划与既有架构冲突。
  ```

- [x] 4. 插入位置精确：在原有「## 核心职责」块（第 19 行的 `4. **三层计划**...`）之后、「## 产出格式」（第 21 行）之前。插入后确保 markdown 章节层级正确（`##` 级别）
- [x] 5. 写完后校对行数，预计从 35 行增至约 56 行
- [x] 6. 确认新增内容中没有覆盖或破坏原有章节

## 产出文件
- `.opencode/agents/planner.md`（修改）

## 自测清单
- [x] `planner.md` 中存在「## KB 检索增强」章节
- [x] 该章节位于「## 核心职责」之后、「## 产出格式」之前（通过行号验证）
- [x] 章节内容包含「加载 `check-kb` 技能」的明确指令
- [x] 章节内容包含 4 个分类的查阅优先规则
- [x] 章节内容包含「无相关条目时照常制定计划」的兜底说明
- [x] 所有原有章节（核心职责 / 产出格式 / 与其他 Agent 的关系 / 模型选择）完整保留
- [x] 总行数约 54~60 行
- [x] 文件编码为 UTF-8

## 修正记录
| 次数 | 时间 | 问题 | 修正内容 |
|------|------|------|----------|
