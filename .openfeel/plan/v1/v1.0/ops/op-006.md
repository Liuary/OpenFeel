# op-006：CHANGELOG.md + 用户入门指南

- **阶段**：v1.0.0-stage-03（文档完善）
- **最多重试**：3

## 目标

在项目根目录创建 `CHANGELOG.md`（按 Keep a Changelog 规范，覆盖 v0.1.0 → v1.0.0，聚合成 v0.1/v0.2/v0.3/v0.4/v0.5/v1.0 六个大版本，每版本 3-5 条核心变更）；创建 `docs/GETTING_STARTED.md` 用户入门指南（简介/环境要求/安装/初始化/基本工作流/命令速查表 六节）。不修改任何源码。

## 实施步骤

- [x] 1. 收集数据源：`.openfeel/kb/index.md`（归档记录，含日期/版本/摘要）与 `.openfeel/plan/plan_log.md`（计划变更日志），提取版本时间线
- [x] 2. 创建 `CHANGELOG.md`：Keep a Changelog 结构（Added/Changed/Fixed），v0.1→v1.0 六个版本各 3-5 条核心变更，标题版本号格式 `## [x.y.0] - yyyy-mm-dd`
- [x] 3. 创建 `docs/GETTING_STARTED.md`：六节内容（简介/环境要求 Node≥20/安装 npm install -g openfeel/初始化 openfeel init/基本工作流/命令速查表），命令信息以 `openfeel --help` 实测输出为准
- [x] 4. 自测：`npm run build` 通过；两个文件格式正确、Markdown 链接均指向存在的文件

## 产出文件

- `CHANGELOG.md`（新建，项目根目录）
- `docs/GETTING_STARTED.md`（新建）

## 自测清单

- [x] `CHANGELOG.md` 存在，含 `# Changelog` 标题与 6 个版本节（v0.1~v1.0），每版本 3-5 条核心变更
- [x] `docs/GETTING_STARTED.md` 存在，含六节大纲内容（简介/环境要求/安装/初始化/基本工作流/命令速查表）
- [x] GETTING_STARTED 中引用的命令均为 `openfeel --help` 实测存在的命令
- [x] 两个文件内 Markdown 链接均指向存在的文件（无破损链接）
- [x] `npm run build` 通过

## 偏差记录

- 前置校验偏差：flow.json `pipeline.current.op` 为空（未注册 op-006），stage-03 phase=exec_running 合法，CLI `openfeel flow health --quick` 通过，Feel 明确指示执行，故继续（已记入自测报告）。
- CHANGELOG 版本日期为按数据源聚合标注：以各系列关键日期为准（如 v0.4.0 用 v0.4.0 全系列归档日 2026-07-05、v0.5.0 与 v1.0.0 用 2026-08-07），非逐次小版本精确日期。
- 声明产出与实际产出比对：一致（CHANGELOG.md + docs/GETTING_STARTED.md，无遗漏、无超范围）。
