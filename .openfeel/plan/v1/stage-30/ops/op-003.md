# op-003：新增 stage create 子命令 + i18n 键
- **阶段**：v1.0.0-stage-30
- **前置**：无（独立操作）
- **负责 Agent**：Executor
- **最多重试**：3
- **Bug 编号**：#3（缺少 CLI 阶段创建入口）
- **审查反馈**：REV-001（缺少 `help.stage.create` i18n 键）、REV-002（stage.ts 文件头注释更新）、REV-003（一致性已确认，仅记录）
- **参考**：plan.md #Bug #3 详细技术方案

## 目标

在 `stage` 命令组中新增 `stage create <stageId>` 子命令，让用户可以通过 CLI 创建新的工作阶段；同时补齐相关 i18n 键。

## 知识库参考

- kb/patterns.md #CLI 原子管理模式：Agent 通过 CLI 命令操作数据文件
- `flow stage add` 已存在（`src/commands/flow.ts` L26），行为对等，复用 `FlowManager.addStage()` 确保一致性

## 产出文件

- `src/commands/stage.ts`
- `src/core/i18n-data/zh-CN.ts`
- `src/core/i18n-data/en.ts`

## 实施步骤

### 步骤 1：更新 stage.ts 文件头注释（REV-002）

**文件**：`src/commands/stage.ts` L3

**当前**：
```
 * openfeel stage status|set|task — status.md 的 CLI 原子操作管理
```

**修改为**：
```
 * openfeel stage status|set|task|create — status.md 的 CLI 原子操作管理
```

- [x] 在 L3 的 `|set|task` 后追加 `|create`

### 步骤 2：为 stage.ts 添加 FlowManager 导入

**文件**：`src/commands/stage.ts`

**当前位置**：L16（`import { t, getCliLang }` 之后）

**新增行**（L17 位置）：
```typescript
import { FlowManager } from '../core/flow-manager.js';
```

- [x] 在 `src/core/i18n.js` 导入之后插入新导入
- [x] 使用与 `src/commands/flow.ts` L26 相同的导入路径格式
- [x] 导入路径使用 `.js` 扩展名（ESM 规范）

### 步骤 3：注册 stage create 子命令

**文件**：`src/commands/stage.ts`

**插入位置**：在 `registerStageCommand()` 函数中，`stage task` 命令注册（L378 `});`）之后、函数闭合 `}`（L379）之前。

**新增代码**：
```typescript

  // ═══ stage create <stageId> ═══
  stage
    .command('create')
    .description(t('stage.create.desc', getCliLang(process.cwd())))
    .argument('<stageId>', '阶段 ID（如 v1.0.0-stage-30）')
    .action((stageId: string) => {
      const projectPath = process.cwd();
      const lang = getCliLang(projectPath);
      const mgr = new FlowManager(projectPath);
      if (!mgr.isLoaded()) {
        console.error(t('common.errorNoInit', lang));
        process.exit(1);
      }
      try {
        mgr.addStage(stageId);
        mgr.save();
        console.log(t('stage.create.addedTmpl', lang, { stage: stageId }));
      } catch (err: unknown) {
        const msg = err instanceof Error ? err.message : String(err);
        console.error(t('common.errorTmpl', lang, { msg }));
        process.exit(1);
      }
    });
```

- [x] 命令名 `create`，参数 `<stageId>`
- [x] 使用 `FlowManager.addStage()` 复用现有逻辑（与 `flow stage add` 行为一致）
- [x] 错误处理：未 init（`isLoaded()` 返回 false）时报错退出
- [x] 阶段已存在时 `addStage()` 抛出异常，被 catch 块统一处理
- [x] 创建后调用 `mgr.save()` 持久化到 flow.json

### 步骤 4：新增 i18n 键 — 功能域

#### 4a. zh-CN.ts — stage 域

**文件**：`src/core/i18n-data/zh-CN.ts`

**插入位置**：在 stage 域中 `'task.actionLabelTmpl'`（L324）之后、`};`（L325）之前。

**新增代码**：
```typescript
  'create.desc':                 { key: 'stage.create.desc',                 zh: '创建新的工作阶段',                        en: '' },
  'create.addedTmpl':            { key: 'stage.create.addedTmpl',            zh: '✓ 已创建阶段: {stage} → plan_pending',    en: '' },
```

- [x] 缩进与 domain 内其他条目对齐（2 空格）
- [x] key 命名：`stage.create.desc`、`stage.create.addedTmpl`

#### 4b. en.ts — stage 域

**文件**：`src/core/i18n-data/en.ts`

**插入位置**：在 stage 域中 `'task.actionLabelTmpl'`（L306）之后、`};`（L307）之前。

**新增代码**：
```typescript
  'create.desc':                 { key: 'stage.create.desc',                 zh: '', en: 'Create a new work stage' },
  'create.addedTmpl':            { key: 'stage.create.addedTmpl',            zh: '', en: '✓ Stage created: {stage} → plan_pending' },
```

### 步骤 5：新增 i18n 键 — help 域（REV-001）

#### 5a. zh-CN.ts — help 域

**文件**：`src/core/i18n-data/zh-CN.ts`

**插入位置**：在 help 域中 `'stage.task.undone'`（L495）之后、空行（L496）之前。

**新增代码**：
```typescript
  'stage.create':          { key: 'help.stage.create',          zh: '创建新的工作阶段（自动写入 flow.json）', en: '' },
```

#### 5b. en.ts — help 域

**文件**：`src/core/i18n-data/en.ts`

**插入位置**：在 help 域中 `'stage.task.undone'`（L469）之后、空行（L470）之前。

**新增代码**：
```typescript
  'stage.create':          { key: 'help.stage.create',          zh: '', en: 'Create a new work stage (writes to flow.json)' },
```

### 执行顺序建议

建议按 1→2→3→4→5 顺序执行，因为代码修改（1-3）和 i18n（4-5）可分别独立进行，但 i18n 键在代码中被引用，建议先完成 i18n 再验证编译。

## 自测清单

- [ ] **TypeScript 编译**：`npm run build` 零错误
- [ ] **现有测试无回归**：`npm test` 全部通过（399/399）
- [ ] **stage create 基本功能**：
  - `node dist/index.js stage create v1.0.0-stage-30-test` → 输出 "✓ 已创建阶段: v1.0.0-stage-30-test → plan_pending"
  - 检查 flow.json，确认 `stages["v1.0.0-stage-30-test"]` 存在，phase 为 `plan_pending`
- [ ] **已存在阶段报错**：
  - 再次执行 `node dist/index.js stage create v1.0.0-stage-30-test` → 报错 "Stage already exists"
- [ ] **未 init 项目报错**：
  - 在非 OpenFeel 项目目录执行 `node dist/index.js stage create test` → 报错未初始化
- [ ] **帮助信息**：`node dist/index.js help stage` 显示 `create` 子命令
- [ ] **i18n 中英文切换**：在有 `lang: "en"` 的 `.openfeel/.info.json` 项目中，输出为英文

### REV 专项验证

- [ ] **REV-001**：`help.stage.create` 键在 zh-CN.ts 和 en.ts 中均已定义，帮助输出包含 `stage create` 说明
- [ ] **REV-002**：stage.ts 文件头 L3 注释已包含 `|create`
- [ ] **REV-003**：确认 `stage create` 与 `flow stage add` 行为一致（均调用 `FlowManager.addStage()`）

## 风险说明

| 风险 | 概率 | 缓解 |
|------|------|------|
| `stage.ts` 新增 `FlowManager` 导入增加耦合 | 极低 | `FlowManager` 不导入 stage.ts，单向依赖安全；`flow.ts` 已有相同导入模式 |
| `stage create` 与 `flow stage add` 行为差异 | 极低 | 两者均调用 `FlowManager.addStage()`，预期行为完全一致 |

## 修正记录

| 类型 | 时间 | 操作者 | 说明 |
|------|------|--------|------|
| 自检 | 2026-08-09 | Executor | 方案一致性检查：声明产出 3 文件（stage.ts / zh-CN.ts / en.ts）与实际产出一致，无偏差。REV-001（help.stage.create 键）、REV-002（文件头 |create）、REV-003（与 flow stage add 行为一致）均验证通过 |
