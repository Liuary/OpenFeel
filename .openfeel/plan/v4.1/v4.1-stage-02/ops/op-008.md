# op-008：新增事务官 Agent — utility.md 创建 + update.ts 注册

- **阶段**：v4.1-stage-02
- **前置**：op-001（feel.md 的事务官路由表已就绪，但本 op 为新增文件，可与 Batch 1 串行推进）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

创建 `.opencode/agents/utility.md` 事务官 Agent 文件，并将其注册到 `src/core/update.ts` 的 `AGENT_DEFINITIONS` 中，使事务官成为第 8 个可用 Agent。

> 参见 kb/architecture.md #15→7 Agent 精简体系 — 新增事务官是在现有 7 Agent 体系上的扩展，遵循相同的 Agent 定义格式和注册机制。

## 实施步骤

### 第一部分：创建 utility.md

- [x] 1. 读取现有任一 Agent 文件（如 `executor.md`）的 frontmatter 格式作为模板参考
- [x] 2. 创建 `.opencode/agents/utility.md`，按以下结构编写：

**frontmatter**：
```yaml
---
description: 事务官 Agent，快速模型，负责文件操作、格式转换、构建测试等机械性辅助任务。
mode: subagent
model: deepseek/deepseek-v4-flash
color: "#8B9DC3"
permission:
  bash: "allow"
  read: "allow"
  glob: "allow"
  grep: "allow"
  write: "allow"
---
```

**正文内容**（~40 行）：
- `## 核心职责`：文件增删复制移动、格式转换（JSON↔YAML↔Markdown）、编码检查（UTF-8/换行符）、构建测试（`npm run build` / `npm test`）、批量文本替换（限定非 .ts 业务逻辑文件）
- `## 调起方式`：Feel 通过 `task` 工具调起，传入简单文本指令（无需 Schemer→Executor 完整流水线）；传入格式含 `task_type: utility` 和具体操作描述
- `## 明确禁止`（6 项）：
  1. 不参与设计决策
  2. 不修改 `.ts` 业务逻辑源码
  3. 不修改 Agent prompt 文件（`.opencode/agents/*.md`）
  4. 不调用其他 Agent
  5. 不操作流水线状态（flow.json / status.md）
  6. 超出职责范围的任务立即回退 Feel
- `## 与 Executor 分工`：事务官处理机械性文件操作（无判断逻辑）；需要理解业务逻辑上下文时由 Feel 升级派发给 Executor 并标注 `type: utility`
- `## 模型选择`：快速模型驱动，机械性操作无需深度推理

### 第二部分：注册到 update.ts

- [x] 3. 读取 `src/core/update.ts` 中 `AGENT_DEFINITIONS` 区域的完整内容（`AUTO-GENERATED-BEGIN` 到 `AUTO-GENERATED-END`）
- [x] 4. 在 `AGENT_DEFINITIONS` 的 `Record<string, string>` 中按字母序插入 `utility` 条目（位于 `schemer` 和 `reviewer` 之间，因为当前无 `schemer` 条目，实际按 `archiver → executor → feel → feel-tester → planner → reviewer → schemer → utility` 排序，即追加在 `schemer` 之后）
  - 内容为 utility.md 的完整正文（含 frontmatter，使用模板字符串 `` ` `` 包裹，内部 backtick 需转义为 `` \` ``）
  - 注意：现有 `AGENT_DEFINITIONS` 中的条目顺序为 archiver → executor → feel → feel-tester → 无 planner/schemer/reviewer（需确认），检查实际顺序后按字母序插入
- [x] 5. 确认 `AUTO-GENERATED-BEGIN: AGENT_DEFINITIONS` 和 `AUTO-GENERATED-END: AGENT_DEFINITIONS` 标记未被破坏

### 第三部分：验证

- [x] 6. 运行 `npm run build` 确认构建成功，`templates.ts` 中应包含 utility 模板
- [x] 7. 运行 `npm test` 确认无回归

## 产出文件

- `.opencode/agents/utility.md`（新增）
- `src/core/update.ts`（修改 — AGENT_DEFINITIONS 中新增 utility 条目）

## 自测清单

### utility.md 文件检查
- [x] `.opencode/agents/utility.md` 文件存在
- [x] frontmatter 包含 `mode: subagent`、`model: deepseek/deepseek-v4-flash`、`color`、`permission`
- [x] permission 包含 `write: "allow"`（事务官需要写文件）
- [x] 正文包含 `## 核心职责`、`## 调起方式`、`## 明确禁止`（6 项）、`## 与 Executor 分工`、`## 模型选择`
- [x] `## 明确禁止` 中能 grep 到"不修改 .ts 业务逻辑源码"
- [x] `## 明确禁止` 中能 grep 到"不操作流水线状态"
- [x] 文件编码 UTF-8

### update.ts 注册检查
- [x] `src/core/update.ts` 中 `AGENT_DEFINITIONS` 包含 `utility` 键
- [x] utility 的模板字符串内容与 utility.md 一致（逐段对比）
- [x] `AUTO-GENERATED` 标记完整（BEGIN 和 END 都存在且配对）
- [x] 其他 Agent 条目的顺序和内容未被修改

### 构建和测试
- [x] `npm run build` 成功退出
- [x] `npm test` 全部通过（225+/227，无新增失败）

## 修正记录

| 次数 | 时间 | 问题 | 修正内容 |
|------|------|------|----------|
| 自检 | 2026-07-07 00:43 | 方案一致性检查 | 声明与产出一致，无偏差。伴随修改：`test/core/update.test.ts` 中 Agent 数量断言 7→8（op-008 的合理副作用），skipped 断言 17→18
