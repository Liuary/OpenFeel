# op-007：版本号同步 1.0.9（package.json + config.yaml + config.ts + CHANGELOG）

- **阶段**：v1.0.0-stage-34
- **前置**：op-006（soft，stage-34 全量回归已通过，本次为归档后版本号收尾）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

版本 1.0.8 → 1.0.9 全链路同步：package.json version、config.yaml meta.version、config.ts 双语言模板常量、CHANGELOG 新增 1.0.9 条目，触发 CI npm publish（由 Feel 收尾统一提交）。

> 与 stage-33 op-005（1.0.7 → 1.0.8）完全同类；本次范围收窄——不涉及 AGENTS.md / agents-md 版本声明（stage-33 已改为「v1.0.x 已发布」措辞，本次确认即可）。

## 实施步骤

- [x] 改动 1：`package.json` `"version": "1.0.8"` → `"1.0.9"`
- [x] 改动 2：`.openfeel/config.yaml` `meta.version` `  version: 1.0.8` → `  version: 1.0.9`（保留缩进与注释，改后确认仍为 UTF-8 无 BOM）
- [x] 改动 3：`src/core/config.ts` `CONFIG_TEMPLATE_ZH`（约 304 行）与 `CONFIG_TEMPLATE_EN`（约 361 行）`meta:` 块下 `  version: 1.0.8` → `  version: 1.0.9`（两处字面量，defaults 块插值不动）
- [x] 改动 4：`CHANGELOG.md` 在 `## [1.0.8] - 2026-08-15` 前插入 `## [1.0.9] - 2026-08-15` 条目（Keep a Changelog，Added/Changed 分类，概括 stage-34「plan 目录多级化与路径统一」主题）
- [x] 确认 AGENTS.md 版本声明为「v1.0.x 已发布」措辞，无需修改
- [x] `npm run build && npm test` 全量通过

## 产出文件

- `package.json`
- `.openfeel/config.yaml`
- `src/core/config.ts`
- `CHANGELOG.md`

## 自测清单

- [x] package.json `version` 为 `1.0.9`
- [x] config.yaml `meta.version` 为 `1.0.9`，且文件仍为 UTF-8 无 BOM（首字节非 EF BB BF）
- [x] config.ts CONFIG_TEMPLATE_ZH/EN 的 `version: 1.0.9` 各 1 处（defaults 块插值未动）
- [x] CHANGELOG `## [1.0.9] - 2026-08-15` 条目已加，含 Added/Changed 分类，概括 stage-34「plan 目录多级化与路径统一」主题
- [x] AGENTS.md 版本声明未改动（确认「v1.0.x 已发布」措辞存在）
- [x] `npm run build && npm test` 全量通过
