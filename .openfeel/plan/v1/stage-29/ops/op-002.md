# op-002：init 集成 — OpenCode 部署 + AGENTS.md 修复 + 测试 + 重启提醒

- **阶段**：v1.0.0-stage-29-02
- **前置**：[hard] op-001 — 模板数据源化和构建管线必须先完成（`template-loader.ts` 新增导出函数可用）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

在 `init` 流程中新增 OpenCode 部署确认步骤、实现 opencode 适配器部署逻辑，修复 AGENTS.md `{项目名称}` 占位符替换，增加重启提醒，编写自动化测试，并同步 `update.ts` 的重启提醒。

> **知识库参考**：kb/patterns.md #双语 CLI 交互模式、kb/patterns.md #i18n 域扩展与 config 命令组模式、kb/patterns.md #部署传播内容哈希比对模式

## 实施步骤

### 一、修复 AGENTS.md 项目名称占位符

- [ ] **1.1 [FIX]** 在 `src/core/init.ts` 的 `initProject()` 函数中，步骤 8（当前约第 244 行 `const agentsMdContent = loadTemplate(selectedLang, 'agents-md');`）之后，`writeTemplateIfMissing()` 之前，新增变量替换行：
  ```typescript
  const projectName = path.basename(projectPath);
  const replacedContent = agentsMdContent.replace(/\{项目名称\}/g, projectName);
  ```
  然后将 `writeTemplateIfMissing(agentsMdPath, agentsMdContent)` 改为：
  ```typescript
  const agentsMdResult = writeTemplateIfMissing(agentsMdPath, replacedContent);
  ```

### 二、新增 OpenCode 部署确认交互

- [ ] **2.1 [FIX]** 在 `init.ts` 中新增 `promptOpencodeDeploy()` 函数（位置：`promptLanguage()` 函数之后、`writeLang()` 函数之前）：
  ```typescript
  /**
   * 提示用户是否部署 OpenCode 平台适配器
   * 交互模式：提问 Y/n（默认 Y），非交互模式：静默返回 false
   */
  async function promptOpencodeDeploy(lang: 'zh-CN' | 'en'): Promise<boolean> {
    if (!process.stdout.isTTY) {
      return false; // 非交互模式，跳过
    }
    return new Promise((resolve) => {
      const rl = readline.createInterface({
        input: process.stdin,
        output: process.stdout,
      });
      const prompt = lang === 'en'
        ? 'Deploy OpenCode platform adapter? [Y/n] '
        : '是否部署 OpenCode 平台适配器？[Y/n] ';
      rl.question(prompt, (answer) => {
        rl.close();
        const trimmed = answer.trim().toLowerCase();
        // 默认 Y（仅显式 n/no 时拒绝）
        resolve(trimmed !== 'n' && trimmed !== 'no');
      });
    });
  }
  ```
  > 对应 REV-004：重命名自原 `promptPlatform()` 并简化为单平台布尔返回。

### 三、新增 OpenCode 部署逻辑

- [ ] **3.1 [FIX]** 在 `init.ts` 顶部 import 区新增导入：
  ```typescript
  import { loadOpencodeAgentTemplate, loadOpencodeSkillTemplate, loadOpencodeConfigTemplate, listOpencodeAgentIds } from './template-loader.js';
  import { basename } from 'node:path';
  ```

- [ ] **3.2 [FIX]** 新增 `deployOpencode()` 函数（位置：`writeTemplateIfMissing()` 函数之后、`initProject()` 函数之前）：
  ```typescript
  /** opencode 部署结果 */
  interface OpencodeDeployResult {
    created: number;
    skipped: number;
  }

  /**
   * 部署 OpenCode 平台适配器到目标项目
   * 所有写入遵循"已存在不覆盖"原则
   * ⚠️ 不部署 package.json（REV-001）
   */
  function deployOpencode(projectPath: string, lang: 'zh-CN' | 'en'): OpencodeDeployResult {
    let created = 0;
    let skipped = 0;

    const track = (result: { created: boolean }) => {
      if (result.created) created++; else skipped++;
    };

    // 1. 部署 Agent 定义（9 个）
    const agentIds = listOpencodeAgentIds(lang);
    for (const agentId of agentIds) {
      const content = loadOpencodeAgentTemplate(lang, agentId);
      const filePath = resolve(projectPath, '.opencode', 'agents', `${agentId}.md`);
      track(writeTemplateIfMissing(filePath, content));
    }

    // 2. 部署 Skill 定义（14 个）
    const skillNames = Object.keys(OPENCODE_SKILL_DEFINITIONS); // 需从 template-loader 导出
    // 方案：template-loader.ts 需新增 listOpencodeSkillNames() 导出，或 deployOpencode 直接 import OPENCODE_SKILL_DEFINITIONS
    // 推荐方案：新增 listOpencodeSkillNames() 导出函数，避免直接访问内部常量
    for (const skillName of skillNames) {
      const content = loadOpencodeSkillTemplate(skillName);
      const skillDir = resolve(projectPath, '.opencode', 'skills', skillName);
      const filePath = join(skillDir, 'SKILL.md');
      track(writeTemplateIfMissing(filePath, content));
    }

    // 3. 部署 instructions/core.md
    track(writeTemplateIfMissing(
      resolve(projectPath, '.opencode', 'instructions', 'core.md'),
      loadOpencodeConfigTemplate(lang, 'instructions'),
    ));

    // 4. 部署 opencode.jsonc（替换 {项目名称}）
    const configContent = loadOpencodeConfigTemplate(lang, 'opencode_jsonc')
      .replace(/\{项目名称\}/g, basename(projectPath));
    track(writeTemplateIfMissing(
      resolve(projectPath, 'opencode.jsonc'),
      configContent,
    ));

    // 5. 部署 ADAPTER.{lang}.md（文件名保留语言后缀）
    const adapterSuffix = lang === 'en' ? 'en' : 'zh-CN';
    track(writeTemplateIfMissing(
      resolve(projectPath, '.opencode', `ADAPTER.${adapterSuffix}.md`),
      loadOpencodeConfigTemplate(lang, 'adapter'),
    ));

    // 6. 部署 .gitignore
    track(writeTemplateIfMissing(
      resolve(projectPath, '.opencode', '.gitignore'),
      loadOpencodeConfigTemplate(lang, 'gitignore'),
    ));

    // ⚠️ 不部署 package.json（REV-001）

    return { created, skipped };
  }
  ```
  > **困难点**：`deployOpencode()` 需要访问 `OPENCODE_SKILL_DEFINITIONS` 获取完整 skill 名列表。若直接 import 内部常量，违反"template-loader 内部常量不应在外部直接使用"的原则。**方案**：在 op-001 的 template-loader.ts 中额外新增 `listOpencodeSkillNames(): string[]` 导出函数，返回 `Object.keys(OPENCODE_SKILL_DEFINITIONS)`。

### 四、更新 initProject() 主流程

- [ ] **4.1 [FIX]** 修改 `initProject()` 函数签名和返回值类型——`InitResult` 中新增 `opencode` 字段：
  ```typescript
  export interface InitResult {
    created: string[];
    updated: string[];
    opencode?: OpencodeDeployResult; // 如部署了 opencode 则有此字段
  }
  ```

- [ ] **4.2 [FIX]** 在 `initProject()` 中，在步骤 1a（语言选择）之后、步骤 2（写入 config）之前，插入：
  ```typescript
  // 1b. OpenCode 部署确认
  const deployOpencodeFlag = await promptOpencodeDeploy(selectedLang);
  ```

- [ ] **4.3 [FIX]** 在步骤 8（AGENTS.md 生成）之前、步骤 7（kb/index.md）之后，插入：
  ```typescript
  // 7b. 部署 opencode 适配器（如用户确认）
  let opencodeResult: OpencodeDeployResult | undefined;
  if (deployOpencodeFlag) {
    opencodeResult = deployOpencode(projectPath, selectedLang);
  }
  ```

- [ ] **4.4 [FIX]** 更新 `initProject()` 的 return 语句，在 `return { created, updated }` 中新增字段：
  ```typescript
  return { created, updated, opencode: opencodeResult };
  ```

- [ ] **4.5 [FIX]** 在 `return` 之前、末尾，新增重启提醒输出：
  ```typescript
  // 10. 重启提醒（仅在 opencode 首次部署时，且为交互模式）
  if (opencodeResult && opencodeResult.created > 0 && process.stdout.isTTY) {
    console.log(
      selectedLang === 'en'
        ? 'opencode configuration deployed. Please restart opencode to load the new configuration.'
        : 'opencode 配置已部署，请重启 opencode 以加载新配置。'
    );
  }
  ```

### 五、i18n 字符串（可选）

- [ ] **5.1** （可选）如需 i18n 化，在 `src/core/i18n-data/zh-CN.ts` 的 `init` 域下新增键：`init.opencodePrompt`、`init.opencodeDeploying`、`init.opencodeSkipped`、`init.opencodeRestart`，并在 `en.ts` 中对称添加。若选择硬编码（推荐——部署提示简单直白），则跳过此步骤。

### 六、新增测试用例（REV-002）

- [ ] **6.1 [FIX]** 在 `test/core/init.test.ts` 中新增 `describe('initProject — opencode deployment')` 块，包含以下测试：

  **测试 1**：`{项目名称}` 变量替换正向验证
  ```typescript
  it('应替换 AGENTS.md 中的 {项目名称} 占位符为目录名', async () => {
    await initProject(tmpDir, 'zh-CN');
    const content = readFileSync(join(tmpDir, 'AGENTS.md'), 'utf-8');
    const dirName = basename(tmpDir);
    expect(content).not.toContain('{项目名称}');
    expect(content).toContain(dirName);
  });
  ```

  **测试 2**：`deployOpencode()` 创建/跳过计数（需模拟 TTY 或直接调用内部函数）
  ```typescript
  // 注意：deployOpencode() 是内部函数，不直接导出。此测试需通过集成测试方式验证：
  // 先 init 一个新项目 → 检查 .opencode/ 下文件 → 再 init 同一目录 → 验证 skipped
  it('首次 init 应创建完整的 opencode 适配器文件', async () => {
    // 此处需要能将 promptOpencodeDeploy 设为 true 的测试机制
    // 方案：通过环境变量或模块内部标志控制测试模式，或导出 deployOpencode 供测试
    // [设计决策]：导出 deployOpencode 和 promptOpencodeDeploy 为测试可见
  });
  ```
  > **困难点**：`promptOpencodeDeploy()` 是内部函数且依赖 `stdout.isTTY` 和 `readline`，单元测试中无法直接模拟用户输入。**方案**：在 `init.ts` 中新增导出 `__test__` 命名空间（或简单导出内部函数），或使用环境变量 `OPENFEEL_TEST_DEPLOY_OPENCODE=1` 控制。**推荐**：简单导出 `deployOpencode` 函数，测试中直接调用它而不依赖 `initProject()` 的交互流程。

  **测试 3**：非交互模式跳过 OpenCode 部署
  ```typescript
  it('非交互模式应跳过 opencode 部署', async () => {
    // 方案：利用现有测试环境（vitest 运行时 stdout.isTTY 可能为 false），验证 initProject 后 .opencode/ 目录不存在
    const result = await initProject(tmpDir, 'zh-CN');
    const opencodeDir = join(tmpDir, '.opencode');
    // 在 CI/CD 环境中 stdout.isTTY 为 false，因此不应创建 .opencode/
    expect(existsSync(opencodeDir)).toBe(false);
    // 验证 opencode 字段为 undefined
    expect(result.opencode).toBeUndefined();
  });
  ```

  **测试 4**：`opencode.jsonc` 中 `{项目名称}` 被正确替换
  ```typescript
  it('opencode.jsonc 中 {项目名称} 应被替换为目录名', async () => {
    // 仅在部署了 opencode 时执行
    const opencodeJsonPath = join(tmpDir, 'opencode.jsonc');
    if (existsSync(opencodeJsonPath)) {
      const content = readFileSync(opencodeJsonPath, 'utf-8');
      expect(content).not.toContain('{项目名称}');
      expect(content).toContain(basename(tmpDir));
    }
  });
  ```

### 七、重启提醒

- [ ] **7.1 [FIX]** 上述步骤 4.5 已实现 init 后的重启提醒。确认：仅在 `created > 0` 且交互模式时输出。

- [ ] **7.2 [FIX]** 在 `src/core/update.ts` 的 `updateProject()` 函数中，在返回结果之前新增重启提醒逻辑：
  - 判断条件：`updated` 数组中包含以 `.opencode/agents/` 为前缀的条目
  - 交互模式：`console.log(...)` 输出提醒
  - 非交互模式：静默跳过
  - 实现参考：
  ```typescript
  // 在 updateProject 末尾、return 之前
  if (updated.some(f => f.startsWith('.opencode/agents/')) && process.stdout.isTTY) {
    const lang = getCliLang(projectPath);
    console.log(
      lang === 'en'
        ? 'opencode agent configuration updated. Please restart opencode to load the new configuration.'
        : 'opencode agent 配置已更新，请重启 opencode 以加载新配置。'
    );
  }
  ```

### 八、验证

- [ ] **8.1** 运行 `npm run build` 确认编译通过
- [ ] **8.2** 运行 `npm test` 确认无回归（298/298 + 新增测试全通过）

## 产出文件

| 文件 | 操作 | 说明 |
|------|:--:|------|
| `src/core/init.ts` | MODIFY | AGENTS.md 变量替换 + `promptOpencodeDeploy()` + `deployOpencode()` + `initProject()` 流程重组 + 重启提醒 |
| `src/core/update.ts` | MODIFY | 新增 update 后重启提醒 |
| `src/core/template-loader.ts` | MODIFY | 新增 `listOpencodeSkillNames()` 导出（配合 deployOpencode 使用） |
| `test/core/init.test.ts` | MODIFY | 新增 4 个测试用例 |
| `src/core/i18n-data/zh-CN.ts` | MAYBE | 新增 opencode 相关 i18n 键（可选） |
| `src/core/i18n-data/en.ts` | MAYBE | 新增 opencode 相关 i18n 键英文版（可选） |

## 自测清单

- [ ] `npm run build` 成功（退出码 0）
- [ ] `npm test` 无回归（现有 298/298 全通过 + 新增测试全通过）
- [ ] **新增测试用例逐个通过：**
  - [ ] `{项目名称}` 变量替换验证：`AGENTS.md` 中不含 `{项目名称}` 字面量
  - [ ] `deployOpencode()` 计数验证：首次创建数正确（约 28 文件），重复 init 返回 skipped
  - [ ] 非交互模式跳过：CI 环境 `.opencode/` 目录不存在且 `result.opencode` 为 undefined
  - [ ] `opencode.jsonc` 变量替换：不含 `{项目名称}`，含实际目录名
- [ ] **交互模式手工验证**（本地执行 `openfeel init /tmp/test-project`）：
  - [ ] 确认部署 → `AGENTS.md` 中项目名已替换
  - [ ] `opencode.jsonc` 中项目名已替换
  - [ ] `.opencode/agents/` 下有 9 个 Agent .md 文件
  - [ ] `.opencode/skills/` 下有 14 个 Skill 目录
  - [ ] `.opencode/instructions/core.md` 存在
  - [ ] `.opencode/ADAPTER.zh-CN.md`（或 en）存在
  - [ ] `.opencode/.gitignore` 存在
  - [ ] `.opencode/` 下 **无 `package.json`**（REV-001 验证）
  - [ ] **重启提醒已输出**
- [ ] **拒绝部署验证**：重新 init，选 n → `.opencode/` 不存在，无重启提醒
- [ ] **重复 init 验证**：对已有项目再 init → "已存在不覆盖"，无重启提醒（`opencode.created === 0`）
- [ ] **`openfeel update` 验证**：
  - [ ] 在已有项目上正常工作（不退化）
  - [ ] 修改 agent 配置后输出重启提醒
- [ ] **init → update 顺序无冲突**（REV-003 验收）：先 init 部署，再 update，无异常

## 困难点与设计决策

| 困难点 | 决策 | 理由 |
|--------|------|------|
| 单元测试中如何触发 `promptOpencodeDeploy()` 返回 true | 导出 `deployOpencode` 函数供测试直接调用，不依赖交互流程 | 避免 mock readline/prompt，测试更简洁可靠 |
| `deployOpencode()` 需要 skill 列表完整遍历 | template-loader.ts 新增 `listOpencodeSkillNames()` 导出 | 不直接 import 内部常量，保持封装性 |
| i18n 键是否必要 | 硬编码中英双语字符串（可选不做 i18n） | 提示文本极简，i18n 化反而增加间接层 |
