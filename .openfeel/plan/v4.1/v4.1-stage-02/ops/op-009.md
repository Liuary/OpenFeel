# op-009：AGENTS.md 更新 — Agent 职责边界表纳入事务官

- **阶段**：v4.1-stage-02
- **前置**：op-008（需 utility.md 已创建，以便在边界表中引用）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

更新 `AGENTS.md` 中的「Agent 职责边界表」（`## 跨 Agent 工具使用约束` → 第 3 条），将新增的 事务官 纳入 8 Agent 体系，明确其职责边界和与其他 Agent 的协作关系。

## 实施步骤

- [ ] 1. 读取当前 `AGENTS.md` 完整内容（103 行）
- [ ] 2. 定位 `## 跨 Agent 工具使用约束` 下的第 3 条「职责边界」（约第 75–82 行）
- [ ] 3. 在现有职责边界列表中新增 事务官 条目（按字母序或逻辑序插入，建议插在 Feel Tester 之后、Archiver 之前）：
  ```markdown
     - 事务官 执行文件机械操作，不参与设计决策
  ```
- [ ] 4. 在职责边界表之后（或 Feel 调度约束之前）新增 `## 8 Agent 体系总览` 小节，以表格形式列出 8 个 Agent 的名称、角色、驱动模型：
  ```markdown
  ### 8 Agent 体系总览

  | Agent | 角色 | 驱动模型 | 调起方式 |
  |-------|------|----------|----------|
  | Feel | 总统领 | 主力推理模型 | primary |
  | Planner | 计划官 | 推理模型 | subagent |
  | Schemer | 方案官 | 主力推理模型 | subagent |
  | Executor | 执行官 | 快速模型 (Flash) | subagent |
  | Reviewer | 审查官 | 异种推理模型 (GLM) | subagent |
  | Feel Tester | 测试官 | 推理模型 | subagent |
  | 事务官 | 事务官 | 快速模型 (Flash) | subagent |
  | Archiver | 归档官 | 推理模型 | subagent |
  ```
- [ ] 5. 更新第 4 条「Feel 调度约束」中的下游 Agent 列表，将 `Planner / Schemer / Executor / Reviewer / Feel Tester / Archiver` 扩展为 `Planner / Schemer / Executor / Reviewer / Feel Tester / 事务官 / Archiver`
- [ ] 6. 检查文件行数变化：`wc -l AGENTS.md` 确认在 110–120 行左右（原有 103 行 + 新增 ~10–15 行）

## 产出文件

- `AGENTS.md`

## 修正记录

| 次数 | 时间 | 问题 | 修正内容 |
|------|------|------|----------|
| 自检 | 2026-07-07 | 方案一致性检查 | 声明与产出一致，无偏差 |

## 自测清单

- [ ] `AGENTS.md` 的职责边界列表中包含 事务官行
- [ ] 事务官行的描述为 `事务官 执行文件机械操作，不参与设计决策`
- [ ] `## 8 Agent 体系总览` 表格存在，含 8 行 Agent 条目
- [ ] 表格中 事务官 的驱动模型列为 `快速模型 (Flash)`
- [ ] Feel 调度约束中的下游 Agent 列表已包含 事务官
- [ ] 原有的核心约束、编码规范、注释规范等节未被修改
- [ ] git diff 检查仅新增目标行，未意外修改其他内容
- [ ] 文件编码 UTF-8
