# op-003：flow advance 跳转失败增强诊断信息
- **阶段**：v1.0.0-stage-31
- **前置**：无（独立操作）
- **负责 Agent**：Executor
- **最多重试**：3
- **问题编号**：#6（跳转失败不显示当前 phase 和合法目标）
- **参考**：plan.md #问题 #6 详细技术方案
- **关联 REV**：REV-001（测试策略补充）

## 目标

在 `flow advance` 跳转校验失败时，额外输出当前 phase 和所有合法跳转目标，帮助用户快速定位问题。

## 知识库参考

- kb/architecture.md #Flow CLI 严格校验：非法 phase 拒绝推进的设计原则
- kb/patterns.md #CLI 国际化封装模式：新增诊断信息通过 i18n 体系输出

## 产出文件

- `src/commands/flow.ts`
- `src/core/i18n-data/zh-CN.ts`
- `src/core/i18n-data/en.ts`

## 实施步骤

### 步骤 1：修改 flow.ts advance 跳转失败处理

**当前代码**（`src/commands/flow.ts` L417-425）：
```typescript
      // 阶段跳跃保护：基于 stage phase 检查当前 phase 到目标 phase 是否存在直接路径
      if (!options.force) {
        const phaseResult = PipelinePhaseSchema.safeParse(options.to);
        if (phaseResult.success && !mgr.hasTransition(options.to, options.stage)) {
          console.error(t('flow.advance.errorPhaseJumpTmpl', lang, { stage: options.stage || '', to: options.to }));
          console.error(t('flow.advance.hintUseForce', lang));
          process.exit(1);
        }
      }
```

**修改为**：
```typescript
      // 阶段跳跃保护：基于 stage phase 检查当前 phase 到目标 phase 是否存在直接路径
      if (!options.force) {
        const phaseResult = PipelinePhaseSchema.safeParse(options.to);
        if (phaseResult.success && !mgr.hasTransition(options.to, options.stage)) {
          // 获取当前 phase 和合法目标列表
          const data = mgr.getData();
          const currentPhase = data?.stages[options.stage || '']?.phase ?? t('common.unknown', lang);
          const availableTargets = mgr.getAvailablePhases(options.stage);

          console.error(t('flow.advance.errorPhaseJumpTmpl', lang, { stage: options.stage || '', to: options.to }));
          console.error(t('flow.advance.currentPhaseTmpl', lang, { phase: currentPhase }));
          if (availableTargets.length > 0) {
            console.error(t('flow.advance.availableTargets', lang) + `: [${availableTargets.join(', ')}]`);
          } else {
            console.error(t('flow.advance.noAvailableTargets', lang));
          }
          console.error(t('flow.advance.hintUseForce', lang));
          process.exit(1);
        }
      }
```

- [ ] 使用 `edit` 工具替换 L417-425 的代码块

> **API 使用说明**：
> - `mgr.getAvailablePhases(stageId)` 已在 wizard L1057 验证可用，返回 `PipelinePhase[]`
> - `mgr.getData()?.stages[stageId]?.phase` 为标准 FlowManager API
> - `t('common.unknown', lang)` 作为 `??` 回退值，处理 stage 不存在的边界情况

### 步骤 2：新增 i18n 键

在 `zh-CN.ts` 的 flow advance 域（`advance.hintUseForce` L100 之后、`advance.warnSkipReview` L101 之前）新增 3 个键：

```typescript
'advance.currentPhaseTmpl':   { key: 'flow.advance.currentPhaseTmpl',   zh: '当前阶段 phase: {phase}',                en: '' },
'advance.availableTargets':   { key: 'flow.advance.availableTargets',   zh: '合法跳转目标',                           en: '' },
'advance.noAvailableTargets': { key: 'flow.advance.noAvailableTargets', zh: '当前 phase 无合法跳转目标',               en: '' },
```

在 `en.ts` 的对应位置（`advance.hintUseForce` L94 之后、`advance.warnSkipReview` L95 之前）新增：

```typescript
'advance.currentPhaseTmpl':   { key: 'flow.advance.currentPhaseTmpl',   zh: '', en: 'Current phase: {phase}' },
'advance.availableTargets':   { key: 'flow.advance.availableTargets',   zh: '', en: 'Available targets' },
'advance.noAvailableTargets': { key: 'flow.advance.noAvailableTargets', zh: '', en: 'No available targets from current phase' },
```

- [ ] zh-CN.ts：在 `advance.hintUseForce` 之后、`advance.warnSkipReview` 之前插入 3 个新键
- [ ] en.ts：在对应位置插入 3 个新键

### 执行顺序

步骤 1 先执行，步骤 2 后执行（代码引用新键名，必须 i18n 先就绪）。

## 自测清单

- [ ] **TypeScript 编译**：`npm run build` 零错误
- [ ] **i18n 对称性**：`openfeel lint i18n` 通过（新增 3 键对称）
- [ ] **现有测试无回归**：`npm test` 全部通过
- [ ] **验证 — 非法跳转诊断信息（REV-001 场景 1：done 阶段跳转）**：
  1. 选择一个 phase 为 `done` 的阶段
  2. 执行 `openfeel flow advance --stage <done-stage> --to exec_running`
  3. 确认错误输出包含：
     - `错误: 阶段 "<stage>" 当前 phase 无法跳转到 "exec_running"`
     - `当前阶段 phase: done`
     - `当前 phase 无合法跳转目标`（done 是终态，无合法跳转）
     - `使用 --force 强制执行`
- [ ] **验证 — 非法跳转诊断信息（REV-001 场景 2：中间阶段反向跳转）**：
  1. 选择一个 phase 为 `plan_passed` 的阶段
  2. 执行 `openfeel flow advance --stage <stage> --to plan_pending`（反向跳转）
  3. 确认错误输出包含：
     - 错误消息 + `当前阶段 phase: plan_passed`
     - `合法跳转目标: [scheme_pending, exec_running]`（或其他合法目标）
- [ ] **验证 — 合法跳转不受影响**：
  1. 对一个 `plan_pending` 阶段执行 `openfeel flow advance --stage <stage> --to plan_review`
  2. 确认正常推进，不额外输出诊断信息（仅输出 `✓ 已推进`）
- [ ] **验证 — stage 不存在边界情况**：
  1. 执行 `openfeel flow advance --stage nonexistent --to done`
  2. 确认 `currentPhase` 回退显示 `unknown`（不崩溃）

## REV-001 回应：测试策略补充

| 检测项 | 方法 | 覆盖 |
|--------|------|------|
| done 终态跳转拒绝 + 诊断 | CLI 手动验证（stderr 含当前 phase + "无合法跳转目标"） | 终态边界 |
| 中间阶段反向跳转 + 诊断 | CLI 手动验证（stderr 含当前 phase + 合法目标列表） | 常规错误路径 |
| 合法跳转不受影响 | CLI 手动验证（正常推进，无额外 stderr） | 正常路径回归 |
| stage 不存在边界 | CLI 手动验证（`common.unknown` 回退，不崩溃） | 边界防护 |
| i18n 对称性 | `openfeel lint i18n` 自动化 | 双语一致性 |

## 风险说明

| 风险 | 概率 | 缓解 |
|------|------|------|
| `getAvailablePhases()` 对不存在阶段返回空数组 | 低 | 外层 `if (!options.stage)` 已拦截空 stageId，且 advance 命令开头有 autoRepair |
| 多行输出增加 stderr 噪音 | 极低 | 仅错误路径触发，正常流程不受影响 |
