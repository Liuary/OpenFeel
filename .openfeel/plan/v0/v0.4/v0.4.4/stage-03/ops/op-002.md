# op-002：完善 update 的 AGENTS.md 语言同步

- **阶段**：v4.4-stage-03
- **前置**：op-001（hard — 依赖 `recordProjectLang()` 在 identity.ts 中的实现）
- **负责 Agent**：Executor
- **最多重试**：3
- **参考**：kb/patterns.md #双语 CLI 交互模式（init 选择 → .info.json 持久化 → update 读取三段式）、kb/patterns.md #向后兼容可选配置字段模式（只读访问器 + 默认值回退 + 不强制写入）、kb/patterns.md #CLI 国际化封装模式

## 目标

完善 `openfeel update` 命令的语言同步逻辑：首次部署时使用全局默认语言部署全部内容（含 AGENTS.md）；已有项目 update 且 `--lang` 与当前语言不同时提示用户确认后再覆盖 AGENTS.md；语言相同时跳过 AGENTS.md 覆盖仅更新框架内容。支持 `--force` 参数跳过确认。update 完成后自动调用 `recordProjectLang()` 记录项目语言映射。

## 技术决策

> **AGENTS.md 保守覆盖策略**：AGENTS.md 是项目根级文件，用户可能已自定义。遵循 kb/patterns.md #向后兼容可选配置字段模式——不强制覆盖、先确认再操作。
>
> **首次部署 vs 已有项目判定**：通过检测 `.opencode/agents/` 目录是否存在 + 是否为空来判定是否为首次部署。目录不存在或为空 → 首次部署 → 使用全局默认语言部署含 AGENTS.md。
>
> **项目语言映射记录时机**：`openfeel update` 成功后调用 `recordProjectLang()`，写入全局配置的 `projects` 表。该函数在 op-001 中实现。

## 实施步骤

### A. 修改 `updateProject()` — 新增 AGENTS.md 语言同步逻辑

- [ ] A1. 打开 `src/core/update.ts`
- [ ] A2. 在文件头部导入区域，新增导入：
  ```typescript
  import { existsSync, readFileSync } from 'node:fs';  // 如已存在则无需新增
  import { recordProjectLang, getGlobalConfig } from './workspace/identity.js';
  ```

  > 注意：`existsSync` 和 `readFileSync` 已在文件顶部导入（第8行），确认无需重复导入。

- [ ] A3. 修改 `updateProject()` 函数签名，增加 `options` 参数：
  ```typescript
  export function updateProject(
    projectPath: string,
    selectedTools: string[] = ["opencode"],
    lang: 'zh-CN' | 'en' = 'zh-CN',
    options?: { force?: boolean; interactive?: boolean }
  ): UpdateResult {
  ```

- [ ] A4. 在 `updateProject()` 函数开头（`mkdirSync(agentsDir, ...)` 之前或之后），新增 AGENTS.md 处理逻辑块：

  ```typescript
  // ── AGENTS.md 语言同步逻辑 ──
  const agentsDirExists = existsSync(agentsDir) && 
    (() => { try { return require('node:fs').readdirSync(agentsDir).length > 0; } catch { return false; } })();
  const agentsMdPath = resolve(projectPath, 'AGENTS.md');
  const agentsMdExists = existsSync(agentsMdPath);
  ```

  > 实际实现时应使用 `readdirSync` 而非简化的 IIFE，此处展示逻辑骨架。

  完整逻辑：

  ```typescript
  // 检测 agents/ 目录是否有内容（用于判定是否为首次部署）
  let agentsDirHasContent = false;
  try {
    agentsDirHasContent = existsSync(agentsDir) && readdirSync(agentsDir).length > 0;
  } catch {
    agentsDirHasContent = false;
  }

  const agentsMdPath = resolve(projectPath, 'AGENTS.md');
  const agentsMdExists = existsSync(agentsMdPath);

  if (!agentsDirHasContent && !agentsMdExists) {
    // 情况 1：首次部署 → 使用全局默认语言部署 AGENTS.md
    const globalConfig = getGlobalConfig();
    const deployLang = lang ?? globalConfig.lang ?? 'zh-CN';
    const agentsMdContent = loadTemplate(deployLang, 'agents-md');
    writeFileSync(agentsMdPath, agentsMdContent, 'utf-8');
    created.push('AGENTS.md');
  } else if (agentsMdExists && options?.lang) {
    // 情况 2：已有项目 + --lang 参数指定了语言
    // 读取项目当前语言
    // （从 .info.json 获取，此处复用已传入的 lang 参数和 getLang()）
    const projectLang = getLang(projectPath);
    const requestedLang = options.lang;
    
    if (requestedLang !== projectLang) {
      // 语言不同 → 提示确认
      if (options?.force) {
        // --force → 跳过确认，直接覆盖
        const agentsMdContent = loadTemplate(requestedLang, 'agents-md');
        writeFileSync(agentsMdPath, agentsMdContent, 'utf-8');
        updated.push('AGENTS.md');
      } else if (options?.interactive) {
        // 交互模式 → 等待用户确认（由命令层处理）
        // 核心层抛出"需要确认"信号
        throw new AgentsMdLangConflictError(projectLang, requestedLang);
      } else {
        // 非交互模式 → 输出警告，不覆盖
        console.warn(`[update] AGENTS.md language mismatch: project=${projectLang}, requested=${requestedLang}. Skipped. Use --force to override.`);
        skipped.push('AGENTS.md');
      }
    } else {
      // 语言相同 → 跳过 AGENTS.md，仅更新框架内容
      skipped.push('AGENTS.md (language unchanged)');
    }
  } else if (agentsMdExists) {
    // 情况 3：已有项目 + 无 --lang 参数 → 保持现状，跳过
    skipped.push('AGENTS.md (use existing)');
  }
  ```

- [ ] A5. 在 `updateProject()` 函数末尾（`return { created, updated, skipped }` 之前），新增项目语言映射记录：
  ```typescript
  // 记录项目语言映射到全局配置
  try {
    recordProjectLang(projectPath, lang);
  } catch {
    // 记录失败不影响主流程
  }
  ```

- [ ] A6. 在文件顶部（`interface UpdateResult` 附近）新增错误类：
  ```typescript
  /** AGENTS.md 语言冲突错误（由命令层捕获处理） */
  export class AgentsMdLangConflictError extends Error {
    constructor(
      public projectLang: string,
      public requestedLang: string,
    ) {
      super(`AGENTS.md language conflict: project=${projectLang}, requested=${requestedLang}`);
      this.name = 'AgentsMdLangConflictError';
    }
  }
  ```

### B. 修改 `commands/update.ts` — 新增 `--force` 和交互确认

- [ ] B1. 打开 `src/commands/update.ts`
- [ ] B2. 在已有 `.option('--lang <lang>', ...)` 之后新增一行 `.option()`：
  ```typescript
  .option('--force', '跳过 AGENTS.md 覆盖确认，直接覆盖')
  ```

- [ ] B3. 修改 `action` 回调，从 `options` 中解构 `force` 和 `lang`：
  ```typescript
  .action(async (path?: string, options?: { lang?: string; force?: boolean }) => {
    const targetPath = resolve(path ?? process.cwd());
    const lang = getCliLang(targetPath);
    const force = options?.force ?? false;
  ```

- [ ] B4. 在 `updateProject()` 调用处，传入 `options`：
  ```typescript
  const result = updateProject(targetPath, selectedTools, lang, { 
    force, 
    interactive: true,  // 命令层是交互模式
    lang: options?.lang  // 传递 --lang 参数
  });
  ```

- [ ] B5. 在 `try-catch` 块中新增 `AgentsMdLangConflictError` 的捕获处理：
  ```typescript
  } catch (err) {
    if (err instanceof AgentsMdLangConflictError) {
      // 语言冲突 → 提示用户确认
      console.warn(t('update.langConflictTmpl', getCliLang(targetPath), {
        projectLang: (err as AgentsMdLangConflictError).projectLang,
        requestedLang: (err as AgentsMdLangConflictError).requestedLang,
      }));
      // TODO: 交互确认后可重试（当前版本仅输出警告）
      process.exit(0);  // 非致命，正常退出
    }
    console.error(t('update.errorDeployFailedTmpl', lang, { message: (err as Error).message }));
    process.exit(1);
  }
  ```

- [ ] B6. 导入 `AgentsMdLangConflictError`：
  ```typescript
  import { updateProject, supportedTools, selectTools, AgentsMdLangConflictError } from '../core/update.js';
  ```

### C. 新增 i18n 键（update 域）

- [ ] C1. 打开 `src/core/i18n-data/zh-CN.ts`，在 `update` 域中新增：
  - `'langConflict'`：`{ key: 'update.langConflict', zh: '⚠️ AGENTS.md 语言差异：当前项目为 {projectLang}，请求部署为 {requestedLang}。使用 --force 覆盖或手动调整。', en: '' }`

- [ ] C2. 打开 `src/core/i18n-data/en.ts`，在 `update` 域中新增对应英文：
  - `'langConflict'`：`{ key: 'update.langConflict', zh: '', en: '⚠️ AGENTS.md language mismatch: project is {projectLang}, requested is {requestedLang}. Use --force to override or adjust manually.' }`

## 产出文件

| 类型 | 文件 | 说明 |
|------|------|------|
| **修改** | `src/core/update.ts` | AGENTS.md 语言同步逻辑 + recordProjectLang 调用 + 新增错误类 |
| **修改** | `src/commands/update.ts` | `--force` 参数 + 语言冲突异常捕获 |
| **修改** | `src/core/i18n-data/zh-CN.ts` | 新增 `update.langConflict` 中文键 |
| **修改** | `src/core/i18n-data/en.ts` | 新增 `update.langConflict` 英文键 |

## 自测清单

- [ ] 首次 update（无 `.opencode/agents/` 目录且无 AGENTS.md）→ 自动部署全局默认语言的 AGENTS.md
- [ ] 已有 en 项目执行 `openfeel update --lang zh-CN` → 输出语言差异警告（非交互模式）
- [ ] 已有 en 项目执行 `openfeel update --lang en` → 跳过 AGENTS.md，仅更新框架内容（Agent/Skills/core.md）
- [ ] 已有 en 项目执行 `openfeel update --lang zh-CN --force` → 直接覆盖 AGENTS.md 为中文版
- [ ] `openfeel update` 成功后 → `~/.openfeel/config.json` 的 `projects` 中新增/更新项目映射
- [ ] 同一项目相同语言多次 update → `projects` 映射不重复写入（`recordProjectLang` 幂等性）
- [ ] 非交互环境（CI）执行 `openfeel update --lang zh-CN`（语言不匹配）→ 输出警告但正常退出
- [ ] `openfeel help update` 显示 `--lang` 和 `--force` 选项
- [ ] `npm test` 全量通过
- [ ] `npm run build` 编译成功
