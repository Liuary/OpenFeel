# op-001: v5.6-stage-01 版本规范 + manual 模块文档 + 思考深度配置

## 目标

完成 v5.6-stage-01 的三项基础设施改进：

1. **版本号规范约束**：在 AGENTS.md 写入版本号语义（主.次.修订），在 feel.md 调度逻辑中规定新版本默认递增尾部版本号。
2. **模块文档系统 `.openfeel/manual/`**：建立分级模块文档系统（index.md 树图 + core/cli/agents 模块文档），由归档官在归档时维护。
3. **思考深度配置**：为 9 个 Agent 的 YAML frontmatter 增加 `reasoning_effort` 字段，区分思考深度，并同步中英模板。

## 实施步骤

- [x] 步骤1：创建 `.openfeel/manual/` 目录结构（index.md、core/flow-manager.md、core/config.md、cli/commands.md、agents/feel.md）
- [x] 步骤2：编写 `index.md` 模块树图索引（列出核心引擎/CLI 层/Agent 体系及层级关系）
- [x] 步骤3：编写 4 个模块初始文档（每个 20-30 行）：flow-manager（职责、核心 API、状态机）、config（配置层级、读写方法）、commands（命令注册、i18n 集成）、agents（9 Agent 体系、调度模型）
- [x] 步骤4：AGENTS.md 新增「版本管理」节（主.次.修订语义 + Feel 默认尾部版本递增）和「模块手册」约束（归档官归档时更新 manual 对应模块文档）
- [x] 步骤5：`.opencode/agents/feel.md` 调度逻辑中新增新版本递增规则（如 v5.6 → v5.7）
- [x] 步骤6：`.opencode/agents/archiver.md` 新增职责：归档时维护 `.openfeel/manual/`
- [x] 步骤7：9 个 `.opencode/agents/*.md` frontmatter 增加 `reasoning_effort` 字段（Planner/Schemer=high，Feel/Reviewer/Feel Tester=medium，Executor/事务官/Archiver/Vision=low）
- [x] 步骤8：同步 `src/core/templates-data/agents/{zh-CN,en}/*.md` 的 frontmatter（reasoning_effort + feel 版本递增 + archiver manual 职责）
- [x] 步骤9：运行 `npm run build` 和 `npm test` 验证无回归

## 产出文件

- `.openfeel/manual/index.md`（新增）
- `.openfeel/manual/core/flow-manager.md`（新增）
- `.openfeel/manual/core/config.md`（新增）
- `.openfeel/manual/cli/commands.md`（新增）
- `.openfeel/manual/agents/feel.md`（新增）
- `AGENTS.md`（修改：版本管理节 + 模块手册约束）
- `.opencode/agents/feel.md`（修改：版本递增规则）
- `.opencode/agents/archiver.md`（修改：manual 维护职责）
- `.opencode/agents/{planner,schemer,reviewer,feel-tester,executor,utility,vision}.md`（修改：reasoning_effort）
- `src/core/templates-data/agents/zh-CN/*.md`（修改：9 个文件同步）
- `src/core/templates-data/agents/en/*.md`（修改：9 个文件同步）

## 自测清单

- [x] `npm run build` 通过，无 TypeScript 编译错误
- [x] `npm test` 无回归
- [x] `.openfeel/manual/` 目录结构完整（index.md + core/ + cli/ + agents/）
- [x] AGENTS.md 包含「版本管理」节与「模块手册」约束
- [x] 9 个 `.opencode/agents/*.md` frontmatter 均含 `reasoning_effort`
- [x] 18 个模板文件（zh-CN/en 各 9 个）frontmatter 均含 `reasoning_effort`
- [x] `.opencode/agents/feel.md` 含版本递增调度逻辑
- [x] `.opencode/agents/archiver.md` 含 manual 维护职责

- **阶段**：exec_running
- **最多重试**：3

## 修正记录

| 时间 | 说明 | 操作者 |
|------|------|--------|
