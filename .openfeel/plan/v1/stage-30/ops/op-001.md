# op-001：flow-manager load() ops 类型守卫 + repair() ops 修复
- **阶段**：v1.0.0-stage-30
- **前置**：无（独立操作）
- **负责 Agent**：Executor
- **最多重试**：3
- **Bug 编号**：#1（flow status/current/overview 报告 "flow.json 不存在"）
- **参考**：plan.md #Bug #1 详细技术方案

## 目标

修复 `flow-manager.ts` 中 `load()` 方法因 `stage.ops` 为 `null`/`undefined`/非对象 时遍历崩溃，以及 `repair()` 方法缺失 ops 字段修复的问题。

## 知识库参考

- kb/troubleshooting.md #"手动 edit status.md 频繁失败 — 格式匹配脆弱"：本 Bug 与格式健壮性问题同源，核心思路是在数据加载/修复阶段增加防御性类型守卫

## 产出文件

- `src/core/flow-manager.ts`

## 实施步骤

### 步骤 1：`load()` 增加 ops 类型守卫（L259-264）

**当前代码**（`src/core/flow-manager.ts` L259-264）：
```typescript
if (this.data && this.data.stages) {
  for (const [, stage] of Object.entries(this.data.stages)) {
    for (const [opKey, op] of Object.entries(stage.ops)) {
      op.id = opKey;
    }
  }
}
```

**修改为**：
```typescript
if (this.data && this.data.stages) {
  for (const [, stage] of Object.entries(this.data.stages)) {
    // 类型守卫：仅当 ops 为普通对象时才遍历（跳过 null/undefined/数组）
    if (stage.ops && typeof stage.ops === 'object' && !Array.isArray(stage.ops)) {
      for (const [opKey, op] of Object.entries(stage.ops)) {
        op.id = opKey;
      }
    }
  }
}
```

- [x] 使用 `edit` 工具精确替换 L259-264 的代码块
- [x] 确保 `stage.ops` 三重检查：存在性（truthy）、类型（object）、排除数组

### 步骤 2：`repair()` 增加 ops 字段修复（在 stage phase 修复循环内）

**插入位置**：`src/core/flow-manager.ts`，在修复 stage phase 的循环内（L2077 `}` 之后、L2078 `}` 之前），即：
```
s.phase = 'plan_pending';
changes.push(`已补全缺失的 stages.${stageId}.phase`);
modified = true;
}                   // ← L2077: 闭合 if (s.phase) / else
// ← 在此处插入 ops 修复代码
}                   // ← L2078: 闭合 for 循环
```

**插入代码**：
```typescript
// 修复缺失或非对象的 ops 字段
if (!s.ops || typeof s.ops !== 'object' || Array.isArray(s.ops)) {
  s.ops = {};
  changes.push(`已为阶段 ${stageId} 补全缺失的 ops`);
  modified = true;
}
```

- [x] 在 `for (const [stageId, stage] of Object.entries(flowData.stages))` 循环内、phase 修复闭合后插入
- [x] 插入位置在 L2077 的 `}` 和 L2078 的 `}` 之间
- [x] 使用 `s` 变量（已在循环中声明为 `stage as unknown as Record<string, unknown>`）
- [x] 修复逻辑：ops 缺失或非普通对象时重置为 `{}`

### 执行顺序

步骤 1 和步骤 2 无相互依赖，可任意顺序执行。建议按步骤 1 → 步骤 2 顺序。

## 自测清单

- [ ] **TypeScript 编译**：`npm run build` 零错误
- [ ] **现有测试无回归**：`npm test` 全部通过（399/399）
- [ ] **Bug #1 验证 — 加载不崩溃**：
  - 手动构造含 `"ops": null` 的 stage 的 flow.json
  - 执行 `node dist/index.js flow status` → 不再崩溃，正常显示流水线状态
- [ ] **Bug #1 验证 — undefined ops**：
  - 构造一个 stage 但不含 `ops` 字段
  - 执行 `node dist/index.js flow current` → 正常输出，不报错
- [ ] **Bug #1 验证 — repair 修复**：
  - 对含 null ops 的 flow.json 执行 `node dist/index.js flow repair --dry-run` → 报告检测到缺失 ops
  - 执行 `node dist/index.js flow repair` → flow.json 中 ops 被修复为 `{}`
- [ ] **正常 flow.json 不受影响**：对已有合法 ops 的 flow.json 执行 flow status，功能不变

## 风险说明

| 风险 | 概率 | 缓解 |
|------|------|------|
| `repair()` 误改合法 ops | 极低 | 三重类型守卫仅在 ops 缺失/非对象时触发，已有合法 `{}` 不受影响 |
| `load()` 守卫过于宽泛 | 极低 | 三重检查精确过滤 null/undefined/数组，仅对普通对象放行 |

## 修正记录

| 类型 | 时间 | 操作者 | 说明 |
|------|------|--------|------|
| 自检 | 2026-08-09 | Executor | 方案一致性偏差（超范围 2 项）：`src/core/flow-manager.ts` summary()/getSummary() 两处 ops 计数、`src/commands/flow.ts` overview ops 遍历补类型守卫——自测发现 Bug #1 同源崩溃点（null ops 时 flow status/overview 崩溃于 `Object.keys(stage.ops)`），为满足自测清单「flow status/current/overview 不崩溃」的必要补充，与方案 load()/repair() 守卫同模式 |
