# op-004：init 命令完善

- **阶段**：v4.1-stage-01
- **前置**：无
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

两处改造：
1. `openfeel init` 在目标项目根目录创建 `AGENTS.md` 空骨架文件（含标准章节标题）；
2. `FlowManager.initFlow()` 创建的 `flow.json` 中 `pipeline.current` 从空值 `{ stage: '', op: '' }` 改为合法初始值 `{ stage: '-', op: 'init' }`。

## 知识库参考

- kb/patterns.md #CLI 原子管理模式
- kb/patterns.md #Executor 前置校验三步模式 — pipeline.current 空值导致 Executor 步骤 2 Phase 合法性校验断裂

## 实施步骤

### Part A：AGENTS.md 骨架

- [ ] **步骤 1**：read 本方案文件完整内容
- [ ] **步骤 2**：在 `src/core/templates.ts` 中新增 `AGENTS_MD_TEMPLATE` 导出常量，内容为 AGENTS.md 空骨架：
  ```markdown
  # {项目名称}

  > 本文档为 {项目名称} 核心约束层，跨平台统一适用。

  AI Agent 项目级行为约束与编码规范。本文件为永久性约束，适用于项目中所有 AI Agent 会话。

  ## 行为准则

  （待填充 — 描述项目期望的 AI Agent 行为风格）

  ## 核心约束

  （待填充 — 列出项目级约束规则）

  ## 操作规范

  （待填充 — 文件操作、编码风格等约定）

  ## 项目流程工具

  （待填充 — 列出项目使用的工具和命令）
  ```
- [ ] **步骤 3**：在 `src/core/init.ts` 的 `initProject()` 函数中，在第 7 步（生成 kb/index.md）之后添加第 8 步（原第 8 步顺延为第 9 步）：
  ```typescript
  // 8. 生成 AGENTS.md 骨架文件（项目根目录）
  const agentsMdPath = resolve(projectPath, 'AGENTS.md');
  const agentsMdResult = writeTemplateIfMissing(agentsMdPath, AGENTS_MD_TEMPLATE);
  if (agentsMdResult.created) {
    created.push('AGENTS.md');
  }
  ```
- [ ] **步骤 4**：确认 `src/core/init.ts` 顶部 import 已添加 `AGENTS_MD_TEMPLATE`：
  ```typescript
  import {
    DEV_CORE_TEMPLATE,
    CURRENT_TEMPLATE,
    AGENTS_MD_TEMPLATE,
  } from './templates.js';
  ```

### Part B：pipeline.current 初始值修复

- [ ] **步骤 5**：在 `src/core/flow-manager.ts` 中找到 `defaultFlowData()` 函数（约第 195 行），将 `pipeline.current` 从：
  ```typescript
  current: { stage: '', op: '' },
  ```
  改为：
  ```typescript
  current: { stage: '-', op: 'init' },
  ```
- [ ] **步骤 6**：同步检查 `src/core/flow-manager.ts` 中 `repair()` 方法的 `pipeline.current` 补全逻辑（约第 1521-1523 行），确认其默认值也更新为 `{ stage: '-', op: 'init' }`（若存在该逻辑）
- [ ] **步骤 7**：全局搜索 `pipeline.current.stage` 的使用位置，确认：
  - 所有依赖 `pipeline.current.stage` 非空的逻辑不受影响（`-` 作为哨兵值合法）
  - Feel Agent 读取 pipeline.current 时能正确处理 `stage: '-'`（表示"尚未分配阶段"）
- [ ] **步骤 8**：运行 `npm run build` 编译通过
- [ ] **步骤 9**：运行 `npm test` 确保测试通过（225+/227），特别关注 flow-manager 相关测试
- [ ] **步骤 10**：手动验证 `openfeel init`（在某临时目录）：
  - 确认 `AGENTS.md` 已创建且含标准章节标题
  - 确认 `.openfeel/flow.json` 中 `pipeline.current` 为 `{ "stage": "-", "op": "init" }`
  - 确认 Feel 首次 `flow advance` 不会报"未初始化"

## 产出文件

- `src/core/templates.ts` — 新增 `AGENTS_MD_TEMPLATE` 常量
- `src/core/init.ts` — import 新增 + initProject() 新增 AGENTS.md 创建步骤
- `src/core/flow-manager.ts` — defaultFlowData() 中 pipeline.current 默认值修复 + repair() 补全逻辑同步更新（如适用）

## 自测清单

- [ ] AGENTS.md 骨架文件含 `## 行为准则`、`## 核心约束`、`## 操作规范`、`## 项目流程工具` 四个标准章节标题
- [ ] init 在已有 AGENTS.md 的项目中不覆盖（skip）
- [ ] flow.json 初始 `pipeline.current` 为 `{ "stage": "-", "op": "init" }`（非空）
- [ ] `npm run build` 编译通过
- [ ] `npm test` 通过（225+/227），flow-manager 相关测试无回归
- [ ] opencode.jsonc 中 `instructions` 字段引用包含 `AGENTS.md`
- [ ] Feel 读取 `{ "stage": "-", "op": "init" }` 时能正常识别为"未分配阶段"状态

## 修正记录

| 次数 | 时间 | 问题 | 修正内容 |
|------|------|------|----------|
| 自检 | 2026-07-06 00:10 | `flow-manager.test.ts` 中 `initFlow` 测试期望 `getCurrent()` 为 null | 更新测试为 `toEqual({ stage: '-', op: 'init' })` |
