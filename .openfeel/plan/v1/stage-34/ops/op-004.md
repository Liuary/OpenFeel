# op-004：命令层 + init 路径统一（commands/stage.ts 映射修正 + init 示例阶段多级化）

- **阶段**：v1.0.0-stage-34
- **前置**：op-001（hard，复用 findStageStatusPath / planDirToStageId / parseStageId）；op-002（soft，findStatusPath 定版后语义对齐，文件不重叠可并行）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

修正 `stage status/set/task` 的 stageId→目录映射（原按 `**/{stageId}/status.md` 搜索无法命中多级目录 `stage-NN`）；`stage status`（无参）列表通过回查 flow.json 得到完整 stageId；init 示例阶段从平铺 `plan/stage-01/` 迁移到 `plan/v1/stage-01/` 并注册完整 ID。

## 实施步骤

### 步骤 1：改 `src/commands/stage.ts`

**新增 import**（第 17 行 `FlowManager` import 之后）：

```ts
import { findStageStatusPath, planDirToStageId, parseStageId } from '../core/plan/path.js';
```

**resolveStatusPath（38-57 行）替换**——委托 path.ts 三级回退（原 `**/${stageId}/status.md` 搜索在多级目录 `stage-NN` 下匹配失败）：

```ts
/**
 * 根据 stageId 解析 status.md 文件路径
 * 三级回退：plan/{series}/ 精确 → plan 递归 → stages 兜底（委托 path.ts）
 */
function resolveStatusPath(projectPath: string, stageId: string): string | null {
  return findStageStatusPath(projectPath, stageId);
}
```

> 原 `fastGlob.sync(\`**/${stageId}/status.md\`, ...)` 对 stageId `v1.0.0-stage-33` 会去找目录 `v1.0.0-stage-33`，但实际目录是 `stage-33`，匹配失败——这正是根因。委托 `findStageStatusPath` 后由 parseStageId 解析出 `stage-33` 再搜索。

**listAllStages（111-147 行）替换**——目录名反向映射回查 flow.json：

```ts
function listAllStages(projectPath: string): { stageId: string; statusPath: string; status: string }[] {
  const planDir = resolve(projectPath, '.openfeel', 'plan');
  if (!existsSync(planDir)) {
    return [];
  }

  // 搜索所有 status.md 文件
  const statusFiles = fastGlob.sync('**/status.md', {
    cwd: planDir,
    onlyFiles: true,
    caseSensitiveMatch: true,
  });

  const results: { stageId: string; statusPath: string; status: string }[] = [];

  for (const file of statusFiles) {
    // 从路径中提取目录名：.openfeel/plan/v1/stage-33/status.md → stage-33
    //                     .openfeel/plan/v4/v4-stage-04/status.md → v4-stage-04（历史）
    const parts = file.replace(/\\/g, '/').split('/');
    const rawDir = parts.length >= 3 ? parts[parts.length - 2] : parts[0];

    // 反向映射：目录名 → 完整 stageId（回查 flow.json 匹配 *-stage-NN）
    let stageId: string;
    const parsed = parseStageId(rawDir);
    if (parsed) {
      const full = planDirToStageId(projectPath, parsed.stageDir);
      stageId = full ?? parsed.fullStageId; // 回查失败兜底用解析结果
    } else {
      stageId = rawDir; // 无法解析时直接用目录名
    }

    try {
      const content = readFileSync(resolve(planDir, file), 'utf-8');
      const fields = parseStatusFields(content);
      results.push({
        stageId,
        statusPath: `.openfeel/plan/${file.replace(/\\/g, '/')}`,
        status: fields['状态'] || 'unknown',
      });
    } catch {
      // 文件读取失败时跳过
    }
  }

  // 按 stageId 排序
  results.sort((a, b) => a.stageId.localeCompare(b.stageId));
  return results;
}
```

> `stage status` 无参时输出从 `stage-33` 提升为完整 `v1.0.0-stage-33`（回查 flow.json）。`stage create`（382-403 行）走 `mgr.addStage(stageId)`，参数已是完整 stageId，无需改。

### 步骤 2：改 `src/core/init.ts`

**新增 import**（第 20 行 template-loader import 之后）：

```ts
import { DEFAULT_STAGE_VERSION } from './plan/path.js';
```

**262 行注释修正**：

```ts
  // 1. 创建 .openfeel/ 目录结构（含 plan/, roadmap/, dev/note/, 等）
```

（原为「含 plan/, stages/, roadmap/」，去掉 `stages/`，因 structure.ts WORKSPACE_DIRS 本就不含 stages。）

**initDemo 示例阶段（457-475 行）替换**：

```ts
  // .openfeel/plan/v1/stage-01/status.md — 示例阶段（双语）
  // 示例阶段版本前缀抽为常量（path.ts DEFAULT_STAGE_VERSION），短名规范化与 init 保持一致
  const demoStageId = `${DEFAULT_STAGE_VERSION}-stage-01`; // v1.0.0-stage-01
  const statusMdContent = lang === 'en'
    ? `# ${demoStageId} Status\n\n- **Status**: planned\n- **Current Agent**: executor\n- **Previous Agent**: none\n- **Updated**: ${new Date().toISOString().substring(0, 16).replace('T', ' ')}\n\n## Current Task\n\nInitialize project skeleton, create basic file structure.\n\n## Status Log\n\n| Time | Agent | Status Change | Description |\n|------|-------|---------------|-------------|\n| - | - | - | Sample stage |\n`
    : `# ${demoStageId} 状态\n\n- **状态**：planned\n- **当前责任 Agent**：executor\n- **上一责任 Agent**：none\n- **更新时间**：${new Date().toISOString().substring(0, 16).replace('T', ' ')}\n\n## 当前任务\n\n初始化项目骨架，创建基础文件结构。\n\n## 状态记录\n\n| 时间 | Agent | 状态变化 | 说明 |\n|------|-------|----------|------|\n| - | - | - | 示例阶段 |\n`;
  ensureFile('.openfeel/plan/v1/stage-01/status.md', statusMdContent);
```

**473 行注册改完整 stageId**：

```ts
  // 在 flow.json 中注册 v1.0.0-stage-01
  const flowMgr = new FlowManager(projectPath);
  if (flowMgr.isLoaded()) {
    flowMgr.registerStage(demoStageId, []);
    flowMgr.save();
  }
```

（原 `registerStage('stage-01', [])` → `registerStage(demoStageId, [])`。）

### 步骤 3：init.test.ts 新增断言

在 `test/core/init.test.ts` 末尾新增 `describe('initDemo')` 测试块：

```ts
describe('initDemo', () => {
  let tmpDir: string;

  beforeEach(() => {
    tmpDir = mkdtempSync(join(tmpdir(), 'openfeel-init-demo-test-'));
  });

  afterEach(() => {
    rmSync(tmpDir, { recursive: true, force: true });
  });

  it('应部署多级示例阶段 plan/v1/stage-01/status.md', async () => {
    // 先初始化工作区（确保 flow.json 存在）
    await initProject(tmpDir);
    const { initDemo } = await import('../../src/core/init.js');
    initDemo(tmpDir, 'zh-CN');

    // 断言多级路径存在
    expect(existsSync(join(tmpDir, '.openfeel', 'plan', 'v1', 'stage-01', 'status.md'))).toBe(true);

    // 断言 flow.json 注册完整 stageId
    const flowData = JSON.parse(readFileSync(join(tmpDir, '.openfeel', 'flow.json'), 'utf-8'));
    expect(flowData.stages['v1.0.0-stage-01']).toBeDefined();
    expect(flowData.stages['stage-01']).toBeUndefined();

    // 断言 status.md 标题为完整 stageId
    const statusContent = readFileSync(join(tmpDir, '.openfeel', 'plan', 'v1', 'stage-01', 'status.md'), 'utf-8');
    expect(statusContent).toContain('# v1.0.0-stage-01 状态');
  });
});
```

> 注：`initDemo` 已在 init.ts 顶部 import 或直接 `await import` 均可；建议测试文件顶部静态 import `initDemo`（`import { initProject, deployOpencode, initDemo } from '../../src/core/init.js'`）。

## 产出文件

- `src/commands/stage.ts`
- `src/core/init.ts`
- `test/core/init.test.ts`

## 自测清单

- [ ] `resolveStatusPath` 已委托 `findStageStatusPath`，`stage status v1.0.0-stage-33` 能命中 `plan/v1/stage-33/status.md`
- [ ] `listAllStages` 反向映射回查 flow.json，输出完整 stageId（`v1.0.0-stage-33` 而非 `stage-33`）
- [ ] initDemo 示例阶段部署到 `plan/v1/stage-01/`，注册 `v1.0.0-stage-01`，status.md 标题 `v1.0.0-stage-01 状态`
- [ ] 262 行注释已去掉 `stages/`
- [ ] `npm test` 中 init.test.ts（含新增 initDemo 测试）+ stage 命令冒烟通过
- [ ] i18n 键 `status.noStages` 文本「.openfeel/plan/ 中无 status.md」无需改（plan/ 目录未变，已由 Reviewer 确认）

## 验证

- op-004 完成后执行 `npm test`，确认 init.test.ts（新增 initDemo 断言）+ flow-manager.test.ts（op-002 已改）+ stage 命令相关测试通过。

## 困难点说明

- **listAllStages 历史目录兼容**：OpenFeel 自身存量 `plan/v4/v4-stage-04/`（目录名含版本前缀）与多级 `plan/v1/stage-33/` 并存。提取 `rawDir` 后先 `parseStageId` 归一化出 `stageDir`，再 `planDirToStageId` 回查；`v4-stage-04` 归一化为 `stage-04` 后回查命中 `v4-stage-04`，展示仍正确。
- **示例阶段版本前缀**（遗留风险 5）：抽 `DEFAULT_STAGE_VERSION` 常量（path.ts），与短名规范化共源，避免 initDemo 与 path.ts 各写一份 `v1.0.0` 魔法字符串。
