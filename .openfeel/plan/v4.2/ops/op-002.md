# op-002：新增 `openfeel project overview` CLI 命令

- **阶段**：v4.2-stage-01
- **前置**：无（可并行执行 op-001）
- **负责 Agent**：Executor
- **最多重试**：3

## 目标

新增 `openfeel project overview` CLI 命令，实时扫描项目目录结构，输出结构化概览（目录树、核心模块、入口路径、统计信息），零维护 100% 准确。

## 背景

参考 `.openfeel/plan/v4.2/plan.md` 方案 B：Agent 启动时调用 `openfeel project overview` 即可获得实时、准确的项目结构，无需手动维护索引文件。纯代码驱动，每次运行实时扫描。

## 实施步骤

- [ ] 1. 创建 `src/commands/project.ts` 文件，实现 `registerProjectCommand` 函数。

参照现有模式（如 `src/commands/view.ts`），使用 Commander 注册 `project overview` 命令组。

文件结构：
```typescript
/**
 * project 命令组注册
 * openfeel project overview  — 实时扫描项目结构，输出结构化概览
 */
import { Command } from 'commander';
import { existsSync, readFileSync } from 'node:fs';
import { resolve } from 'node:path';
import { globSync } from 'fast-glob';

export function registerProjectCommand(program: Command): void {
  const project = program
    .command('project')
    .description('项目管理与概览');

  // project overview — 实时扫描并输出结构化项目概览
  project
    .command('overview')
    .description('实时扫描项目结构，输出结构化概览')
    .action(() => {
      const cwd = process.cwd();
      outputProjectOverview(cwd);
    });
}
```

- [ ] 2. 实现 `outputProjectOverview(cwd: string)` 函数，输出以下结构化信息：

```
╔══════════════════════════════════════════╗
║     OpenFeel 项目结构化概览              ║
╚══════════════════════════════════════════╝

📋 基本信息
   项目名: openfeel (v{x.y.z})
   定位:   AI Agent 开发流程治理 CLI 工具
   语言:   TypeScript (Node.js)

📁 目录结构
   src/
    ├─ cli/          — CLI 入口程序
    ├─ commands/     — CLI 命令模块（{N} 个）
    ├─ core/         — 核心逻辑
    └─ utils/        — 工具函数
   .opencode/
    ├─ agents/       — Agent 定义（{N} 个）
    └─ skills/       — 技能定义
   .openfeel/
    ├─ kb/           — 项目知识库
    ├─ plan/         — 工作计划
    └─ code_review/  — 代码审查记录
    └─ bugs/         — Bug 追踪

📊 统计信息
   TS 源文件:   {N} 个
   Agent 定义:  {N} 个
   CLI 命令模块: {N} 个
   KB 条目:     {N} 个
   计划版本:    {N} 个

🚪 入口路径
   CLI 入口:  src/cli/index.ts
   包入口:    src/index.ts
   构建产物:  dist/

🔧 技术栈
   运行时:    Node.js ≥20
   语言:      TypeScript 5.7+
   CLI 框架:  Commander 14+
   校验:      Zod 4+
   配置:      YAML
   文件匹配:  fast-glob
   测试:      Vitest 3+
```

- [ ] 3. 扫描实现细节：

   a. **基本信息**：读取 `package.json` 获取 `name` 和 `version`。
   b. **目录结构**：硬编码关键目录树（src/、.opencode/、.openfeel/ 三棵子树），目录节点旁标注文件/子目录数量。使用 `existsSync` 检查目录存在性，使用 `globSync` 统计文件数。
   c. **统计信息**：
      - TS 源文件：`globSync(['src/**/*.ts'], { cwd }).length`
      - Agent 定义：`globSync(['.opencode/agents/*.md'], { cwd }).length`
      - CLI 命令模块：`globSync(['src/commands/*.ts'], { cwd }).length`
      - KB 条目：读取 `.openfeel/kb/index.md` 解析分类表格的条目数求和
      - 计划版本：`globSync(['.openfeel/plan/*/plan.md'], { cwd }).length`
   d. **入口路径**：硬编码（项目架构稳定）
   e. **技术栈**：读取 `package.json` 的 `dependencies` 和 `devDependencies`，提取关键包名和版本。

- [ ] 4. 在 `src/cli/index.ts` 中注册新命令。定位到文件末尾（`registerStageCommand(program);` 之后），追加：

```typescript
import { registerProjectCommand } from '../commands/project.js';
registerProjectCommand(program);
```

- [ ] 5. 构建并验证命令可用：

```bash
npm run build
node bin/openfeel.js project overview
```

- [ ] 6. 确保 `node bin/openfeel.js project --help` 和 `node bin/openfeel.js project overview --help` 输出正确。

## 产出文件

| 文件 | 操作 |
|------|------|
| `src/commands/project.ts` | 新增 |
| `src/cli/index.ts` | 修改（追加 import + register 调用） |

## 依赖版本锁定

| 包名 | 版本 | 用途 | 选定依据 |
|------|------|------|----------|
| commander | 14.0.0（已安装） | CLI 框架 | package.json 现有依赖 |
| fast-glob | 3.3.3（已安装） | 文件匹配/统计 | package.json 现有依赖 |

> 仅使用项目已有依赖，无新增依赖。

## 涉及知识库条目

| 条目 | 来源 | 与当前任务关系 |
|------|------|:--|
| CLI 命令注册模式 | `src/commands/` 现有代码 | 参照 `view.ts` / `archive.ts` 的 `export function registerXxx(program)` 模式 |
| 输出格式规范 | `src/commands/flow.ts` overview 子命令 | 参照其边框和图标输出风格 |

## 自测清单

- [ ] `src/commands/project.ts` 文件存在，包含 `registerProjectCommand` 导出函数
- [ ] `src/cli/index.ts` 中已 import 并调用 `registerProjectCommand`
- [ ] `npm run build` 编译通过，无 TypeScript 错误
- [ ] `node bin/openfeel.js project overview` 输出包含 "基本信息"、"目录结构"、"统计信息"、"入口路径"、"技术栈" 五个节
- [ ] 输出中的 TS 源文件数 ≥ 38
- [ ] 输出中的 Agent 定义数 = 8
- [ ] 输出中的 CLI 命令模块数 ≥ 10（含新增的 project.ts）
- [ ] `node bin/openfeel.js project --help` 显示 project overview 子命令
- [ ] `node bin/openfeel.js project overview --help` 显示命令描述
- [ ] 命令在项目根目录外的目录执行时不会崩溃（空项目场景容忍）
