# op-005：CI/CD 集成

- **阶段**：v1.0.0-stage-02（发布工程）
- **最多重试**：3

## 目标

创建 `.github/workflows/ci.yml` GitHub Actions 工作流：push/PR 触发，Node.js 20.x/22.x 双版本矩阵，`npm ci` → `npm run build` → `npm test`。

## 实施步骤

- [ ] 1. 确认 `.github/workflows/` 目录不存在则创建
- [ ] 2. 创建 `.github/workflows/ci.yml`（push/PR 触发 + 20.x/22.x 矩阵 + checkout@v4 + setup-node@v4 + npm ci + build + test）
- [ ] 3. 验证 `.gitignore` 未忽略 `.github/`
- [ ] 4. 运行 `npm run build` + `npm test` 确认本地全通过（模拟 CI 步骤）

## 产出文件

- `.github/workflows/ci.yml`（新建）

## 自测清单

- [ ] ci.yml 存在且结构正确（on、jobs、strategy.matrix、steps 齐全）
- [ ] Node 矩阵含 20.x 与 22.x
- [ ] 步骤顺序：checkout → setup-node → npm ci → build → test
- [ ] `.gitignore` 未忽略 `.github/`
- [ ] `npm run build` + `npm test` 本地通过

## 偏差记录

- （待回写）
